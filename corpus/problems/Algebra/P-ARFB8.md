---
schema: qual/card@1
id: P-ARFB8
kind: problem
title: $\CC^2$ as a cyclic torsion $\CC[x]$-module via $\begin{pmatrix}1&2\\2&1\end{pmatrix}$
classification:
  areas:
  - algebra
  topics:
  - Modules
  - Cyclic Groups
  - Minimal and Characteristic Polynomials
relations: []
review: draft
audit:
- event: solution-written
  by: gemini-3.7-flash
  date: 2026-08-30
---

::: problem
Let $R = \CC[x],~ M = \CC^2$, and
$$
A = 
\left[\begin{array}{ccc}
1 & 2 \\ 2 & 1
\end{array}\right]
\in M_2(\CC).
$$

Then $\CC^2$ is a module over $\CC[x]$ with action given by
$$
p(x) \actson \vector v \definedas p(A)\vector v
$$

Then $M$ is cyclic as an $R\dash$module and generated by the basis vector $[1, 0]^2 \in \CC^2$, since
\[
\begin{align*}
(tA + s)
\left[\begin{array}{cc}
1 \\ 0
\end{array}\right]
&= 
\left[\begin{array}{cc}
x \\ y
\end{array}\right]
\\
\implies
\left[\begin{array}{cc}
t & 2t \\ 2t & t
\end{array}\right]
\left[\begin{array}{cc}
1 \\ 0
\end{array}\right]
\left[\begin{array}{cc}
s \\ 0
\end{array}\right]
&=
\left[\begin{array}{cc}
x \\ y
\end{array}\right] \\
\implies 
\left[\begin{array}{cc}
t + s \\ 2t
\end{array}\right]
&=
\left[\begin{array}{cc}
x \\ y
\end{array}\right] \\
\implies 
\left[\begin{array}{cc}
1 & 1 \\ 2 & 0
\end{array}\right]
\left[\begin{array}{cc}
t \\ s
\end{array}\right] 
&=
\left[\begin{array}{cc}
x \\ y
\end{array}\right] \\
,\end{align*}
\]

which is a linear system of equations represented by an invertible matrix, which always has a solution.
So every $\vector v\in \CC^2$ is the image of some polynomial in $A$.

It is then easy to see that $\CC^2$ is torsion as a module over $\CC[x]$, since by Cayley-Hamilton we have $\mathrm{Ann}(A) = (\mathrm{minpoly}(A)) = (x^2-2x-3)$, and so letting $p(x) = x^2-2x-3$, we find that
$$
\forall~ \vector v\in \CC^2 \quad p(A) \actson \vector v = 0 \actson \vector v = 0.
$$
:::

::: {.solution}
<1>1. $M = \CC^2$ is a $\CC[x]$-module via $p(x) \cdot v = p(A)v$.
::: {.proof}
definition of the module structure.
:::

<1>2. $M$ is cyclic, generated by $e_1 = (1, 0)^t$.
::: {.proof}
for any $v = (x, y)^t$, the equation $(tA + s)e_1 = v$ is the linear system $\begin{pmatrix} 1 & 1 \\ 2 & 0 \end{pmatrix}\begin{pmatrix} t \\ s \end{pmatrix} = \begin{pmatrix} x \\ y \end{pmatrix}$, whose matrix is invertible (determinant $-2 \neq 0$), so it always has a solution; hence $v = (tA + s)e_1 = (tx + s)e_1$ is in the cyclic submodule generated by $e_1$.
:::

<1>3. $M$ is torsion.
::: {.proof}
by Cayley–Hamilton, $A$ satisfies its characteristic polynomial $\chi_A(x) = x^2 - 2x - 3$ (since $\operatorname{tr} A = 2$ and $\det A = -3$), so $p(x) = x^2 - 2x - 3$ annihilates every $v \in M$: $p(A)v = 0$ for all $v$.
:::

<1>4. Hence $\operatorname{Ann}(M) = (x^2 - 2x - 3)$, and $M$ is a cyclic torsion $\CC[x]$-module.
::: {.proof}
<1>2 and <1>3.
:::

<1>5. Q.E.D.
::: {.proof}
<1>4.
:::
:::
