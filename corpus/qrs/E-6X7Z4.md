---
schema: qual/card@1
id: E-6X7Z4
kind: exercise
title: Fourier transform is injective on $L^1$, but inversion need not hold pointwise
classification:
  areas:
  - real-analysis
  topics:
  - Fourier Analysis
  - L¹
relations: []
review: draft
audit:
- event: solution-written
  by: gemini-3.7-flash
  date: 2026-08-16
---

::: exercise
- Show that if $f\in L^1(\RR)$ and $\hat f = 0$, then $f = 0$ almost everywhere.

- Show that Fourier inversion formula $f(x) = \int_\RR \hat f(\xi) e^{2\pi i x \xi}\, d\xi$ does not necessarily hold pointwise for arbitrary $f \in L^1(\RR)$ (since $\hat f$ need not be in $L^1(\RR)$).
:::

::: {.solution}
**(1) Injectivity of the Fourier Transform on $L^1(\RR)$ ($f \in L^1$ and $\hat f = 0 \implies f = 0$ a.e.):** Let $f \in L^1(\RR)$ with $\hat f(\xi) = \int_\RR f(x) e^{-2\pi i \xi x}\, dx = 0$ for all $\xi \in \RR$.
Consider the Gaussian summability kernel (approximations to the identity):
$$
\Phi_\varepsilon(x) = \frac{1}{\varepsilon} \Phi\left(\frac{x}{\varepsilon}\right) = \frac{1}{\varepsilon} e^{-\pi (x/\varepsilon)^2}, \qquad \text{with } \widehat{\Phi_\varepsilon}(\xi) = e^{-\pi \varepsilon^2 \xi^2}.
$$
By standard Fourier analysis (multiplication formula / convolution theorem):
$$
(f * \Phi_\varepsilon)(x) = \int_\RR f(y) \Phi_\varepsilon(x - y)\, dy = \int_\RR \hat f(\xi) e^{2\pi i x \xi} \widehat{\Phi_\varepsilon}(\xi)\, d\xi.
$$
Since $\hat f(\xi) = 0$ everywhere, the right-hand side is identically $0$, so:
$$
(f * \Phi_\varepsilon)(x) = 0 \quad \text{for all } x \in \RR \text{ and all } \varepsilon > 0.
$$
Since $\Phi_\varepsilon$ is an approximate identity on $L^1(\RR)$, as $\varepsilon \to 0^+$:
$$
\|f * \Phi_\varepsilon - f\|_{L^1} \to 0.
$$
Because $f * \Phi_\varepsilon = 0$ for all $\varepsilon > 0$, we have:
$$
\|0 - f\|_{L^1} = \|f\|_{L^1} = 0.
$$
Thus $f(x) = 0$ almost everywhere on $\RR$.

**(2) Failure of Pointwise Fourier Inversion for Arbitrary $L^1$ Functions:** For Fourier inversion $f(x) = \int_\RR \hat f(\xi) e^{2\pi i x \xi}\, d\xi$ to hold as an ordinary Lebesgue integral, $\hat f$ must be integrable ($\hat f \in L^1(\RR)$). However, for an arbitrary $f \in L^1(\RR)$, $\hat f$ is continuous and vanishes at infinity (Riemann-Lebesgue lemma), but $\hat f$ is **not** necessarily in $L^1(\RR)$.

*Counterexample Specimen:* Let $f = \mathbf{1}_{[-1, 1]} \in L^1(\RR)$.
Computing the Fourier transform:
$$
\hat f(\xi) = \int_{-1}^1 e^{-2\pi i x \xi}\, dx = \frac{e^{2\pi i \xi} - e^{-2\pi i \xi}}{2\pi i \xi} = \frac{\sin(2\pi \xi)}{\pi \xi}.
$$
The function $\hat f(\xi) = \frac{\sin(2\pi \xi)}{\pi \xi}$ decays like $O(1/|\xi|)$ as $|\xi| \to \infty$, which is **not** Lebesgue integrable on $\RR$:
$$
\int_\RR |\hat f(\xi)|\, d\xi = \int_\RR \left| \frac{\sin(2\pi \xi)}{\pi \xi} \right| d\xi = \infty.
$$
Hence the classical inversion integral $\int_\RR \hat f(\xi) e^{2\pi i x \xi}\, d\xi$ does not exist in the Lebesgue sense for arbitrary $L^1$ functions.
Furthermore, at points of discontinuity (such as $x = \pm 1$), the principal value limit equals $1/2 \neq f(\pm 1) = 1$.
:::
