---
schema: qual/card@1
id: FE-CRVQUAD
kind: example
title: Curves of type $(a,b)$ on a quadric surface
classification:
  areas:
  - algebraic-geometry
  topics:
  - Quadric Surface
  - Curves
  - Intersection Theory
relations:
- kind: uses
  target: T-SRFADJ
review: draft
prompts:
- What are the curves on a smooth quadric surface?
- Compute the genus of a curve of type $(a,b)$.
- What happens on a quadric cone?
---

::: {.example}
A smooth quadric $Q \subseteq \PP^3$ is $\PP^1 \times \PP^1$, so $\Pic Q = \ZZ^2$ generated by the two rulings, with $A^2 = B^2 = 0$ and $A \cdot B = 1$.
A curve $C$ of type $(a,b)$ meets a plane in $a+b$ points and has
\[
\deg C = a+b , \qquad g(C) = (a-1)(b-1) = ab - a - b + 1 ,
\]
using $K_Q = (-2,-2)$ and adjunction $2g-2 = C \cdot (C + K_Q)$.
Smooth irreducible curves of type $(a,b)$ exist for all $a, b \geq 1$.
:::

::: {.example title="The quadric cone"}
On a cone $Q$ the class group is $\ZZ$, generated by a ruling line $L$ with $2L$ cut by a plane.
A curve of even degree $d = 2a$ is the complete intersection of $Q$ with a surface of degree $a$ and has $g = (a-1)^2$; a curve of odd degree $d = 2a+1$ has $g = a^2 - a$.
:::

::: {.remark}
This is the single most useful family of examples in the subject, because it produces a curve of every genus on demand: type $(g+1, 2)$ gives degree $g+3$ and genus $g$, which is how one proves curves of every genus exist.

It also explains the two-family phenomena in $\PP^3$.
The twisted cubic is type $(1,2)$; the elliptic quartic is type $(2,2)$; the canonical genus-$4$ sextic is type $(3,3)$, and the two rulings restrict to its two $g^1_3$'s. Whether a given curve lies on a quadric is a cohomological question about $h^0(\mci_C(2))$, and it is exactly the invariant that separates the two families of curves with $\deg = 9$ and $g = 10$.
:::
