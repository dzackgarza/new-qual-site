---
schema: qual/card@1
id: D-FULSAT
kind: definition
title: Saturated semigroups and the normalisation of an affine toric variety
classification:
  areas:
  - algebraic-geometry
  topics:
  - Toric Varieties
  - Normal Varieties
  - Semigroup Rings
relations:
- kind: uses
  target: PR-TORSMAFF
review: draft
prompts:
- What does it mean for an affine semigroup to be saturated, and how does that relate to normality?
- Compute the normalisation of an affine toric variety presented by a semigroup.
---

::: {.definition title="Saturated"}
An affine semigroup $S \subseteq M$ is \dfn{saturated} if
\[
k \in \ZZ_{>0}, \quad m \in M, \quad km \in S \implies m \in S .
\]
$\Spec k[S]$ is normal exactly when $S$ is saturated in the lattice $\ZZ S$ it generates, and $S_\sigma = \sigma\dual \intersect M$ is saturated for every cone, since its defining inequalities are homogeneous.
:::

::: {.example title="A semigroup that is not saturated"}
Let $S$ be generated by $(4,0), (3,1), (1,3), (0,4)$ inside $M = \ZZ^2$.
Then $2 \cdot (2,2) = (4,4) = (4,0) + (0,4) \in S$, but $(2,2) \notin S$, so $S$ is not saturated and $\Spec k[S]$ is not normal.
:::

::: {.remark title="The normalisation recipe"}
Given a non-normal affine toric variety $X = \Spec k[S]$, set $M = \ZZ S$, take any finite generating set $S'$ of $S$, and put
\[
\sigma \da \Cone(S')\dual \subseteq N_\RR .
\]
Then $\sigma\dual \intersect M$ is the saturation of $S$, and
\[
\Spec k[\sigma\dual \intersect M] \longrightarrow X
\]
is the normalisation.
It is finite and birational, and it is the identity exactly when $S$ was already saturated.
:::

::: {.remark title="Why this matters for the dictionary"}
Fans produce normal toric varieties and nothing else.
A torus acting with a dense orbit on a variety $X$ makes $X$ a toric variety in the loose sense, but $X$ appears in a fan only after normalisation.
The cuspidal cubic $V(x^3 - y^2)$ is the standard example.
Its torus is $\ts{(t^2, t^3) \st t \in \GG_m}$, so $S = \gens{2,3} \subseteq \ZZ$, which is not saturated because $2 \cdot 1 \in S$ while $1 \notin S$.
The recipe returns $\NN \subseteq \ZZ$ and the normalisation $\AA^1 \to V(x^3 - y^2)$, $t \mapsto (t^2, t^3)$.
:::
