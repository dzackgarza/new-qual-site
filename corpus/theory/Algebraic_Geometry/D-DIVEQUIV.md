---
schema: qual/card@1
id: D-DIVEQUIV
kind: definition
title: Linear, algebraic, and numerical equivalence
classification:
  areas:
  - algebraic-geometry
  topics:
  - Numerical Equivalence
  - Neron-Severi Group
  - Nef Divisors
relations:
- kind: uses
  target: D-5PQ5W
- kind: uses
  target: D-DIVAMPLE
review: draft
prompts:
- What are the three equivalence relations on divisors?
- What is the Neron--Severi group?
- What is a nef divisor? A big divisor?
---

::: {.definition}
Let $X$ be a smooth projective variety, with intersection pairing $D \cdot C = \deg \restrictionof{\OO_X(D)}{C}$ for $C$ an irreducible curve.

- $D_1 \sim D_2$ (linear) when $D_1 - D_2 = \div(f)$;

- $D_1 \sim_{\mathrm{alg}} D_2$ (algebraic) when the two sit in one connected family of divisors on $X$;

- $D_1 \equiv D_2$ (numerical) when $(D_1 - D_2) \cdot C = 0$ for every curve $C \subseteq X$.
:::

::: {.definition}
$\NS(X) = \Div(X)/{\sim_{\mathrm{alg}}} = \Pic(X)/\Pic^0(X)$, a finitely generated abelian group; its rank is the Picard number $\rho(X)$.
$D$ is nef when $D \cdot C \geq 0$ for every curve $C$, and big when $h^0(\OO(mD))$ grows like $m^{\dim X}$.
:::

::: {.remark}
The implications go one way, $\sim \implies \sim_{\mathrm{alg}} \implies \equiv$, and each gap is worth naming.
Linear equivalence classes form $\Pic$, which can be positive-dimensional; quotienting by the connected component $\Pic^0$ leaves the discrete group $\NS$, finitely generated by the theorem of the base.
Quotienting further by numerical equivalence kills only torsion.

The point of the coarser relations is that ampleness and nefness are numerical, so they are questions about the finitely generated lattice $\NS(X)$ with its intersection form — which is what makes positivity computable at all.
On a curve all three relations agree with equality of degree on $\Pic^0$-cosets, so $\NS = \ZZ$ by degree and nef means $\deg \geq 0$: nef is the closure of ample, and big is ample up to a correction supported on a proper subvariety.
:::
