---
schema: qual/card@1
id: P-6NMU2
kind: problem
title: Galois group and intermediate fields of the splitting field of $x^3-5$
classification:
  areas:
  - algebra
  topics:
  - Galois Theory
  - Splitting Fields
  - Field Extensions
relations: []
review: draft
audit:
- event: solution-written
  by: OpenAI
  date: 2026-09-11
- event: solution-reviewed
  by: OpenAI
  date: 2026-09-11
---

::: {.problem}
Set $f(x) = x^3 - 5 \in \QQ[x]$.

a. Find the splitting field $K$ of $f(x)$ over $\QQ$.

b. Find the Galois group $G$ of $K$ over $\QQ$.

c. Exhibit explicitly the correspondence between subgroups of $G$ and intermediate fields between $\QQ$ and $K$.
:::

::: {.solution}
Let
\[
\alpha=\sqrt[3]{5}\in\mathbb R
\qquad\text{and}\qquad
\zeta=e^{2\pi i/3}.
\]
The roots of $x^3-5$ are
\[
\alpha,\quad \zeta\alpha,\quad \zeta^2\alpha.
\]

<1>1. The splitting field is
\[
K=\mathbb Q(\alpha,\zeta),
\]
and
\[
[K:\mathbb Q]=6.
\]
::: {.proof}
The polynomial $x^3-5$ is Eisenstein at $5$, hence irreducible over $\mathbb Q$. Therefore
\[
[\mathbb Q(\alpha):\mathbb Q]=3.
\]
Adjoining $\zeta$ gives all three roots, so $K=\mathbb Q(\alpha,\zeta)$ is the splitting field.

The field $\mathbb Q(\alpha)$ is contained in $\mathbb R$, while $\zeta\notin\mathbb R$, so $\zeta\notin\mathbb Q(\alpha)$. Since $\zeta$ satisfies
\[
t^2+t+1=0,
\]
we have
\[
[K:\mathbb Q(\alpha)]=2.
\]
Hence
\[
[K:\mathbb Q]=2\cdot3=6.
\]
:::

<1>2. Define automorphisms $\sigma,\tau\in\operatorname{Gal}(K/\mathbb Q)$ by
\[
\sigma(\alpha)=\zeta\alpha,
\qquad
\sigma(\zeta)=\zeta,
\]
and
\[
\tau(\alpha)=\alpha,
\qquad
\tau(\zeta)=\zeta^2.
\]
Then
\[
\sigma^3=\tau^2=1,
\qquad
\tau\sigma\tau=\sigma^{-1}.
\]
::: {.proof}
The map $\sigma$ cyclically permutes the three roots of $x^3-5$ and fixes $\zeta$, so it extends to a $\mathbb Q$-automorphism of $K$ of order $3$.
The map $\tau$ is complex conjugation on $K$, hence has order $2$.
Finally,
\[
(\tau\sigma\tau)(\alpha)
=\tau\sigma(\alpha)
=\tau(\zeta\alpha)
=\zeta^2\alpha
=\sigma^{-1}(\alpha),
\]
and both sides send $\zeta$ to $\zeta$, so $\tau\sigma\tau=\sigma^{-1}$.
:::

<1>3. The Galois group is
\[
G=\operatorname{Gal}(K/\mathbb Q)
=\langle\sigma,\tau\mid \sigma^3=\tau^2=1,\ \tau\sigma\tau=\sigma^{-1}\rangle
\cong S_3.
\]
::: {.proof}
The six elements
\[
1,\sigma,\sigma^2,\tau,\sigma\tau,\sigma^2\tau
\]
are distinct, so the subgroup generated by $\sigma$ and $\tau$ has order $6$.
Since $[K:\mathbb Q]=6$, the full Galois group also has order $6$, so these automorphisms exhaust it.
The displayed presentation is the standard presentation of $S_3$.
:::

<1>4. The subgroup $\langle\sigma\rangle$ of order $3$ fixes exactly
\[
\mathbb Q(\zeta).
\]
::: {.proof}
The automorphism $\sigma$ fixes $\zeta$, so
\[
\mathbb Q(\zeta)\subseteq K^{\langle\sigma\rangle}.
\]
By the fundamental theorem of Galois theory,
\[
[K^{\langle\sigma\rangle}:\mathbb Q]
=\frac{|G|}{|\langle\sigma\rangle|}
=2.
\]
Also $[\mathbb Q(\zeta):\mathbb Q]=2$, so equality holds.
:::

<1>5. The three subgroups of order $2$ have fixed fields
\[
K^{\langle\tau\rangle}=\mathbb Q(\alpha),
\]
\[
K^{\langle\sigma\tau\rangle}=\mathbb Q(\zeta^2\alpha),
\]
and
\[
K^{\langle\sigma^2\tau\rangle}=\mathbb Q(\zeta\alpha).
\]
::: {.proof}
The first equality is immediate because $\tau$ fixes $\alpha$, and the fixed field of an order-$2$ subgroup has degree $3$ over $\mathbb Q$.

For $\sigma\tau$, using composition from right to left,
\[
(\sigma\tau)(\zeta^2\alpha)
=\sigma(\zeta\alpha)
=\zeta^2\alpha,
\]
so $\mathbb Q(\zeta^2\alpha)$ lies in the fixed field. Since $\zeta^2\alpha$ is a root of the irreducible polynomial $x^3-5$, this field has degree $3$, hence is the whole fixed field.

Similarly,
\[
(\sigma^2\tau)(\zeta\alpha)
=\sigma^2(\zeta^2\alpha)
=\zeta\alpha,
\]
so $\mathbb Q(\zeta\alpha)$ is the fixed field of $\langle\sigma^2\tau\rangle$.
:::

<1>6. The complete subgroup--field correspondence is
\[
\begin{array}{c|c}
H\le G & K^H\\ \hline
G & \mathbb Q\\
\langle\sigma\rangle & \mathbb Q(\zeta)\\
\langle\tau\rangle & \mathbb Q(\alpha)\\
\langle\sigma\tau\rangle & \mathbb Q(\zeta^2\alpha)\\
\langle\sigma^2\tau\rangle & \mathbb Q(\zeta\alpha)\\
\{1\} & K
\end{array}.
\]
::: {.proof}
These are all subgroups of $S_3$: the whole group, its unique subgroup of order $3$, its three subgroups of order $2$, and the trivial subgroup. By <1>4 and <1>5 their fixed fields are exactly those displayed, and the fundamental theorem of Galois theory reverses inclusions and gives all intermediate fields.
:::
:::
