---
schema: qual/card@1
id: P-65GIR
kind: problem
title: Ascending chains of ideals in a PID stabilize
classification:
  areas:
  - algebra
  topics:
  - Principal Ideal Domains
  - Noetherian Rings
  - Ideals
relations: []
review: draft
---

::: problem
Let $R$ be a principal ideal domain (PID), and let $(a_1) \subseteq (a_2) \subseteq (a_3) \subseteq \cdots$ be an ascending chain of ideals in $R$.

Prove that there exists $n \in \mathbb{N}$ such that $(a_j) = (a_n)$ for all $j \ge n$ (that is, $R$ satisfies the ascending chain condition on ideals).
:::

::: solution
**Goal:** Prove that every PID is Noetherian by considering the union ideal $I = \bigcup_{k=1}^\infty (a_k)$ and applying the principal ideal property.

<1>1. The union $I = \bigcup_{k=1}^\infty (a_k)$ is an ideal of $R$:
    *Proof:*
    <2>1. Non-empty: $0 \in (a_1) \subseteq I$.
    <2>2. Closure under subtraction:
        - Let $x, y \in I$.
        - There exist indices $i, j \in \mathbb{N}$ such that $x \in (a_i)$ and $y \in (a_j)$.
        - Let $k = \max(i, j)$. Since the chain is ascending, $(a_i) \subseteq (a_k)$ and $(a_j) \subseteq (a_k)$, so $x, y \in (a_k)$.
        - Since $(a_k)$ is an ideal, $x - y \in (a_k) \subseteq I$.
    <2>3. Closure under multiplication by elements of $R$:
        - Let $r \in R$ and $x \in I$.
        - There exists $i \in \mathbb{N}$ such that $x \in (a_i)$.
        - Since $(a_i)$ is an ideal, $r x \in (a_i) \subseteq I$.
    <2>4. Thus $I$ is an ideal of $R$.

<1>2. Existence of a finite stabilization index $n$:
    *Proof:*
    <2>1. Since $R$ is a PID, every ideal of $R$ is principal.
    <2>2. Thus there exists an element $b \in R$ such that $I = (b)$.
    <2>3. Since $b \in (b) = I = \bigcup_{k=1}^\infty (a_k)$, there exists an index $n \in \mathbb{N}$ such that $b \in (a_n)$.
    <2>4. Since $b \in (a_n)$, the principal ideal generated by $b$ satisfies $(b) \subseteq (a_n)$.

<1>3. Chain stabilization for all $j \ge n$:
    *Proof:*
    <2>1. For any integer $j \ge n$, the chain condition gives $(a_n) \subseteq (a_j)$.
    <2>2. By definition of $I$, $(a_j) \subseteq I = (b)$.
    <2>3. Combining the inclusions:
    $$(b) \subseteq (a_n) \subseteq (a_j) \subseteq I = (b).$$
    <2>4. Therefore $(a_j) = (a_n) = (b)$ for all $j \ge n$.

<1>4. Conclusion:
    *Proof:*
    The ascending chain $(a_1) \subseteq (a_2) \subseteq \cdots$ stabilizes at index $n$.
:::
