---
schema: qual/card@1
id: E-HK-104-17
kind: problem
title: Forms invariant under the real orthogonal group
classification:
  areas:
  - algebra
  topics:
  - Groups Preserving Bilinear Forms
  - Bilinear Forms
  - Linear Algebra
relations: []
review: draft
audit:
- event: solution-written
  by: gemini-3.7-flash
  date: 2026-08-30
- event: solution-reviewed
  by: chatgpt
  date: 2026-09-11
  note: "Replaced the invalid real-Schur-lemma shortcut with a direct computation using coordinate sign changes and permutations."
---

::: {.exercise}
Find all bilinear forms on the space of $n \times 1$ matrices over $\RR$ which are invariant under $O(n, R)$.
:::

::: {.solution}
Write
$$
f(x,y)=x^tBy
$$
for a unique matrix $B\in M_n(\mathbb R)$.

<1>1. Orthogonal invariance is equivalent to commuting with every orthogonal matrix.
::: {.proof}
For $A\in O(n,\mathbb R)$,
$$
f(Ax,Ay)=x^tA^tBAy.
$$
Thus invariance for all $x,y$ is equivalent to
$$
A^tBA=B.
$$
Since $A^{-1}=A^t$, this is equivalent to
$$
BA=AB.
$$
:::

<1>2. Commuting with all coordinate sign changes forces $B$ to be diagonal.
::: {.proof}
For each $k$, let
$$
D_k=\operatorname{diag}(1,\ldots,1,-1,1,\ldots,1),
$$
with $-1$ in the $k$th position. Then $D_k\in O(n,\mathbb R)$.

If $B=(b_{ij})$ and $i\ne j$, use $D_iB=BD_i$. Its $(i,j)$ entry gives
$$
-b_{ij}=b_{ij},
$$
so $b_{ij}=0$. Hence $B$ is diagonal.
:::

<1>3. Commuting with coordinate permutations forces a diagonal $B$ to be scalar.
::: {.proof}
Write
$$
B=\operatorname{diag}(b_1,\ldots,b_n).
$$
Every permutation matrix is orthogonal. If $P_{ij}$ exchanges coordinates
$i$ and $j$, then
$$
BP_{ij}=P_{ij}B
$$
forces $b_i=b_j$. Hence all diagonal entries are equal and
$$
B=cI
$$
for some $c\in\mathbb R$.
:::

<1>4. Conclude the classification.
::: {.proof}
If $B=cI$, then for every $A\in O(n,\mathbb R)$,
$$
A^tBA=cA^tA=cI=B,
$$
so the form is invariant. Conversely, steps <1>1--<1>3 show that every
invariant form has this shape. Therefore the invariant bilinear forms are
exactly
$$
\boxed{f_c(x,y)=c\,x^ty\qquad(c\in\mathbb R).}
$$
:::
:::
