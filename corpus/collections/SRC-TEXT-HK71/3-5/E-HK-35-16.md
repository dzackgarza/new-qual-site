---
schema: qual/card@1
id: E-HK-35-16
kind: problem
title: Functionals with $f(AB) = f(BA)$ are scalar multiples of trace
classification:
  areas:
  - algebra
  topics:
  - Linear Functionals
  - Linear Algebra
relations: []
review: draft
audit:
- event: source-checked
  by: chatgpt
  date: 2026-09-10
  note: "Compared the statement with Hoffman--Kunze 3.5.16, including the normalization f(I)=n."
- event: solution-written
  by: chatgpt
  date: 2026-09-10
- event: solution-reviewed
  by: chatgpt
  date: 2026-09-10
  note: "Used matrix units to force all off-diagonal values to zero and all diagonal values to agree, then checked the normalization using characteristic zero."
---

::: {.exercise}
Show that the trace functional on $n \times n$ matrices is unique in the following sense.
If $W$ is the space of $n \times n$ matrices over the field $F$ and if $f$ is a linear functional on $W$ such that $f(AB) = f(BA)$ for each $A$ and $B$ in $W$, then $f$ is a scalar multiple of the trace function.
If, in addition, $f(I) = n$, then $f$ is the trace function.
:::

::: {.solution}
Let $E_{ij}$ denote the standard matrix unit. Put
$$
c=f(E_{11}).
$$
We prove that $f(A)=c\operatorname{tr}(A)$ for every $A\in W$.

<1>1. The functional vanishes on every off-diagonal matrix unit.
::: {.proof}
Fix $i\ne j$. Since
$$
E_{ii}E_{ij}=E_{ij},
\qquad
E_{ij}E_{ii}=0,
$$
the hypothesis $f(AB)=f(BA)$ gives
$$
f(E_{ij})=f(0)=0.
$$
Thus $f(E_{ij})=0$ whenever $i\ne j$.
:::

<1>2. The functional has the same value on every diagonal matrix unit.
::: {.proof}
For any $i,j$,
$$
E_{ij}E_{ji}=E_{ii},
\qquad
E_{ji}E_{ij}=E_{jj}.
$$
Applying the defining identity to $A=E_{ij}$ and $B=E_{ji}$ yields
$$
f(E_{ii})=f(E_{jj}).
$$
Hence every diagonal matrix unit has value $c$.
:::

<1>3. Therefore $f$ is a scalar multiple of trace.
::: {.proof}
Every matrix $A=(a_{ij})$ has the expansion
$$
A=\sum_{i,j}a_{ij}E_{ij}.
$$
By linearity and steps <1>1--<1>2,
$$
f(A)=\sum_{i,j}a_{ij}f(E_{ij})
=c\sum_i a_{ii}
=c\operatorname{tr}(A).
$$
This proves the asserted uniqueness up to scalar multiple [@HK71].
:::

<1>4. The normalization $f(I)=n$ forces $c=1$.
::: {.proof}
Since $I=\sum_{i=1}^nE_{ii}$,
$$
f(I)=nc.
$$
The hypothesis says $nc=n$. Hoffman--Kunze works over a subfield of
$\mathbb C$ unless otherwise specified, so the characteristic is zero and
$n\ne0$ in $F$ [@HK71]. Hence $c=1$, and step <1>3 gives
$$
f=\operatorname{tr}.
$$
:::
:::
