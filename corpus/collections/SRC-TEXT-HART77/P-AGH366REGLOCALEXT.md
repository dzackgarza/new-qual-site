---
schema: qual/card@1
id: P-AGH366REGLOCALEXT
kind: problem
title: Projective dimension over a regular local ring detected by Ext into the ring
classification:
  areas:
  - algebraic-geometry
  topics:
  - Ext Groups
  - Regular Local Rings
  - Projective Dimension
relations: []
review: draft
---

::: {.problem}
Let $A$ be a regular local ring, and let $M$ be a finitely generated $A$-module.
In this case, strengthen the result (6.10A) as follows.

a. $M$ is projective if and only if $\Ext^i(M, A)=0$ for all $i>0$.

Hint: Use (6.11A) and descending induction on $i$ to show that $\Ext^i(M, N)=0$ for all $i>0$ and all finitely generated $A$-modules $N$.
Then show $M$ is a direct summand of a free $A$-module.

b. Use (a) to show that for any $n$, $\operatorname{pd} M \leq n$ if and only if $\Ext^i(M, A)=0$ for all $i>n$.
:::
