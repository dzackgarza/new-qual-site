---
schema: qual/card@1
id: P-8TF24
kind: problem
title: The open topologist's sine curve union a vertical segment is not path-connected
classification:
  areas:
  - topology
  topics:
  - Connectedness
  - Euclidean Spaces
  - Counterexamples
relations: []
review: draft
audit:
- event: solution-written
  by: gemini-3.7-flash
  date: 2026-08-29
- event: source-checked
  by: gpt-5.6-sol
  date: 2026-09-05
  note: Checked the statement against problem 1 of the official UGA Fall 2012 topology exam.
- event: solution-reviewed
  by: gpt-5.6-sol
  date: 2026-09-05
  note: >-
    Replaced the invalid claim that sin(1/x(t)) must oscillate for every
    x(t)->0 by an intermediate-value argument forcing the path through points
    where the sine coordinate is alternately 1 and -1.
---

::: problem
Let $X=\{(x,\sin(1/x))\mid x>0\}$ and let $Y=\{(0,y)\mid -1\leq y\leq 1\}$.
Show that $X\cup Y$ is not a path-connected subset of $\RR^2$.
:::

::: {.solution}
<1>1. Suppose for contradiction there is a path $\gamma: [0,1] \to X \cup Y$ with $\gamma(0) \in Y$ and $\gamma(1) \in X$.
::: {.proof}
to show $X \cup Y$ is not path-connected, it suffices to show no point of $Y$ can be joined to a point of $X$ by a path.
:::

<1>2. Write $\gamma(t) = (x(t), y(t))$; then $x(t)$ is continuous, $x(0) = 0$, and $x(1) > 0$.
::: {.proof}
the first coordinate is continuous.
:::

<1>3. Let
\[
t_0=\max\{t\in[0,1]:x(t)=0\}.
\]
Then $t_0<1$, and
\[
x(t)>0\qquad(t_0<t\le1).
\]
::: {.proof}
The set $x^{-1}(0)$ is nonempty because it contains $0$, and it is closed in the compact interval $[0,1]$ because $x$ is continuous.
Hence it has a maximum $t_0$.
Since $x(1)>0$, one has $t_0<1$.
Every point of $X\cup Y$ has nonnegative first coordinate, so if $t>t_0$ then $x(t)\ne0$ by maximality of $t_0$, and therefore $x(t)>0$.
:::

<1>4. For every $t\in(t_0,1]$,
\[
\gamma(t)\in X
\qquad\text{and hence}\qquad
y(t)=\sin\frac1{x(t)}.
\]
::: {.proof}
By <1>3, $x(t)>0$ for $t>t_0$.
The only points of $X\cup Y$ with positive first coordinate are points of $X$, where the second coordinate is $\sin(1/x)$.
:::

<1>5. There are sequences
\[
r_m,s_m\downarrow t_0
\]
such that
\[
y(r_m)=1,
\qquad
y(s_m)=-1.
\]
::: {.proof}
For every sufficiently large integer $m$, put
\[
u_m=t_0+\frac1m\le1.
\]
By <1>3, $x(u_m)>0$.

Choose positive numbers $a_m,b_m<x(u_m)$ with
\[
\sin\frac1{a_m}=1,
\qquad
\sin\frac1{b_m}=-1.
\]
Such choices can be made arbitrarily small by taking reciprocals of sufficiently large numbers of the forms
\[
\frac\pi2+2\pi k,
\qquad
\frac{3\pi}2+2\pi k.
\]

Since
\[
x(t_0)=0<a_m,b_m<x(u_m),
\]
the intermediate value theorem applied to $x$ on $[t_0,u_m]$ gives
\[
r_m,s_m\in(t_0,u_m)
\]
with
\[
x(r_m)=a_m,
\qquad
x(s_m)=b_m.
\]
By <1>4,
\[
y(r_m)=\sin\frac1{a_m}=1,
\qquad
y(s_m)=\sin\frac1{b_m}=-1.
\]
Finally,
\[
t_0<r_m,s_m<t_0+\frac1m,
\]
so both sequences converge to $t_0$.
:::

<1>6. The sequences in <1>5 contradict continuity of $y$ at $t_0$.
::: {.proof}
Continuity would give
\[
y(r_m)\longrightarrow y(t_0)
\qquad\text{and}\qquad
y(s_m)\longrightarrow y(t_0).
\]
But the first sequence is constantly $1$ and the second is constantly $-1$.
They cannot have the same limit.
:::

<1>7. Therefore no such path exists, so $X \cup Y$ is not path-connected.
::: {.proof}
<1>1–<1>6.
:::
:::
