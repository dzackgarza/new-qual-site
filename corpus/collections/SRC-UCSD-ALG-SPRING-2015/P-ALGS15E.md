---
schema: qual/card@1
id: P-ALGS15E
kind: problem
title: Extension and contraction of ideals under localization
classification:
  areas:
  - algebra
  topics:
  - Commutative Algebra
  - Localization
  - Ideals
relations: []
review: draft
audit:
- event: source-checked
  by: OpenAI
  date: 2026-09-08
- event: solution-written
  by: OpenAI
  date: 2026-09-08
- event: solution-reviewed
  by: OpenAI
  date: 2026-09-08
---

::: problem
Let $R$ be a commutative ring, and let $S$ be a multiplicative system in $R$.
Let $S^{-1}R$ be the localization of $R$ at $S$ and let $\phi \colon R \to S^{-1}R$ be the canonical homomorphism sending $r \mapsto \frac{r}{1}$.
For an ideal $I$ of $R$ we write $I^e$ for the ideal of $S^{-1}R$ generated by $\phi(I)$, and for an ideal $J$ of $S^{-1}R$ we write $J^c$ for the ideal of $R$ given by $\phi^{-1}(J)$.

(a) Prove that for any ideal $J$ of $S^{-1}R$, one has $(J^c)^e = J$, while for any ideal $I$ of $R$, one has $(I^e)^c = \{x \in R \mid sx \in I \text{ for some } s \in S\}$.

(b) Show that if $R$ is noetherian, then $S^{-1}R$ is noetherian.
Give an example which shows that the converse need not hold.
:::

::: {.solution}
**Part (a).**

<1>1. For every ideal $J\subseteq S^{-1}R$, one has $(J^c)^e=J$.
::: {.proof}
By definition, $J^c=\{r\in R:r/1\in J\}$.
Since $J$ is an ideal, every generator $r/1$ with $r\in J^c$ lies in $J$, so $(J^c)^e\subseteq J$.

Conversely, let $a/s\in J$.
Since $s/1$ is a unit in $S^{-1}R$, multiplication by $s/1$ gives
\[
\frac a1=\frac s1\frac as\in J.
\]
Thus $a\in J^c$, so $a/1\in (J^c)^e$.
Multiplying by the unit $1/s$ shows $a/s\in (J^c)^e$.
Hence $J\subseteq(J^c)^e$.
:::

<1>2. For every ideal $I\subseteq R$,
\[
(I^e)^c=\{x\in R: sx\in I\text{ for some }s\in S\}.
\]
::: {.proof}
An element $x\in R$ lies in $(I^e)^c$ exactly when $x/1\in I^e=S^{-1}I$.
This means that in $S^{-1}R$ one has
\[
\frac x1=\frac a s
\]
for some $a\in I$ and $s\in S$.
Equality in the localization means that there exists $t\in S$ such that
\[
t(sx-a)=0.
\]
Therefore $(ts)x=ta\in I$, with $ts\in S$.

Conversely, if $sx\in I$ for some $s\in S$, then
\[
\frac x1=\frac{sx}{s}\in S^{-1}I=I^e,
\]
so $x\in(I^e)^c$.
:::

**Part (b).**

<1>3. If $R$ is Noetherian, then $S^{-1}R$ is Noetherian.
::: {.proof}
Let $J\subseteq S^{-1}R$ be an ideal.
By <1>1,
\[
J=(J^c)^e.
\]
Since $R$ is Noetherian, $J^c$ is finitely generated, say $J^c=(a_1,\dots,a_n)$.
Extension then gives
\[
J=(a_1/1,\dots,a_n/1),
\]
so every ideal of $S^{-1}R$ is finitely generated.
Hence $S^{-1}R$ is Noetherian.
:::

<1>4. The converse need not hold.
::: {.proof}
Let
\[
R=k[x_1,x_2,x_3,\dots]
\]
be the polynomial ring in countably many indeterminates over a field $k$, and let $S=R\setminus\{0\}$.
The ring $R$ is not Noetherian because
\[
(x_1)\subsetneq(x_1,x_2)\subsetneq(x_1,x_2,x_3)\subsetneq\cdots
\]
is a strictly ascending chain of ideals.
Since $R$ is a domain,
\[
S^{-1}R=\operatorname{Frac}(R),
\]
which is a field and therefore Noetherian.
Thus Noetherianity of $S^{-1}R$ does not imply Noetherianity of $R$.
:::
:::
