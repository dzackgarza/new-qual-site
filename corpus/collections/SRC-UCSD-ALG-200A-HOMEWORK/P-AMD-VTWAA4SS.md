---
schema: qual/card@1
id: P-AMD-VTWAA4SS
kind: problem
title: $\mathrm{Aut}(C_n)\cong(\ZZ/n\ZZ)^\times$ via $x\mapsto x^i$
classification:
  areas:
  - algebra
  topics:
  - Automorphisms
  - Cyclic Groups
  - Number Theory
relations: []
review: draft
audit:
- event: source-checked
  by: gpt-5.6-sol
  date: 2026-09-06
  note: >-
    Checked against UCSD Math 200A Fall 2016 Homework 2, Exercise 6(i)--(iv).
    This card contains the power-map endomorphism and automorphism criterion,
    equality modulo n, exhaustion of all automorphisms, composition law, and
    the resulting isomorphism from (Z/nZ)^x to Aut(G).
- event: solution-written
  by: gpt-5.6-sol
  date: 2026-09-06
- event: solution-reviewed
  by: gpt-5.6-sol
  date: 2026-09-06
  note: >-
    Fixed a generator g of the cyclic group. The image of sigma_a is generated
    by g^a, whose order is n/gcd(a,n), giving the automorphism criterion. The
    remaining claims follow by evaluating homomorphisms on g and checking the
    composition law.
---

::: {.problem}
Let $G$ be a cyclic group of order $n$.
For each $a\in\mathbb Z$, define
\[
\sigma_a:G\longrightarrow G,
\qquad
\sigma_a(x)=x^a.
\]

Prove that:

1. each $\sigma_a$ is an endomorphism of $G$, and $\sigma_a$ is an automorphism if and only if $\gcd(a,n)=1$;

2. $\sigma_a=\sigma_b$ if and only if $a\equiv b\pmod n$;

3. every automorphism of $G$ is $\sigma_a$ for some $a\in\mathbb Z$;

4. $\sigma_a\circ\sigma_b=\sigma_{ab}$.

Deduce that
\[
\psi:(\mathbb Z/n\mathbb Z)^\times\longrightarrow\operatorname{Aut}(G),
\qquad
[a]\longmapsto\sigma_a,
\]
is an isomorphism of groups.
:::

::: {.solution}
Fix a generator $g$ of $G$.

<1>1. For every $a\in\mathbb Z$, the map $\sigma_a$ is an endomorphism of $G$.
::: {.proof}
Since $G$ is cyclic, it is abelian.
Hence for all $x,y\in G$ and every integer $a$,
\[
\sigma_a(xy)=(xy)^a=x^ay^a=\sigma_a(x)\sigma_a(y).
\]
Thus $\sigma_a$ is a group homomorphism $G\to G$.
:::

<1>2. The element $g^a$ has order
\[
\frac{n}{\gcd(a,n)}.
\]
::: {.proof}
Let $d=\gcd(a,n)$, and write
\[
a=da',
\qquad
n=dn',
\qquad
\gcd(a',n')=1.
\]
Then
\[
(g^a)^m=e
\iff g^{am}=e
\iff n\mid am
\iff dn'\mid da'm
\iff n'\mid a'm.
\]
Since $\gcd(a',n')=1$, the last condition is equivalent to $n'\mid m$.
Therefore the least positive such $m$ is $n'=n/d$.
:::

<1>3. The endomorphism $\sigma_a$ is an automorphism if and only if $\gcd(a,n)=1$.
::: {.proof}
The image of $\sigma_a$ is generated by the image of the generator $g$:
\[
\operatorname{im}(\sigma_a)=\langle g^a\rangle.
\]
By <1>2,
\[
|\operatorname{im}(\sigma_a)|=\frac{n}{\gcd(a,n)}.
\]
Thus $\sigma_a$ is surjective exactly when $\gcd(a,n)=1$.
Since $G$ is finite, surjectivity is equivalent to bijectivity, so this is exactly the condition for $\sigma_a$ to be an automorphism.
:::

<1>4. We have $\sigma_a=\sigma_b$ if and only if $a\equiv b\pmod n$.
::: {.proof}
Two homomorphisms from the cyclic group $G=\langle g\rangle$ are equal if and only if they agree on $g$.
Hence
\[
\begin{aligned}
\sigma_a=\sigma_b
&\iff g^a=g^b\\
&\iff g^{a-b}=e\\
&\iff n\mid(a-b)\\
&\iff a\equiv b\pmod n.
\end{aligned}
\]
:::

<1>5. Every automorphism of $G$ is $\sigma_a$ for some $a\in\mathbb Z$.
::: {.proof}
Let $\tau\in\operatorname{Aut}(G)$.
Since every element of $G$ is a power of $g$, there is some $a\in\mathbb Z$ such that
\[
\tau(g)=g^a.
\]
For every $r\in\mathbb Z$,
\[
\tau(g^r)=\tau(g)^r=(g^a)^r=g^{ar}=\sigma_a(g^r).
\]
Thus $\tau=\sigma_a$.
Moreover, because $\tau(g)$ is again a generator, <1>2 implies $\gcd(a,n)=1$.
:::

<1>6. For all $a,b\in\mathbb Z$,
\[
\sigma_a\circ\sigma_b=\sigma_{ab}.
\]
::: {.proof}
For every $x\in G$,
\[
(\sigma_a\circ\sigma_b)(x)
=\sigma_a(x^b)
=(x^b)^a
=x^{ab}
=\sigma_{ab}(x).
\]
Hence the two maps are equal.
:::

<1>7. The map
\[
\psi:(\mathbb Z/n\mathbb Z)^\times\longrightarrow\operatorname{Aut}(G),
\qquad
[a]\longmapsto\sigma_a,
\]
is a well-defined group isomorphism.
::: {.proof}
If $[a]\in(\mathbb Z/n\mathbb Z)^\times$, then $\gcd(a,n)=1$, so <1>3 shows $\sigma_a\in\operatorname{Aut}(G)$.
If $[a]=[b]$, then $a\equiv b\pmod n$, so <1>4 gives $\sigma_a=\sigma_b$.
Thus $\psi$ is well-defined.

By <1>6,
\[
\psi([a][b])=\sigma_{ab}=\sigma_a\circ\sigma_b
=\psi([a])\psi([b]),
\]
so $\psi$ is a homomorphism.
By <1>4 it is injective, and by <1>5 it is surjective.
Therefore $\psi$ is an isomorphism.
:::
:::
