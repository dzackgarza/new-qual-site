---
title: "Orals::Toric Varieties"
---

- What is the **character lattice** $ M $? What is its **dual lattice** $ N $?

    $ M = $ **characters**, $ N = $**cocharacters/1PSs**.

    Set $ T^n \coloneqq{\mathbf{G}}_m^n $, then $ M \coloneqq\mathop{\mathrm{Hom}}(T^n , {\mathbf{G}}_m) $ is the character lattice. Contains elements $ \chi_m $ where $ \mathbf{t} \mapsto \prod t_i^{m_i} $. Then $ N \coloneqq\mathop{\mathrm{Hom}}({\mathbf{G}}_m, T^n) $ is the lattice of cocharacters/1-parameter subgroups, contains elements $ \lambda^u $ where $ t\mapsto \mathbf{t}^u \coloneqq(t^{u_i}) $. Use the perfect pairing $ {\left\langle {m},~{n} \right\rangle} \coloneqq\ell $ where $ \ell $ is the unique integer such that $ \chi_m \circ \lambda^n: {\mathbf{G}}_m\to {\mathbf{G}}_m $ is $ t\mapsto t^\ell $. Note $ N\otimes_{\mathbf{Z}}{\mathbf{G}}_m \cong T $.

- What is a **strongly convex rational polyhedral cone**?

    **Cone**: $ {\mathbf{R}}_{\geq 0} \sigma \subseteq \sigma $.

    **Strongly convex**: $ \sigma \cap(-\sigma) = 0 $, or contains no nonzero subspace of $ N_{\mathbf{R}} $, or $ \dim \sigma {}^{ \vee }= \dim M $.

    **Rational**: generated by lattice elements $ \left\{{\mathbf{v}_i}\right\} \subseteq N $.

    **Polyhedral**: finitely generated.

- What is the **dual of a cone**?

    $ \sigma {}^{ \vee }\coloneqq\left\{{m\in M {~\mathrel{\Big\vert}~}{\left\langle {n},~{m} \right\rangle} \geq 0 \,\, \forall n\in \sigma}\right\} $.

- Compute the toric variety associated to $ { \mathrm{Cone} }(v_1, v_2) $ where $ v_1 = (1, 4) $ and $ v_2 = (1, 4) $.

    Can get perps by $ \pi $ rotation

    $$e_1 \mapsto e_2, e_2 \mapsto-e_1 \rightsquigarrow\left[\begin{array}{cc} 0 & -1 \\1 & 0\end{array}\right]$$

    So $ v_1^\perp = (-1, 2), v_2^\perp = (-4, 1) $. Flip $ v_2^\perp $ since normal was oriented outside of cone to get $ (4, -1) $, take cone spanned:

    Find basis $ (4, -1), (1,0), (0, 1), (-1, 2) $ corresponding to $ u,v,w,t \coloneqq x^4 y^{-1}, x, y, x^{-1}y^2 $.

    Know this should be 1-dimensional, so find 3 relations to get

    $$X_\sigma = \operatorname{Spec}k[x^4 y^{-1}, x, y, x^{-1}y^2] = \operatorname{Spec}{ k[u,v,w,t] \over (uv-w^2, wt-v^4, ut-v^3w) }$$

- Discuss how to construct an affine toric variety from a fan.

    $ \sigma \leadsto S_\sigma \coloneqq\sigma {}^{ \vee }\cap M \leadsto X_\sigma \coloneqq\operatorname{Spec}{\mathbf{C}}[S_\sigma] $ where $ {\mathbf{C}}[S_\sigma] \coloneqq\left\{{ \sum_{s\in S_{\sigma}} c_s \chi^s {~\mathrel{\Big\vert}~}c_s\in {\mathbf{C}}\text{ and }c_s = 0 { \text{a.e.} }}\right\} $ with character multiplication.

- What is a **fan**?

    A collection of SCRPCs closed under taking faces and the intersection of any two cones is a face, where $ \tau \leq \sigma $ is a face iff $ \tau = H_m \cap\sigma $ for some hyperplane corresponding to $ m\in \sigma {}^{ \vee }\subseteq M_{\mathbf{R}} $ and $ H_m \coloneqq\left\{{n\in N_{\mathbf{R}}{~\mathrel{\Big\vert}~}{\left\langle {m},~{u} \right\rangle} = 0}\right\} \subseteq N_{\mathbf{R}} $.

- What is a **convex set**?

    For all $ p,q\in S $, the line $ tp + (1-t)q\in S $. In dimension 2, equivalently for any $ \left\{{p_i}\right\} \subset S $ and any scalars $ \left\{{\lambda_i}\right\} $ with $ \sum \lambda_i = 1 $, $ p \coloneqq\sum \lambda_i p_i\in S $.

- What is the toric variety associated to a polytope?

    For $ P\subseteq M $, take the cone over $ P\times\left\{{1}\right\} \subseteq M_{\mathbf{R}}\times {\mathbf{R}} $ to get a fan whose cones are cones over proper faces of $ P $\

    Note $ F\leq P $ is proper iff $ F = P \cap(H_n + r)) $ for some $ r $, i.e. equal to intersection with some affine hyperplane so $ F = \left\{{p\in P {~\mathrel{\Big\vert}~}{\left\langle {n},~{p} \right\rangle} = r}\right\} $.)

    Alternatively: take the normal fan of $ P $ in $ N $ to define $ X_P $

    Alternatively: $ X_P \coloneqq\bigcup_{m\in M\cap P} \operatorname{Spec}{\mathbf{C}}[\sigma_{\widehat{m}} \cap M] $ where $ \sigma_{\widehat{m}}\coloneqq{ \mathrm{Cone} }(P\cap M-m) $.

    How these are related: normal fan for $ P \cong $ proper faces over $ P^\circ $.

- What is the polytope $ Q_D $ associated to a Weil divisor $ D $ on $ X_P $?

    Write $ D = \sum a_\rho D_\rho $, then

    $$Q_D = \left\{{m\in M_{\mathbf{R}}{~\mathrel{\Big\vert}~}{\left\langle {m},~{\rho } \right\rangle}\geq -a_\rho \, \forall \rho\in \Sigma(1)}\right\}.$$

    For the anticanonical one gets $ Q_{-K_{X_p}} = P $.

- Discuss the **moment map** for $ X_\Sigma $.

    $ \mu_{\Sigma}: {\mathbf{C}}^{{\sharp}\Sigma(1)} \to {\mathbf{R}}^{r-n} $ for $ n = \dim N_{\mathbf{R}} $.

- Discuss the **orbit-cone correspondence**.

    Each cone $ \sigma $ determines a distinguished point $ x_\sigma \in U_\sigma $ corresponding to the map $ S_\sigma \coloneqq\sigma {}^{ \vee }\cap M \to \left\{{0, 1}\right\} $ where $ m\mapsto 1 $ if $ m\in \sigma^\perp $ and 0 otherwise where $ \sigma^\perp \coloneqq\left\{{m\in M_{\mathbf{R}}{~\mathrel{\Big\vert}~}{\left\langle {n},~{m} \right\rangle} = 0\,\, \forall n\in \sigma}\right\} $. Then there is a bijection from $ \Sigma $ to $ T{\hbox{-}} $orbits in $ X_\Sigma $ where $ \sigma \mapsto T\cdot x_\sigma $ and an $ r{\hbox{-}} $dimensional cone corresponds to an $ n-r{\hbox{-}} $dimensional orbit, e.g. $ \dim \sigma =0 \rightleftharpoons{\mathbf{G}}_m^n $ and $ \dim \sigma = n \rightleftharpoons T{\hbox{-}} $invariant points, and the face poset corresponds to the orbit closure poset (contravariantly).

- Discuss as many examples of fans and their corresponding toric varieties as you can.

    $ ({\mathbf{C}}^{\times})^n, {\mathbf{C}}^n, {\mathbf{P}}^n, { \mathbf{F} }_n $ for any $ n $

    The rational normal cone $ \widehat{C}_d $ gotten from $ (d,-1), (0,1) $.

    The cone over a cone $ V(y^2-xz) $, gotten from $ (0, 1), (2, -1) $:

    $ \operatorname{Bl}_m X_\Sigma $ for any $ \Sigma $.

    $ V(xy-zw) $ gotten from $ (1,0,0), (0, 1,0), (1,0,1), (0,1,1) $.

    Any products of the above.

- Show that a divisor class group may not be torsionfree.

    $  \operatorname{Cl}(\widehat{C}_d) = {\mathbf{Z}}/d{\mathbf{Z}} $, see 4.14 Cox.

- What is a toric morphism?

    On lattices: images of cones are contained in cones. A map $ \phi: N_1\to N_1 $ where for each $ \sigma_1 \subseteq N_{1, {\mathbf{R}}} $ there is a $ \sigma_2 \subseteq N_{2, {\mathbf{R}}} $ with $ \phi(\sigma_1) \subseteq \sigma_2 $. At the level of varieties: $ \phi: X_{\Sigma_1} \to X_{\Sigma_2} $ a morphism of varieties with $ \phi(T_{1})\subseteq T_2 $ which restricts to a group morphism. At the level of abstract toric varieties: for $ V_i = \operatorname{Spec}{\mathbf{C}}[S_i] $ and $ \phi: V_1\to V_2 $, toric iff $ \phi^*: {\mathbf{C}}[S_2] \to {\mathbf{C}}[S_1] $ is induced by a semigroup morphism $ (\phi^*)_*: S_1\to S_2 $.

- When is a toric variety smooth?

    Iff each affine patch is smooth, and $ U_\sigma $ is smooth iff admits generators which can be extended to a basis of $ N $. In this case $ U_\sigma \cong {\mathbf{A}}^k \times {\mathbf{G}}_m^{n-k} $ where $ k = \dim \sigma $.

- When is a toric variety proper/complete?

    When $ {\left\lvert {\Sigma} \right\rvert} = N_{\mathbf{R}} $, i.e. full support. Why: each $ n\in N_{\mathbf{R}} $ determines a one-parameter subgroup $ \lambda^n $, and if $ n=(n_1, n_2) $ then one looks at $ \lim_{t\to 0}\lambda^n(t) $ -- if $ n $ is in a cone, the limit exists, else not. E.g. for $ {\mathbf{P}}^2 $ the torus is $ (1,s,t) $ and $ \lambda^n(t) = (1, s^{n_1}, t^{n_2}) $ and one gets:  and one gets the distinguished limit points

- How is a line bundle on $ X $ described?

    Every $ {\mathcal{L}}\in \operatorname{Pic}(X_\Sigma) $ must be trivial when restricted to $ T^n $ so $ {\mathcal{L}}= {\mathcal{O}}_X(D) $ for some $ D\in \mathop{\mathrm{Cart}}(X_\Sigma) $.

- Give a toric example of a Weil divisor that is not Cartier.

    Take Cone $ \left(e_2, d e_1-e_2\right) $, then check $ M \rightarrow \operatorname{Div}_T\left(U_\sigma\right) $ by $ \left[\begin{array}{cc}d & -1 \\ 0 & 1\end{array}\right] $ so $ \mathrm{Cl}\left(U_\sigma\right)=\mathbf{Z} / d \mathbf{Z}( $ $ 0 = \operatorname{dim} \operatorname{div}\left(\chi^{e_1}\right)=d D_1 $ and $ \left.0 =\operatorname{dim} \operatorname{div}\left(\chi^{e_2}\right)=-D_1+D_2\right) $ but $ U_\sigma $ is affine so $ \operatorname{Pic}\left(U_\sigma\right)=0 $.

    Not commutativity of $ \operatorname{Div}\to  \operatorname{Cl} $ and $ \operatorname{CDiv}\to \operatorname{Pic} $ forces $ D \coloneqq D_1 $ to be Weil but not Cartier for any $ d\geq 1 $.

    In fact $ \operatorname{Pic}(X) = \left\langle{mD_2}\right\rangle_{\mathbf{Z}} $ and $ A_1(X) = \left\langle{D_2}\right\rangle_{\mathbf{Z}} $.

- What is $ \operatorname{rank}\operatorname{Pic}(X_\Sigma) $?

    If $ \Sigma $ is simplicial, always $ {\sharp}\Sigma(1)-\dim {\mathbb{N}}_{\mathbf{R}} $. E.g. $ \operatorname{Pic}({ \mathbf{F} }_n) = 4-2 = 2 $, generated by a special curve and a fiber.

- When is $ D $ **globally generated/bpf**?

    Polytope side: $ {\mathcal{O}}(D) $ is globally generated iff $ P_D $ is the convex hull of $ \left\{{u_\sigma {~\mathrel{\Big\vert}~}\sigma \in \Sigma(n)}\right\} $.

    $ {\left\lvert {D} \right\rvert} $ is bpf iff $ \psi_D $ is convex

    $ \psi_D $ is **convex** if $ \psi(tv + (1-t)w)\geq t\psi(v) + (1-t)\psi(w) $

- When is $ D $ **ample/bpf**?

    $ {\mathcal{O}}(D) $ is ample iff $ u_{\sigma}\neq u_{\sigma'} $ for distinct $ \sigma\neq \sigma'\in \Sigma(n) $ and $ P_D $ is $ n{\hbox{-}} $dimensional with vertices $ u_\sigma $.\

- When is $ D $ **very ample**?

    $ D $ is very ample iff $ \psi_D $ is strictly convex **and** $ S_\sigma $ is generated by $ \left\{{u - u(\sigma) {~\mathrel{\Big\vert}~}u\in P_D\cap M}\right\} $ where $ u(\sigma) $ is the linear function associated to $ \sigma $ defined by $ \psi_D({-}) = {\left\langle {u(\sigma)},~{{-}} \right\rangle} $ so $ D = \sum - \psi_D(\rho_i) D_{\rho_i} $ since $ \psi_D(\rho_i) = a_i $ in $ D = \sum a_i D_{\rho_i} $.

    $ \psi_D $ is **strictly convex** if convex and $ u(\sigma), u(\sigma') $ are not equal for distinct maximal cones $ \sigma,\sigma' $.

    If $ X $ is smooth complete, ample iff very ample.

    Otherwise check if $ P_D $ is an ample polytope: for every vertex $ v $, the semigroup $ S_{P, v} \coloneqq{\mathbb{N}}(P_v) $ where $ P_m \coloneqq P\cap M-v \coloneqq\left\{{p-v {~\mathrel{\Big\vert}~}p \in P \cap M}\right\} $ is **saturated** in $ M $.

- Discuss $ \operatorname{Pic}(X_\Sigma) $ and $ \operatorname{Cl}(X_\Sigma) $.

    The fundamental SESs:

- Discuss **global sections** of line bundles.

    $ H^0({\mathcal{O}}(D)) = \bigoplus_{m\in P_D \cap M} {\mathbf{C}}\cdot \chi^m $.

- Classify complete smooth **toric surfaces**.

    Given by sequences $ \left\{{v_0,\cdots, v_{d-1}, v_d = v_0}\right\} $ in counterclockwise order such that $ \left\{{v_i, v_{i+1\operatorname{mod}d}}\right\} $ generate $ N $.

    Yields $ a_i v_i = v_{i-1} + v_{i+1} $.

    For $ d=3 $, must be $ {\mathbf{P}}^2 $, and $ d=4 $ yields $ { \mathbf{F} }_n $. For $ d\geq 5 $, always a blowup of $ {\mathbf{P}}^2 $ or $ { \mathbf{F} }_n $ at $ T{\hbox{-}} $fixed points at $ \tilde v_i \coloneqq v_{i} + v_{i+1} $.

    The integers $ a_i $ must satisfy $ \prod\left[\begin{array}{cc}0 & -1 \\ 1 & a_i\end{array}\right]= \operatorname{id} $ and $ \sum a_i=3 d-12 $.

- What is a **Gorenstein Fano variety**?

    For $ X $ complete and normal: $ -K_X $ is Cartier and ample.

- Classify smooth **Fano** toric surfaces.

    Fano polytopes: $ 0\in \mathrm{int} P $ and the vertices of every facet form a lattice basis. $ P $ is reflexive, so $ P {}^{ \vee } $ is a lattice polytope and $ X_{P {}^{ \vee }} $ is smooth toric Fano whose normal fan comes from taking cones over proper faces of $ P $ -- every smooth toric surface arises this way. Toric *Gorenstein* Fanos: just 16 equivalence classes. If $ X_\Sigma $ is projective Gorenstein Fano, then $ P_{-K_{X_\Sigma}} $ is reflexive, and every $ X_P $ for $ P $ reflexive is Gorenstein Fano.

- Classify toric **del Pezzo** surfaces.

    Smooth projective Fanos of dimension 2, correspond to smooth reflexive polytopes.

    There are exactly 5: $ {\mathbf{P}}^1\times {\mathbf{P}}^1, \operatorname{Bl}_i {\mathbf{P}}^2 $ for $ i=0,1,2,3 $. Get the last three by blowing up (successively) the 3 torus-invariant points on $ {\mathbf{P}}^2 $.

- What is a **smooth polytope**?

    For $ \dim M = n $, each vertex meets exactly $ n $ edges and the primitive vectors along each edge generate a lattice basis.

- How does one **blowup** a toric variety?

    Subdivide any cone.

- How does one find the **minimal resolution** of a toric variety?

    Take the convex hull of $ \sigma \cap N $ and blowup along those lattice points:

- What are the **Weil divisors** of a toric variety?

    $ \operatorname{Div}_T(X_\Sigma) \cong {\mathbf{Z}}^{\Sigma(1)} $, i.e. each $ \rho\in \Sigma(1) $ corresponds to $ D_\rho $. A general Weil divisor is $ D = \sum_{\rho \in \Sigma(1)} a_\rho D_\rho $.

- What are the **Cartier divisors** on a toric variety?

    $ \operatorname{CDiv}_T(U_\sigma) \cong M/M(\sigma) $ where $ M(\sigma) \coloneqq\sigma {}^{ \vee }\cap M $, and $ \operatorname{CDiv}_T(X_\Sigma) = \cocolim_{\sigma \in \Sigma} M/M(\sigma) $. More concretely described by Cartier data $ \left\{{(U_\sigma, m_\sigma)}\right\} $ where $ { \left.{{D}} \right|_{{U_\sigma}} } = \operatorname{div}(\chi^{m_\sigma}) $ for $ m_\sigma\in M $ with $ {\left\langle {\rho },~{m_\sigma} \right\rangle} = -a_\rho $ for every $ \rho\in \Sigma(1) $, where $ D = \sum a_\rho D_\rho $.

    Can also obtain from polytopes: write $ P = \left\{{m\in M_{\mathbf{R}}{~\mathrel{\Big\vert}~}{\left\langle {\mu_F},~{m} \right\rangle} \geq -a_F \,\,\text{ for all facets } F\leq P}\right\} $ where $ \mu_F $ is the minimal generator along the normal to the facet $ F $. Then take $ D \coloneqq\sum a_F D_F $ where $ D_F $ is the divisor for the ray associated to $ F $ in $ X_P $.

- What is $ K_X $ for $ X $ toric?

    $ \omega_X \cong {\mathcal{O}}(-\sum_\rho D_\rho) $, so $ K_X \sim -\sum_\rho D_\rho $. As a consequence, for $ X \coloneqq{\mathbf{P}}^n $ we have $ \omega_X = {\mathcal{O}}(-n-1) $ since there are $ n+1 $ generators in the fan and all $ D_i\sim D_j $.

- When is a toric variety **Fano**?

    Need $ -K_X $ Cartier and ample, so $ {\mathcal{O}}(\sum_\rho D_\rho) $ ample.

- Find the **polytope of a divisor**.

    For $ {\mathbf{P}}^2 $, it's $ 3\Delta $.

- What is $ \pi_1 X $ of a toric variety?

    $ \pi_1 X_{\Sigma} \cong N/N' $ where $ N' \coloneqq\left\langle{\sigma \cap N {~\mathrel{\Big\vert}~}\sigma\in \Sigma}\right\rangle $.

- What is $ \chi(X) $ for a toric variety?

    $ \chi(X_\Sigma) = {\sharp}\Sigma(n) $.

- How are **intersection numbers** of divisors computed?

    For smooth complete toric surfaces: write $ \rho_{i-1} + \rho_{i+1} = b_i \rho_i $ as a linear dependence, then

    $$D_i \cdot D_j=\left\{\begin{array}{lll}0 & \text { if } & |i-j|>1 \\1 & \text { if } & |i-j|=1, \\-b_i & \text { if } & i=j\end{array}\right.$$

    Example:\

- What is the **quadric cone**?

    $ V(z^2-xy) $.

- What is a **reflexive polytope**?

    One with facet presentation

    $$P = \left\{{m\in M_{\mathbf{R}}{~\mathrel{\Big\vert}~}{\left\langle {m},~{\mu_F} \right\rangle} \geq  -1 \text{ for all facets } F}\right\}$$

    The origin is contained in its interior and every facet is at lattice distance 1 from the origin (guarantees that the polar dual is again a lattice polytope).

- What is the **polar dual** of a polytope?

    For reflexive polytopes, since $ a_F = 1 $ for all $ F $,

    $$P^\circ = \mathrm{Conv}(\mu_F \mathrel{\Big|}F \text{ is a facet of } P)$$

    $$P^\circ \coloneqq\left\{{n\in N {~\mathrel{\Big\vert}~}{\left\langle {n},~{p} \right\rangle} \geq -1\, \forall p\in P}\right\}$$

- What is $ P_{-K_X} $ for $ X = {\mathbf{P}}^2 $? What is its dual?

    *(Answer was a figure; image not recovered.)*

- How can you read the projective embedding off of a polytope?

    *(Answer was a figure; image not recovered.)*

- What is an **integral** support function?

    $ \phi: {\left\lvert {\Sigma} \right\rvert} \to {\mathbf{R}} $, linear on each cone, and integral if $ \phi({\left\lvert {\Sigma} \right\rvert} \cap N) \subseteq {\mathbf{Z}} $.

- What are the global sections of $ {\mathcal{O}}(D) $ for $ D\in \operatorname{Div}_T(X) $?

    $ \Gamma\left(X_{\Sigma}, \mathscr{O}_{X_{\Sigma}}(D)\right)=\bigoplus_{m \in P_D \cap M} \mathbb{C} \cdot \chi^m, $

- When is $ D $ **bpf**?

    Supposing the maximal dimensional cone is $ n $, $ D $ is bpf iff $ {\mathcal{O}}(D) $ is globally generated iff $ m_\sigma\in P_D $ for all $ \sigma\in \Sigma(n) $.

- What is the **support function** associated to $ D $?

    For $ D=\sum a_p D_p $, uniquely determined by: $ \phi_D $ is linear on each cone, $ \phi_D(u_p) = -a_p $ where $ u_p $ are primitive ray generators.

- What is a **convex** support function?

    $ \varphi(t u+(1-t) v) \geq t \varphi(u)+(1-t) \varphi(v) $.

- What is a **strictly convex** support function?

    For every $ \sigma\in \Sigma(n) $, $ {\left\langle {m_\sigma},~{u} \right\rangle} = \phi_D(u) \iff u\in \sigma $.

- When is $ D $ **ample**?

    Iff $ \phi_D $ is strictly convex.

- Show that $ {\mathbf{P}}^2 $ is Fano.

    $ \omega_X = {\mathcal{O}}(-\sum_\rho D_\rho) = {\mathcal{O}}(-D_1 - D_2\cdots - D_3) = {\mathcal{O}}(-3) $ so $ -\omega_X = {\mathcal{O}}(3) $ which is ample.

- Why are toric varieties **normal**?

    Need local rings to be integrally closed; the local ring at $ x_\sigma $ is $ R_\sigma = \cap_{?} R_{\tau_i} $, but each $ R_{\tau_i} $ is of the form $ {\mathbf{C}}[x_1,x_2^{\pm 1},\cdots, x_n^{\pm 1}] $ and intersections of integrally closed domains are again integrally closed..

- What is the polytope associated to a Cartier divisor?

    $ D = \sum a_i D_i\implies P_D = \left\{{u\in M_{\mathbf{R}}{~\mathrel{\Big\vert}~}{\left\langle {u},~{\rho_i} \right\rangle} \geq a_i}\right\} $.

- Classify smooth **affine** toric varieties.

    Always of the form $ X_\sigma = {\mathbf{A}}^k \times {\mathbf{G}}_m^{n-k} $.

- Classify **smooth complete toric surfaces**.

    On $ d=3 $ lattice points: $ {\mathbf{P}}^2 $.

    On $ d=4 $ lattice points: $ { \mathbf{F} }_n $ a Hirzebruch surface $ ({ \mathbf{F} }_0 \cong {\mathbf{P}}^1\times {\mathbf{P}}^1) $

    On $ d\geq 5 $ lattice points: $ \operatorname{Bl}_m X $ for $ X = {\mathbf{P}}^2, { \mathbf{F} }_n $ for some $ n, m $. In general one has

    $$\left(\begin{array}{c}v_i \\ v_{i+1}\end{array}\right)=\left(\begin{array}{cc}0 & 1 \\ -1 & a_i \end{array}\right)\left(\begin{array}{c} v_{i-1} \\ v_i\end{array}\right), \quad \prod_i\left[\begin{array}{cc}0 & 1 \\-1 & a_i\end{array}\right]=\mathrm{id}, \quad \sum_i a_i=3 d-12$$

    where $ d $ is the number of rays.

- How do blowups and contractions of fans change self-intersection numbers?

    *(Answer was a figure; image not recovered.)*

- How does one construct weighted projective space as a toric variety?

    $ {\mathbf{P}}(q_0,\cdots, q_n) \leadsto \Conv(0, k_1e_1,\cdots, k_ne_n) - (1,1,\cdots,1) $ where $ k_i \coloneqq\qty{q_i\over \sum q_i}^{-1}\in {\mathbf{Z}} $.
