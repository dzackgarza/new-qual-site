---
title: "Reading::Unsorted"
---

- What is EM duality in Maxwell theory?

    Setup: $ \nabla $ a connection on a principal $ {\operatorname{U}}_1{\hbox{-}} $bundle over $ X\in {\mathsf{Mfd}}^4 $.

    Equations of motion: $ d\star d\nabla = 0 $.

    Theory invariant under $ d\nabla \mapsto \star d\nabla $, exchanges $ E_i $ and $ B_i $.

- What is Yang-Mills theory?

    Nonabelian gauge theory.

    Equations of motion: $ d_A F = 0 $ and $ d_A \star F = 0 $ where $ F = dA + A{ {}^{ \scriptscriptstyle\wedge^{2} }  }\in \Omega^2( { \operatorname{ad}}E) $ is the curvature of $ E $ for $ E\to X^4\in \mathop{\mathrm{Prin}}_{G}{}_{/ {X}} $ and $ A $ a connection with covariant derivative $ d_A $.

- What is AB symmetry on a CY?

    A: path integral localized on holomorphic maps to $ M $, computes GW, depends only on symplectic structure

    B: path integral localized on constant maps, deformation theory of $ M $, depends only on complex structure

- What is a holomorphic Higgs bundle?

    Bundle $ E\to C $ with a holomorphic section of $  { \operatorname{ad}}(E) $, so $ { \overline{{\partial}}}\phi = 0 $.

- What is mirror symmetry for the Hitchin moduli space?

    SYZ: $ {\mathcal{M}}_H(G, C) \rightleftharpoons{\mathcal{M}}_H({}^L G, C) $ are SYZ mirrors

    HMS: A-branes on $ {\mathcal{M}}_H(G, C) $ is derived equivalent to $ \mathbf{D}{{\mathsf{Coh}}}({\mathcal{M}}_{\flat}({}^L G, C)) $.

    GL: $ \mathbf{D}{{\mathsf{Coh}}}({\mathcal{M}}_{\flat}({}^L G, C))  { \, \xrightarrow{\sim}\, }   {}_{\mathcal{D}}{\mathsf{Mod}} ({\mathsf{Bun}}_G(C)) $ sending points to Hecke eigensheaves.

- Motivate the affine Grassmannian.

    Hecke transformations modify a $ G{\hbox{-}} $bundle at a point, such modifications are parameterized by $ {\operatorname{Gr}}_G $, transformations from 't Hooft operators $ T_\mu $ correspond to Schubert cells $ \mu $.

- What is geometric Satake?

    Equivariant perverse sheaves on $ {\operatorname{Gr}}_G $ is equivalent to $ {\mathsf{Rep}}({}^L G)^{\mathrm{fd}} $.

- What is the strong force?

    Holds protons and neutrons together in the nucleus of an atom

    Holds quarks together in protons/neutrons

- What is the standard model?

    Yang Mills for the gauge group $ {\operatorname{SU}}_3 \times {\operatorname{SU}}_2 \times {\operatorname{U}}_1 $.

- What is the defining property of a $ D{\hbox{-}} $brane?

    Dirichlet boundary conditions: surfaces which can occur at the ends of open strings.

- What is quantization?

    Replacing Poisson brackets with commutators.

- What is the central charge?

    *(Answer was a figure; image not recovered.)*

- When is a toric variety $ X_\Sigma $ **normal**?

    $ \operatorname{Spec}{\mathbf{C}}[S] $ is normal for $ S\leq \Lambda $ a submonoid iff $ S $ is saturated in $ \Lambda $.

- When is $ X_P $ normal?

    Iff $ S_m\coloneqq\left\langle{m' - m {~\mathrel{\Big\vert}~}m' \in P \cap M}\right\rangle_{{\mathbb{N}}} $ is saturated in $ M $.

- When is a toric variety $ X_\Sigma $ **smooth**?

    $ \rho_i $ extend to a $ {\mathbf{Z}}{\hbox{-}} $basis of $ \Lambda $

- When is $ X_P $ smooth?

    $ X_{\Sigma} $ is smooth where $ \Sigma $ is the normal fan of $ P $.

    Equivalently the primitive edge vectors at each vertex form a full $ {\mathbf{Z}}{\hbox{-}} $basis of $ M $.

- When is $ P $ ample?

    $ S_m \coloneqq\left\langle{P\cap M - m}\right\rangle_{{\mathbb{N}}} $ is saturated in $ M $.

- What are $ H_{u, b} $ and $ H_{u,b}^+ $ for $ u\in N_{\mathbf{R}} $?

    $$H_{u, b}=\left\{m \in M_{\mathbb{R}}:\langle m, u\rangle=b\right\}, \quad H_{u, b}^{+}=\left\{m \in M_{\mathbb{R}}:\langle m, u\rangle \geq b\right\} \subset M_{\mathbf{R}}$$

- What is $ X_P $?

    For $ A \coloneqq P \cap M = \left\{{m_0,\cdots, m_s}\right\} $, get $ T\curvearrowright{\mathbf{P}}^n $ for $ T $ the character lattice of $ M $ by $ t.\mathbf{x} = {\left[ {\chi^{m_0}(t) \cdot x_0: \cdots : \chi^{m_s}(t) \cdot x_s} \right]} $ and define $ X_A \coloneqq{ \operatorname{cl}}_{{\mathbf{P}}^s}(T. {\left[ {1:\cdots:1} \right]}) $.

- What is $ u_F $?

    For $ P \subset M $, $ u_F\in N_{\mathbf{R}} $ is orthogonal to the face $ F $.

- What is the polar dual $ P^* $?

    $ P^*=\left\{u \in N_{\mathbb{R}}:\langle m, u\rangle \geq-1 \text { for any } m \in P\right\} \text {. } $

- What is an orthogonal space?

    $ X\in {\mathsf{Fun}}(L, \CGWH)^\text{cts} $ where $ L $ is the (topological) linear isometry category (objects = inner product spaces, homs are $ \mathop{\mathrm{Isom}}(V, W) $ with the Stiefel manifold topology)

- What is the Thom space of a bundle?

    Total space of disc bundle mod total space of sphere bundle.

- What is the orthogonal complement bundle?

    $ \xi(V, W) \subset W\times L(V\to W) $ given by $ \left\{{(v, \psi) {~\mathrel{\Big\vert}~}v\perp \psi(V)}\right\} $.

- What is $ O(V, W) $?

    The one-point compactification of $ \xi(V, W) $ the orthogonal complement bundle; equivalently its Thom space.

- What is the category $ O $?

    Inner product spaces, homs are $ O(V, W) $, composition is the smash product $ (w, \phi) \wedge(v, \psi) \mapsto (w + \phi(v), \phi\circ \psi) $.

- What is an orthogonal spectrum? A $ G{\hbox{-}} $spectrum?

    A based $ X\in {\mathsf{Fun}}^\text{cts}(O, {\mathsf{Top}}_*) $ into based spaces.

    For $ G{\hbox{-}} $spectra: $ X\in {\mathsf{Fun}}^\text{cts}(O, G{\hbox{-}}{\mathsf{Top}}_*) $ into based $ G{\hbox{-}} $spaces.

- What is $ S^V $ for $ V $ a vector space?

    $ V\cup\left\{{\infty}\right\} $ the one-point compactification.

- Discuss the actions and structure maps on $ {\mathsf{Sp}} $.

    $ O(V, V) = {\operatorname{O}}(V)_+ $ is an orthogonal group and one gets $ {\operatorname{O}}(V)_+\wedge X(V)\to X(V) $

    $ i_V: S^V \to O(V, V\oplus W) $ induces $ \sigma_{V, W}: S^V\wedge X(W) \to X(V\oplus W) $.

- How are $ G{\hbox{-}} $equivariant homotopy groups defined?

    $ \pi_0^G(X) \coloneqq\colim_{V\in s(U_G)}[S^V\to X(V)]^G_* $ which are $ G{\hbox{-}} $equivariant homotopy classes of based maps, and $ U_G $ a complete universe of $ G{\hbox{-}} $subrepresentations of $ U_G $.

    $ \pi_k^G(X) = \colim_{\cdots}[S^{V\oplus {\mathbf{R}}^k}\to X(V)]^G_* $ for $ k > 0 $ and $ \pi_k^G(X) = \colim_{\cdots}[S^{V}\to X(V\oplus {\mathbf{R}}^{-k})]^G_* $ for $ k < 0 $.

- What are $ \overline{{\mathbf{Z}}} $ and $ \overline{{\mathbf{Q}}} $?

    $ \exists p\in {\mathbf{Z}}[x] $ (resp $ {\mathbf{Q}}[x] $) such that $ p(\alpha) = 0 $.

- Give an example of a $ \alpha\not\in \overline{{\mathbf{Z}}} $.

    $ \alpha \coloneqq i/2 $, since its minimal polynomial is $ x^2 + {1\over 4}\not\in{\mathbf{Z}}[x] $.

- What is the cardinality of $ \overline{{\mathbf{Z}}} $?

    Countable

- Why is $ {\mathbf{Z}}[i] $ Euclidean?

    For $ a,b\in {\mathbf{Z}}[i] $, take $ a/b\in {\mathbf{C}} $ and round to the nearest lattice point.

    Distance is $ {r\over b} \coloneqq d \leq {1\over \sqrt 2}< 1 $, so yields $ {a\over b} = q + {r\over b}\implies a=qb+r $ where $ {\left\lvert {r} \right\rvert} < {\left\lvert {b} \right\rvert} $.

- Describe the Euclidean algorithm.

    $ r_0 = a $

    $ r_1 = b $

    $ r_2 $ is the remainder of $ r_0/r_1 $

    $ r_3 $ is the remainder of $ r_1/r_2 $

    Then $ (a,b) =(r_0, r_1) = (r_1, r_2) = \cdots = (r_{k-1}, 0) = r_{k-1} $ since $ r_0 - r_2 = \ell r_1 $.

- What are the units in $ {\mathbf{Z}}[i] $?

    $ \left\{{\pm 1,\pm i}\right\} = \left\{{x\in {\mathbf{Z}}[i] {~\mathrel{\Big\vert}~}N(x) = 1}\right\} $.

- What are the primes in $ {\mathbf{Z}}[i] $?

    $ 1+i $

    $ a+bi $ where $ N(a+bi) = p \equiv 1\operatorname{mod}4 $.

    $ p $ where $ p \equiv 3 \operatorname{mod}4 $.

    Follows because $ N(\alpha) = p $ prime forces $ \alpha $ to be prime.

- Which primes are sums of two squares?

    $ p\equiv 1 \operatorname{mod}4 $ and $ p=2 $.

- Give a congruence for $ (p-1)! $.

    $ (p-1)! \equiv -1 \operatorname{mod}p \iff p $ is prime.

- Discuss properties of $ {\mathbf{Z}}[i] $.

    PID

    Euclidean domain

- What is $ \zeta_3 $ in rectangular coordinates?

    $ \zeta_3 = {1\over 2}(-1 + \sqrt{3} i) $.

- What is $ \operatorname{ff}{\mathbf{Z}}[\zeta_3] $?

    $ {\mathbf{Q}}(\zeta_3) = {\mathbf{Q}}(\sqrt{-3}) $.

- What are the units in $ {\mathbf{Z}}[\zeta_3] $?

    $ \left\{{\pm 1, \pm \zeta_3, \pm\zeta_3^2}\right\} $.

- What are the primes in $ {\mathbf{Z}}[\zeta_3] $?

    $ 1-\zeta_3 $ with norm 3

    $ N(a+b\zeta_3) = p \equiv 1\operatorname{mod}3 $

    $ p\equiv 2 \operatorname{mod}3 $.

- For $ K = {\mathbf{Q}}(\sqrt{-D}) $, what is $ {\mathbf{Z}}_K $?

    $ {\mathbf{Z}}_K = {\mathbf{Z}} { \left[ \scriptstyle {\sqrt{-D}} \right] }  $ if $ D\not\equiv 1 \operatorname{mod}4 $.

    $ {\mathbf{Z}}_K = {\mathbf{Z}} { \left[ \scriptstyle {1 + \sqrt{D} \over 2} \right] }  $ if $ D\equiv 1 \operatorname{mod}4 $.

- Which imaginary quadratic fields are PIDs? Which are EDs?

    Only 9 cases: $ -1,-2,-3,-7,-11,-19,-43,-67,-163 $.

    EDs: the first five.

    Note: only finitely many units.

- Which real quadratic fields are PIDS? EDs?

    EDs: Finitely many.

    PIDs: unknown, conjecturally infinitely many.

    Infinitely many units: $ a^2-b^2D=1 $ is the Pell equation and has infinitely many solutions.

- Give an example of failure of unique factorization.

    $ 6 = (2)(3) = (1+\sqrt{-5})(1-\sqrt{-5})\in {\mathbf{Z}} { \left[ \scriptstyle {\sqrt{-5}} \right] }  $, all factors are irreducible.
