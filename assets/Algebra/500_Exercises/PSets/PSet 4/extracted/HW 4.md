Algebra Hw 4 Zack\
Garza\
①Since Fis albraically closed over K, every foxe Fx] has a root in F. Since we want to\
showthat Es ao aiclly closed t ie Ex we willshw has a root inE.Wring\
$f ( x ) = \sum \limits _ { i = 0 } ^ { n } e _ { i } x ^ { i }$ , consider J:=Kle,…. en); then fe J(x]. We have [J:K]=n<oo, and Since\
K≤J≤E≤FK[x]≤ J[x]=E[x]≤F[x] we have e F[x]and sof hasa root α E F. But\
[J(o): J]< 00, and thus [Ja:k]= [Ja): J][J: K] <00, so α is algebraic over k. Then\
de E by defn.
But then E is an algebraic extension of k that is algebraiclly closed, so E\
is an algebraic closure of k.\
OEK\
Suppose K= { k,., kn3 is finite, and consider $f ( x ) = k _ { 1 } + \frac { n } { 1 - 1 } ( x - k _ { i } ) \in k [ x ] . T _ { h e n }$\
f(ki)= ki≠O, so f has noroot inK.Thos K≠K.\
3) Let p be prime; for any grop G we have $g \in G \Rightarrow g ^ { | G | } = e$ .Since Zp is a field,\
$( \mathbb { Z } _ { P } ^ { \times } \times )$ is a group.
In particular, 1Zip| = p-1, so for every xeZp we have\
Since this also holds for x=0 in Zp, it thus holds for all xe Zp.\
4) Let 1kl=p and consider the Frobenius endomorphism $\phi : k  k _ { p }$\
This is afield homorphism e charkphs\
$\phi ( x + y ) = ( x + y ) ^ { p } = \sum \limits _ { i = 0 } ^ { p } ( \sum \limits _ { i = 1 } ^ { p } x ^ { i } y ^ { i } ) ^ { p - i } = x ^ { p } + y ^ { p } = \phi ( x ) + \phi ( y ) .$\
diides ll but frat er\
We have ker $\phi = \phi ^ { - 1 } ( 0 ) = \{ x \in k \vert x ^ { p } = 0 \} = 0 ,$ since no element of a field can\
be nilpotent otherwis xx=0= x isa zero divisor⇒x is not a unit).
But then\
is an injective endomorphism and thus bijective sine |doman=range()<oo.Thas for\
every xek, "(x) is a unique pth root of x.

Suppose K is a field, then $\vert k \vert = 0 ^ { n }$ and char(k)=p. If p=2,then : K→k is an\
X →}\$\
automorphism and thus we can write any xek as $x = \frac { - 1 } { 4 } ^ { 2 } ( x ) ^ { 2 } + 0 ^ { 2 }$\
Otherwise, p is odd, so $1 k ^ { x } 1 = \rho - 1$ is even , S0 $p ^ { n } - 1 = 2 l$ for some leN. Morever, K× is\
cyclic, Ssay $k ^ { x } = \langle \alpha \rangle ,$ Where $\cos ( \alpha ) = p ^ { n } - 1 = 2 \ell ,$ SO\
$\alpha ^ { 2 m } = ( \alpha ^ { m } ) ^ { 2 } = \beta \neq 1 \int \limits _ { 0 } ^ { \infty } e a c h m s t . 1 \sin < 2 \pi$\
so this yelds $e = ^ { p ^ { n } - 1 } / 2$ elts that are the squares of some elements in $k ^ { x } \leq k$ . Now fix some\
$x \in K ^ { x }$ , and consider the sets\
$P = \{ ( x ^ { m ^ { 2 } } ) 1 \leq m < 2 \} \cup \{ 0 \} \cos \alpha _ { m } d \alpha = \frac { 1 } { 2 } x - p 1 p \in P \}$\
We have $\vert P \vert = \vert Q \vert = \frac { 1 } { 2 } ( p ^ { n } - 1 ) + 1 ,$ So\
$| Q | + | P | = p ^ { n } + 1 > | K | .$\
So QnP is nonempty, but then $\beta \in \ Q \cap \beta \Rightarrow \beta = ( a ^ { n } ) ^ { 2 } = x - ( d ^ { n } ) ^ { 2 }$ for some m,n, s0\
$x = ( \alpha ^ { n } ) ^ { 2 } + ( \alpha ^ { n } ) ^ { 2 }$\
as desired.\
6) Since ged(p,n)=1, write 1=tp+sn for some tse Z. Then\
$v _ { t } F \Rightarrow v = ( t _ { p } + s _ { n } ) v = t _ { p } v + s _ { n } v _ { t } F .$\
Since char K=p tpv=O, so V=s(nv).\
Since nveK, SInve K, and so VeK must hold as well.\
7) Let n=[F:K]oo where pXn.
Let de F and $f ( x ) = \min ( \alpha , F ) \in k ( x ) \in F ( x ) ;$ We will show\
that has no repeated roots.
We have $d = d e g f = [ K ( \alpha ) : K ]$ and thus f)=x+c+…+c,x+co.But\
$n = [ F \cdot K ] = [ F \cdot K ( a ) ] [ k ( d ) \cdot K ] = m \cdot d$ , and since ptn, wehave pd, so we can compaute\
$f ^ { \prime } ( x ) = d x ^ { d - 1 } + \cdots c _ { 1 }$ and the leading cofficient is nonzero.
So $d y \uparrow = d - 1 < d y f = d$ Moreover,\
f is  the Zeo polyoaieined, ea o psibe\
$\log 4 < \log 4 . S _ { 0 }$\
gcd(f,f=, which happens iff f has o repeted oots.
