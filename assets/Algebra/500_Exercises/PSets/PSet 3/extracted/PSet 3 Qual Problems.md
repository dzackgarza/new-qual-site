a) If :RS is a ring homomorphism, then Kerp eR is an ideal But the only ideals

of a field are O \$ R, so either  is injective or the zero map.

b) No, consider φ: Zp(x]→Zp

f →f(1)

Then f=x+(-1)^2 1+-1=0eZp}\$ whee f2÷fbut \$(f)=(f)=0 soker≠0.\$f2=(p-1)x+x^2p-1+1=0Zp

②If F contains anilpotent (x=) then F contains a zero divisor (x.x) and thos F

Can't be a field since

Bt    m

of the term of a direct sum decomposition, forcing tht term to not be a field.

So Zn=Feach Fi afield iff nis squre-freie.
n= withdi=1 i.

Then take each F: to be Zp and Zn=Zp: by the Chinese RemainderTheorem.

Suppx i nertibe Then re I:RR. This san deoe  is t eb ann

be invertible for any teR. It is Maxmal becaue if IJ, then J contai a un sJ=R.

Supposx reI for some maximal I. If reR, then rER, so r'reI⇒II, so I=R which

Contradicts the maximality of I. So r can not be a unit.

) G is solvable if there is a normal senes with abelian quotientse.

|^H,{}H2{.\$H_n=G\$Hi/H\$alian

b) #G=36=2.3,50

$$
n _ { 2 } = 1 m o d 2 n _ { 3 } = 1 m o d 3
$$

So n2{13,93 &n3{1,4}

If n_3=1,take

<!-- image-->

Otherwise $n _ { 3 } = 4 ,$ so define

Then iS4 and since S is solabe So is an goup

$$
| G | \leq | S _ { 4 } | \Rightarrow 3 6 \leq 2 4
$$

$$
\phi = \bigcap \limits _ { i = 1 } ^ { 4 } N _ { G } ( H _ { i } ) \triangleq G
$$

$$
S _ { y } \vert ( 3 , 5 ) = 3 \vert 4 _ { 1 } , \cdots , 1 + 4 3 \vert ,
$$

So Kerd is solvable & G/kerd= im  is solvable, which implies that Gitself is also solvable.
