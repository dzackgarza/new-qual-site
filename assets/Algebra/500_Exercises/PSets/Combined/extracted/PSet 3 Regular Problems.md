$$
O \triangleq \hat { H } _ { 1 } \underset { 1 } { \Delta } H _ { 2 } \underset { - } { \Delta } \cdot \ r \cdot \hat { \leq } H _ { n } = G \gamma _ { N } \quad \quad \mathrm { s . t . } \quad \forall i \underset { \forall i _ { i - 1 } } { \forall } \quad \mathrm { i } \underset { \forall i _ { i - 1 } } { \forall } \quad \underset { \forall i _ { i } \in \ b { \mathscr { S } } } { \forall i _ { i } } \underset { \forall i _ { i } } { \forall i _ { 2 } } .
$$

So consider

$$
O \div \sqrt { 1 } \leq \sqrt { 2 } \leq \cdots \Delta - 1 = G
$$

and Ji = J/N Hi which is simple, so this is a composition\
Ji-1 Ji-/N Hi-1 Series for G.\
3 Suppose 1G1=p{q,then\
np = 1 modp\
np1q nq| p^{}\$\
Case1: Suppose q=P so #G=p3. Then#Z(G)=P, p2 0r 32\
1Z(G) = 3  G is abelian, so G,G]=e and the derivedSeries terminates\
So G is solvable.
v\
∴12(G)|=122G)=G\
← ↑\
P ] size of quotients\
But all groups of order por ae abelian, so Gis solvable.\
|2(G)|=1ZG)G\
↑ ↑\
P \$] size of quotients\
So G is again Solvable.
v\
Case 2: Suppose q<p. Then Since np=Imodp, or equivalently plnp-1,\
we have either np=1 or p≤np-1.Supose np+1. Then nplqnp≤q,so\
{n_-1≤q-1≤q1\
contradicting p>q.¥\
Otherwise, np=1, so QpeSyl(p,G) is normal, and\
↑\
2\$ a I Size of quotients\
But piq are prime, so G is Solvable.
v\
Cose 2: q>. Then nqe{1 P P33 and npE{1,q3.\
.If np=1 or nq=1 Gwillbe sovable, using either Qp or Qq in a\
normal series.

Otherwise, suppose np=q and $n _ { q } \neq 1$

1)\$nq=p=\$ Then since nq = 1mod q, so q≤nq-1=p-1<p and we find that q <p. But we assumed q>p, a contradiction.
\*

$$
n _ { q } = 0 ^ { 2 }
$$

Then bycanting elementsthere are nq(q-1)=p{q-1)=p^2-2 elements of order q, and thos p elements of order not q land no zer.
The ny he sibe der  pnd . Buy one sylow-p subgroup contributes (p-1) such elements, and ifnp>

$$
\# e l t 5 = \mathbb { r } _ { \stackrel { 0 } { \eta } _ { 0 } } ( \check { \eta } _ { \mathrm { e } } ^ { - 1 } ) + \mathcal { \hat { Z } } \left( \int _ { 0 } ^ { 2 } - 1 \right) + \underline { { q } } _ { \mathrm { i } } \left[ \varphi _ { \mathrm { p } } ^ { - 1 } \right) = \check { \bar { \varphi } } ^ { 2 } ( \acute { \eta } _ { \mathrm { e } } ^ { - 1 } ) + ( \bar { \varphi } ^ { - 1 } ) + \underline { { q } } _ { \mathrm { i } } ( \hat { \varphi } ^ { - 1 } ) + \mathrm { i } 
$$

(): F/K is a I -dimensional Vector space, So F =(e,>for some basis\
element.
We always have KEF, So the claim is Fck.
Since IeF, then\
I = KE, For some KEK, and So E,=K' K and thus dèEK VαEk.So\
FEK.

c) If u has degree n over K, then [K(w: K]=n. Since Kiu) F, we have\
[F: K]=[F:Kw][Ku):K]\
⇒ a = b n → n divides a.\
⑤ Since K(²) c Klu), we have\
[K(W):K]=[K(u):K(u2)][K(2):K]\
odd = m n\
So both m and n must be odd Since fe Kii)(x] given by fix)=x-²2 has degree\
2 and f(u)=0, m≤2.Since mis odd,m=1, S0 K(u)=k(u2).\$\
6\
a) We have x -2=(x+\2)x-2) irredcible verQ,0\
②(√2):0]=2,(√s2)=Span(11√2}).\
We also have x -3=(x+√s3)(x-\s3) irreducible over , and Since if\s3cQ(\s2) we\$\
would have\s3 =a+b√\s23=a+2b}+2ab√s2}=s2=(3-a-2b)/2ab e\$wefind\
that x2-3 is ineduible over Q(√2)as well and [Q(√s √3): (√2)=2.\
Thus[(√s√3):Q]=2.2=4and(√s2s3)=San1,2√s3 √s3\
b) Wehave3=2(\s3+i)E(i √\s3),so (i\s,3=(i \s3).Since min(√\s3,②)=x^{-3and\
m.n(iQ(s)=min()=x+1,[(i s3)]=[②li√s):(√s)((√s):=2.2=4and\
①(i\s3)=span({1,i, \s3,i\s3).\
) Any two -vector spaces of the same dimension are isomorphic, and minli, )=x+\
min(√2,)=x-2=dim (i)=dim(√2)=2. eQ\
I φ:Q(\2) is an isomorphism offields,then x@d(x)=x If (2=a+bi,thon\
2=(√s2}=f(√s2^}2)=f(√\s^}=(a+i}=a-b}+b\
So a-b=2and 2ab=0,whichors eiter a=2rb=-2 contradig abe.So can noexst
