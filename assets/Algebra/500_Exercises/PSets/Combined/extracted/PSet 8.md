## Problem Set 8

D. Zack Garza

November 18, 2019

## Contents

1 Regular Problems 1   
1.1 Problem 1 . 1   
1.1.1 Part a 1   
1.1.2 Part 2 2   
1.1.3 Part 3 3   
1.1.4 Part 4 . 3   
1.2 Problem 2 . . 5   
1.3 Problem 3 . . 6   
1.4 Problem 4 . . 7   
1.5 Problem 5 . 8   
1.6 Problem 6 . . 9   
1.6.1 Part 1 9   
1.6.2 Part 2 9   
1.7 Problem 7 . 11   
2 Qual Problems 13   
2.1 Problem 8 . 13   
2.1.1 Part 1 13   
2.1.2 Part 2 14   
2.2 Problem 9 . . 14   
2.2.1 Part 1 14   
2.2.2 Part 2 . 14   
2.3 Problem 10 15   
2.3.1 Part 1 . 15   
2.3.2 Part 2 15   
2.3.3 Part 3 . 16

## 1 Regular Problems

## 1.1 Problem 1

$$
\begin{array} { r } { \phi _ { \mathrm { e v } } : \mathrm { h o m } _ { \mathbb { Z } } ( \mathbb { Z } _ { m } , A ) \to A } \\ { ( f : \mathbb { Z } _ { m } \to A ) \mapsto f ( 1 ) } \end{array}
$$

Then $\phi _ { \mathrm { e v } }$ is a Z-module homomorphism, since

$$
\begin{array} { r l } { \phi _ { \mathrm { e v } } ( n f + g ) = ( n f + g ) ( 1 ) \quad } & { } \\ & { = n f ( 1 ) + g ( 2 ) \quad } \\ & { = n \phi _ { \mathrm { e v } } ( f ) + \phi _ { \mathrm { e v } } ( g ) } \end{array}
$$

But this forces $f ( \overline { { 0 } } ) = 0 _ { A }$ (where ${ \overline { { 0 } } } : \mathbb { Z } _ { m } \to A$ is the zero map), we have

$$
0 = f ( 0 ) = f ( m ) = m f ( 1 ) ,
$$

we must have $m f ( 1 ) = 0$ in A. So

$$
\mathrm { i m ~ } \phi _ { \mathrm { e v } } = \{ a \in A \mid m a = 0 \} : = A [ m ] .
$$

It is also the case that

$$
\ker \phi _ { \mathrm { e v } } = \{ f \in \mathrm { h o m } _ { \mathbb { Z } } ( \mathbb { Z } _ { m } , A ) \mid f ( 1 ) = 0 \} = \{ \overline { { 0 } } \} ,
$$

which follows from the fact that $\mathbb { Z } _ { m } = \langle 1$ mod mi and $A = \left. 1 _ { A } \right.$ as Z-modules, so if $f ( 1$ mod $m ) =$ $0 _ { A }$ then

$$
f ( n \mod m ) = n f ( 1 \mod m ) = 0
$$

and so f is necessarily the zero map. So ker $\phi = \overline { { 0 } }$

We can then apply the first isomorphism theorem,

$$
{ \frac { \hom _ { \mathbb { Z } } ( \mathbb { Z } _ { m } , A ) } { \ker \phi _ { \mathrm { e v } } } } \cong \operatorname { i m } \phi _ { \mathrm { e v } } \implies \hom _ { \mathbb { Z } } ( \mathbb { Z } _ { m } , A ) \cong A [ m ] .
$$

## 1.1.2 Part 2

Lemma: If x  n and x  m then x  gcd(m, n)

Proof: We have $x \mid k m + \ell n$ for any integers k, \`. So let $d = \operatorname* { g c d } ( m , n )$ , then there exist integers $a , b$ such that $a m + b n = d .$ But we can now just take $k = a$ and $\ell = b$ .

We claim that $\mathbb { Z } _ { n } [ m ] \cong \mathbb { Z } _ { ( m , n ) }$ , from which the result immediately follows by part 1.

Define a map

$$
\begin{array} { r } { \phi : \mathbb { Z } \to \mathbb { Z } _ { n } [ m ] \quad } \\ { 1 \mapsto [ 1 ] \mod n , } \end{array}
$$

which we claim is an isomorphism. φ is clearly surjective since $\mathbb { Z } \to \mathbb { Z } _ { n }$ is a quotient map and $\mathbb { Z } _ { n } [ m ]$ is a subgroup of $\mathbb { Z } _ { n }$ , and if we let $d : = \operatorname* { g c d } ( m , n )$ , we have

$$
{ \begin{array} { r l } & { \operatorname { k e r } \phi = \{ x \in \mathbb { Z } _ { n } \ \operatorname { \mathrm { ~ } } _ { 3 } \operatorname { \mathrm { ~ } } m x = 0 \} } \\ & { \qquad = \left\{ x \in \mathbb { Z } _ { n } \ \operatorname { \mathrm { ~ } } _ { 3 } x \ { \Big | } \ m \right\} } \\ & { \qquad = \left\{ x \in \mathbb { Z } \ \operatorname { \mathrm { ~ } } _ { 3 } \ x \ { \Big | } \ n { \mathrm { ~ } } \ \operatorname { a n d } \ x \ { \Big | } \ m \right\} } \\ & { \qquad = \left\{ x \in \mathbb { Z } \ \operatorname { \mathrm { ~ } } _ { 3 } x \ { \Big | } \ d \right\} \quad \operatorname { b y ~ t h e ~ l e m m a } } \\ & { \qquad = d \mathbb { Z } \ . } \end{array} }
$$

Then by the first isomorphism theorem, we have

$$
{ \frac { \mathbb { Z } } { \ker \phi } } \cong \operatorname { i m } \phi \implies { \frac { \mathbb { Z } } { d \mathbb { Z } } } \cong \mathbb { Z } _ { n } [ m ] .
$$

## 1.1.3 Part 3

Note: let $[ x ] _ { m }$ denote the equivalence class of x mod m.

Let $f \in \mathbb { Z } ^ { * } = \hom _ { \mathbb { Z } } ( \mathbb { Z } _ { m } , \mathbb { Z } )$ , so $f : \mathbb { Z } _ { m } \to \mathbb { Z } .$ . These are both Z-modules generated by their identity elements, so such a map is determined by where it send $[ 1 ] _ { m } .$

So let $f ( [ 1 ] _ { m } ) = n \in \mathbb { Z }$ . Since $f$ is a module homomorphism, we have $f ( [ 0 ] _ { m } ) = 0$ , and in particular we have

$$
\begin{array} { r l } {  { 0 = f ( [ 0 ] _ { m } ) } } \\ & { = f ( [ m ] _ { m } ) } \\ & { = f ( [ 1 m ] _ { m } ) } \\ & { = m f ( [ 1 ] _ { m } ) , } \end{array}
$$

which forces $f ( [ 1 ] ) \in \mathbb { Z } [ m ] = \{ 0 \}$ , so f must be the zero map and $\mathbb { Z } ^ { * } = 0$

Note: $\mathbb { Z } [ m ] = 0$ because Z is an integral domain, so mx = 0 forces m = 0 or x = 0.

## 1.1.4 Part 4

To see that $\mathbb { Z } _ { m }$ is a $\mathbb { Z } _ { m k }$ module, we define an action

$$
\begin{array} { c } { \mathbb { Z } _ { m k }  \mathbb { Z } _ { m } } \\ { \lbrack x \rbrack _ { m k }  \lbrack y \rbrack _ { m } : = \lbrack x y \rbrack _ { m } } \end{array}
$$

This is a well-defined action:

If $[ x _ { 1 } ] _ { m k } = [ x _ { 2 } ] _ { m k }$ are two representatives of the same equivalence class, then

$$
[ x _ { 1 } ] _ { m k } - [ x _ { 2 } ] _ { m k } = [ x _ { 1 } - x _ { 2 } ] _ { m k } = [ 0 ] _ { m k } \implies m ~ \Big | ~ x _ { 1 } - x _ { 2 } .
$$

But then

$$
\begin{array} { r l r } & { } & { ( [ x _ { 1 } ] _ { m k } \cap [ y ] _ { m } ) - ( [ x _ { 2 } ] _ { m k } \cap [ y ] _ { m } ) = [ x _ { 1 } y ] _ { m } - [ x _ { 2 } y ] _ { m } } \\ & { } & { \qquad = [ ( x _ { 1 } - x _ { 2 } ) y ] _ { m } } \\ & { } & { \qquad = [ 0 ] _ { m } , } \end{array}
$$

which shows that their resulting actions on $\mathbb { Z } _ { m }$ are equal.

This action yields a module structure:

$r . ( x + y ) = r . x + r . y { \mathrm { : } }$

$$
[ r ] _ { m k } \cap ( [ x ] _ { m } + [ y ] _ { m } ) = [ r ] _ { m k } \cap [ x + y ] _ { m } = [ r ( x + y ) ] _ { m } = [ r x ] _ { m } + [ r y ] _ { m } .
$$

$( r + s ) . x = r . x + s . x { \mathrm { : } }$

$$
[ r ] _ { m k } + [ s ] _ { m k } \cap [ x ] _ { m } = [ r + s ] _ { m k } \cap [ x ] _ { m } = [ ( r + s ) x ] _ { m } = [ r x ] _ { m } + [ s x ] _ { m } .
$$

$( r s ) . x = r . s . x \colon$

$$
\begin{array} { r l } & { [ r ] _ { m k } \cdot [ s ] _ { m k } \curvearrowright [ x ] _ { m } = [ r s ] _ { m k } \cap [ x ] _ { m } } \\ & { \qquad = [ ( r s ) x ] _ { m } } \\ & { \qquad = [ r ] _ { m k } \curvearrowright [ s x ] _ { m } } \\ & { \qquad = [ r ] _ { m k } \curvearrowright ( [ s ] _ { m k } \curvearrowright [ x ] _ { m } ) . } \end{array}
$$

$1 . x = x \colon$

$$
[ 1 ] _ { m k } \cap [ x ] _ { m } = [ 1 x ] _ { m } = [ x ] _ { m } .
$$

$$
\mathbb { Z } _ { m } ^ { * } : = \mathrm { h o m } _ { \mathbb { Z } _ { m k } } ( \mathbb { Z } _ { m } , \mathbb { Z } _ { m k } ) \cong \mathbb { Z } _ { m } \colon
$$

Define a map

$$
\begin{array} { r l } & { \phi : \hom _ { \mathbb { Z } _ { m k } } ( \mathbb { Z } _ { m } , \mathbb { Z } _ { m k } ) \to \mathbb { Z } _ { m } } \\ & { \qquad f \mapsto [ f ( [ 1 ] _ { m } ) ] _ { m } } \end{array}
$$

$\phi$ is a homomorphism, as

$$
\begin{array} { r l } { \phi ( f + g ) = [ ( f + g ) ( [ 1 ] _ { m } ) ] _ { m } } & { } \\ { \quad } & { = [ f ( [ 1 ] _ { m } ) + g ( [ 1 ] _ { m } ) ] _ { m } } \\ { \quad } & { = [ f ( [ 1 ] _ { m } ) ] _ { m } + [ g ( [ 1 ] _ { m } ) ] _ { m } } \end{array}
$$

$$
\begin{array} { r l } { \phi ( [ r ] _ { m k } \cap f ) = [ [ r ] _ { m k } f ( [ 1 ] _ { m } ) ] _ { m } } & { } \\ & { = [ r ] _ { m } \cdot [ f ( [ 1 ] _ { m } ) ] _ { m } } \\ & { = [ r ] _ { m k } \curvearrowright \phi ( f ) . } \end{array}
$$

$\phi$ is injective, as $[ f ( [ 1 ] _ { m } ) ] _ { m } = [ 0 ] _ { m }$ , then for any $1 \leq \ell \leq m$ , we have

$$
\begin{array} { r l } & { [ f ( [ \ell ] _ { m } ) ] _ { m } = [ \ell f ( [ 1 ] _ { m } ) ] _ { m } } \\ & { \qquad = \ell [ f ( [ 1 ] _ { m } ) ] _ { m } } \\ & { \qquad = \ell [ 0 ] _ { m } } \\ & { \qquad = [ 0 ] _ { m } , } \end{array}
$$

so f must be the zero map.

$\phi$ is surjective, since if $[ \ell ] _ { m } \in \mathbb { Z } _ { m }$ , we can define

$$
\begin{array} { r } { f _ { \ell } : \mathbb { Z } _ { m } \to \mathbb { Z } _ { m k } } \\ { [ 1 ] _ { m } \mapsto [ \ell ] _ { m k } } \end{array}
$$

which makes sense and is well-defined because $\mathbb { Z } _ { m } \hookrightarrow \mathbb { Z } _ { m k }$ , and the map is defined on the generator.   
So we have the desired bijection.

## 1.2 Problem 2

We have the map

$$
\begin{array} { r } { \pi : \mathbb { Z } \to \mathbb { Z } _ { 2 } } \\ { x \mapsto [ x ] _ { 2 } } \end{array}
$$

which is a surjection and thus an epimorphism in the category Z-Mod, and if we apply the functor homZ $\left( \mathbb { Z } _ { 2 } , \cdot \right)$ to π we obtain an induced map

$$
\begin{array} { r l } & { \pi : \operatorname { h o m } _ { \mathbb { Z } } ( \mathbb { Z } _ { 2 } , \mathbb { Z } ) \to \operatorname { h o m } _ { \mathbb { Z } } ( \mathbb { Z } _ { 2 } , \mathbb { Z } _ { 2 } ) } \\ & { \qquad f \mapsto \pi \circ f . } \end{array}
$$

The claim is that π is not a surjection, and thus not an epimorphism (in the same category).

To see that this is the case, we can simply note that hom $\mathbb { z } ( \mathbb { Z } _ { 2 } , \mathbb { Z } ) = 0$ by part 3 of Problem 1, whereas homZ $( \mathbb { Z } _ { 2 } , \mathbb { Z } _ { 2 } ) \neq 0$

For example, one can define

$$
\begin{array} { r l } { \quad } & { \mathrm { i d } _ { \mathbb { Z } _ { 2 } } : \mathbb { Z } _ { 2 } \to \mathbb { Z } _ { 2 } } \\ & { \quad \quad \quad [ x ] _ { 2 } \mapsto [ x ] _ { 2 } , } \end{array}
$$

which is a nontrivial module homomorphisms.

So any such f appearing must be the zero map, and thus π is also the zero map.

## 1.3 Problem 3

Let $f : R  R$ be an endomorphism of R in the category of rings. We can then check that for any $r \in R ,$ , we have $f ( r ) = f ( r 1 _ { R } ) = r f ( 1 _ { R } )$ , which says that f is given by right-multiplication by some fixed element $x _ { f } : = f ( 1 _ { R } )$ , i.e.

$$
\begin{array} { c } { f : R  R } \\ { r \mapsto r \cdot x _ { f } } \end{array}
$$

and so we can attempt to define

$$
\begin{array} { r l r } { \quad } & { { } } & { \phi _ { 1 } : \hom _ { R } ( R , R ) \to R } \\ { } & { { } } & { f \mapsto x _ { f } : = f ( 1 _ { R } ) } \end{array}
$$

We can check that

$$
( g \circ f ( r ) ) = g ( f ( r ) ) = g ( r \cdot x _ { f } ) = r \cdot x _ { f } \cdot x _ { g } ,
$$

which shows that in fact

$$
\phi ( g \circ f ) = x _ { f } \cdot x _ { g } ,
$$

which reverses the multiplication. So the correct codomain is $R ^ { o p }$ , and we amend the definition:

$$
\begin{array} { r l } { \phi _ { 2 } : \hom _ { R } ( R , R ) \to R ^ { o p } } & { { } } \\ { f \mapsto x _ { f } : = f ( 1 _ { R } ) } & { { } } \end{array}
$$

By construction, $\phi _ { s }$ is a ring homomorphism. If R is commutative, then $x _ { f } \cdot x _ { g } = x _ { g } \cdot x _ { f }$ , which makes $\phi _ { 1 }$ a ring homomorphism as well. It remains to check that it is an isomorphism/

$\phi _ { 1 }$ is in injective: We can check that ker $\phi _ { 1 } = 0$ as a ring. To that end, suppose $\phi _ { 1 } ( f ) = x _ { f } = 0$ Then $f ( r ) = r \cdot 0 = 0 $ , so $f$ can only be the zero map.

$\phi _ { 1 }$ is surjective: Let $x \in R$ be arbitrary, then we can define $f : R  R$ by $f ( 1 _ { R } ) = x$ , so $f ( r ) = r \cdot x$ . This is an endomorphism of $R ,$ and thus an element of hom $\textstyle R ( R , R )$

By the first isomorphism theorem for rings, we thus have hom $\mathbf { \Omega } _ { \cdot R } ( R , R ) \cong R$

## 1.4 Problem 4

Note: Let $X ^ { \vee } : = \hom _ { R } ( X , R )$ denote the dual.

We have maps

$$
\begin{array} { c } { { \theta _ { A } : A \to ( A ^ { \vee } ) ^ { \vee } } } \\ { { a \mapsto ( \mathrm { e v } _ { a } : f \mapsto f ( a ) ) } } \end{array}
$$

$$
\begin{array} { c } { { \theta _ { B } : B \to ( B ^ { \vee } ) ^ { \vee } } } \\ { { b \mapsto ( \mathrm { e v } _ { b } : g \mapsto g ( b ) ) } } \end{array}
$$

$$
\begin{array} { c } { { f : A \to B } } \\ { { a \mapsto f ( a ) } } \end{array}
$$

$$
\begin{array} { c } { f ^ { \vee } : B ^ { \vee }  A ^ { \vee } } \\ { g \mapsto g \circ f } \end{array}
$$

$$
\begin{array} { c } { f ^ { \vee \vee } : A ^ { \vee \vee }  B ^ { \vee \vee } } \\ { h \mapsto h \circ f ^ { \vee } } \end{array}
$$

We can now check that $f ^ { \vee \vee } \circ \theta _ { A } = \theta _ { B } \circ f$ as maps from A to $B ^ { \vee \vee }$ . Letting $a \in A$ , and $h \in B ^ { \vee \vee }$ (so $h : B ^ { \vee } \to R )$ , we will show that both maps act on h in the same way.

For notational convenience, write φ $\curvearrowright h : = h \circ \phi$ . We then have

$$
\begin{array} { r l } & { ( f ^ { \vee \vee } \circ \theta _ { A } ) ( a ) \cap \wedge = f ^ { \vee \vee } ( \theta _ { A } ( a ) ) \cap \wedge } \\ & { \qquad : = f ^ { \vee \vee } ( \mathrm { e v } _ { a } ) \cap h } \\ & { \qquad = ( \mathrm { e v } _ { a } \circ f ^ { \vee } ) \wedge h } \\ & { \qquad : = h \circ ( \mathrm { e v } _ { a } \circ f ) } \\ & { \qquad : = h ( f ( a ) ) } \\ & { \qquad = \mathrm { e v } _ { f ( a ) } \wedge h } \\ & { \qquad : = \theta _ { B } ( f ( a ) ) \wedge h } \\ & { \qquad : = ( \theta _ { B } \circ f ) ( a ) \wedge h , } \end{array}
$$

which shows that these actions agree, and thus the diagram commutes.

## 1.5 Problem 5

Let E be a free module over R an integral domain. Then E has a basis $\{ \mathbf { e } _ { i } \} \subseteq F$ , so if x $\neq 0 \in E$ we have

$$
x = \sum _ { i } r _ { i } \mathbf { e } _ { i }
$$

where each $r _ { i } \in R$ . Moreover, since $x \neq 0$ , at least one $r _ { i } \neq 0$ , so let $r _ { j }$ denote one of the nonzero coefficients.

Now suppose x is a torsion element, so $m x = 0$ for some m $\neq 0 \in E$ . We can then write

$$
m x = m \sum _ { i } r _ { i } \mathbf { e } _ { i } = \sum _ { i } m r _ { i } \mathbf { e } _ { i } = 0
$$

But by linear independence, this forces $m r _ { i } = 0$ for all i. In particular, $m r _ { j } = 0$ where $r _ { j } \neq 0$ . But this exhibits either m or $r _ { j }$ as a zero divisor, and since the only zero divisor in an integral domain is zero, we must have $m = 0$ or $r _ { j } = 0$ , a contradiction.

So x can not be a torsion element. But since $x \in E$ was arbitrary, $E$ must be torsion-free.

For an example of a torsion-free module over an integral domain that is not free, consider $\mathbb { Q }$ as a Z-module. Then $\mathbb { Q }$ is clearly torsion-free, since it is an integral domain and the same argument as above applies.

But Q is not free as Z-module. Supposing that $B = \{ \mathbf { b } _ { 1 } , \mathbf { b } _ { 2 } , \cdot \cdot \cdot \} \subset \mathbb { Q }$ was a Z-basis, consider $\begin{array} { r } { \mathbf { b } _ { 1 } = \frac { p _ { 1 } } { q _ { 1 } } } \end{array}$ and $\begin{array} { r } { \mathbf { b } _ { 2 } = \frac { p _ { 2 } } { q _ { 2 } } } \end{array}$ . Then $\mathbf { b } _ { 1 }$ , b2 can not be linearly independent over $\mathbb { Z } .$ which follows from the fact that

$$
q _ { 1 } p _ { 2 } { \bf b } _ { 1 } + q _ { 2 } p _ { 1 } { \bf b } _ { 2 } = p _ { 2 } p _ { 1 } - p _ { 1 } p _ { 2 } = 0 ,
$$

while $q _ { 1 } p _ { 2 }$ , q2p1 $\neq 0 \in \mathbb { Z }$

## 1.6 Problem 6

If A is a cyclic module over a commutative ring R, so we have $A = R a$ for some $a \in A$ . By Hungerford’s definition, the submodule A has order $r \iff$ the element a has order $r \iff$ the order ideal $\mathcal { O } _ { a } : = \{ x \in R \mid x a = 0 \} = ( r )$

In particular, $r a = 0$

## 1.6.1 Part 1

Since $( r , s ) = ( 1 )$ , we can find $t _ { 1 } , t _ { 2 } \in R$ such that

$$
\begin{array} { r l r } { \quad } & { } & { t _ { 1 } r + t _ { 2 } s = 1 \implies t _ { 1 } r a + t _ { 2 } s a = 1 a } \\ { \implies } & { } & \\ { \implies t _ { 1 } ( r a ) + t _ { 2 } s a = a } \\ { \implies } & { } & \\ { \implies t _ { 2 } s a = a } \\ { \implies s ( t _ { 2 } a ) = a } & { } & { \mathrm { s i n c e ~ } R \mathrm { ~ i s ~ c o m m u t a t i v e } , } \end{array}
$$

which implies that $a \in s A$ and thus $A \subseteq s A$ . However, we always have $s A \subseteq A$ for modules, so this shows that $A = s A$

To see that $A [ s ] = \{ x \in A \mid s x = 0 \} = 0$ , let $x \in A [ s ] ;$ ; we will show $x = 0$ . Since $x \in A = R a$ , we have $x = r _ { 1 } a$ , and in particular

$$
r a = 0 \implies r x = r r _ { 1 } a = r _ { 1 } ( r a ) = 0 .
$$

So we now have $r x = 0$ and $s x = 0$ , and we can write

$$
{ \begin{array} { r l } & { x = ( t _ { 1 } r + t _ { 2 } s ) x } \\ & { \quad = t _ { 1 } ( r x ) + t _ { 2 } ( s x ) } \\ & { \quad = t _ { 1 } 0 + t _ { 2 } 0 } \\ & { \quad = 0 . } \end{array} }
$$

So $x = 0$ and thus $A [ s ] = 0$

## 1.6.2 Part 2

Suppose $r = s k$ . Toward an application of the first isomorphism theorem, define a map

$$
\begin{array} { c } { { \phi : R  s A = s R a } } \\ { { { } } } \\ { { x \mapsto s x a . } } \end{array}
$$

φ is well-defined:

This follows from that fact that $a \in A \implies x A \in A$ for any $x \in R ,$ , so the codomain is in fact $s A .$ $\phi$ is an R-module homomorphism:

We have

$$
\begin{array} { c } { { t \in R \implies \phi ( t x ) = s ( t x ) a = t ( s x a ) = t \phi ( x ) } } \\ { { x , y \in R \implies \phi ( x + y ) = s ( x + y ) a = s x a + s y a + \phi ( x ) + \phi ( y ) } } \end{array}
$$

ker $\phi = ( k )$

Suppose $x \in$ ker φ so $s x a = 0 _ { A } ;$ we’d like to show $x \in ( k )$

By definition $s x \in \mathcal { O } _ { a } .$ , and by assumption $\mathcal { O } _ { a } = ( r )$ , so $s x = t _ { 1 } r$ for some $t _ { 1 } \in R$

$$
\begin{array} { r l r } { s x a = 0 _ { A } } \\ { \implies s x = t _ { 1 } r } & { } & { \qquad \mathrm { s i n c e } ~ s x \in \mathcal { O } _ { a } } \\ { \implies s x = t _ { 1 } ( s k ) } & { } & { \qquad \mathrm { s i n c e } ~ r = s k ~ \mathrm { b y ~ a s s u m p t i o n } } \\ { \implies s x = s ( t _ { 1 } k ) } & { } & { \qquad \mathrm { s i n c e ~ e l e m e n t s ~ i n ~ } R ~ \mathrm { a n d } ~ A ~ \mathrm { c o m m u t e } } \\ { \implies x = t _ { 1 } k } & { } & { \qquad \mathrm { s i n c e } ~ R ~ \mathrm { i s ~ a ~ d o m a i n } , ~ \mathrm { s o } ~ s m = s n , s \neq 0 ~ \Longrightarrow ~ m = n , } \end{array}
$$

which exhibits $x = t _ { 1 } k \implies x \in ( k )$ as desired.

$\phi$ is surjective:

Since $A = R a$ , we have $s A = s R A$ and thus $x \in s A \implies x = s r a$ for some $r \in R ;$ but then $\phi ( r ) = s r a = x . $

We thus have

$$
R / \ker \phi \cong \operatorname { i m } \phi \implies R / ( k ) \cong s A .
$$

Similarly, define a map

$$
\begin{array} { r } { \psi : R  A [ s ] } \\ { x \mapsto k x a } \end{array}
$$

ψ is well-defined:

It suffices to check that im $\psi \subseteq A [ s ]$ (since we will show surjectivity shortly), i.e. that s annihilates anything in the image. This follows from

$$
s ( k x a ) = ( s k ) x a = r x a = x ( r a ) = 0 ,
$$

since $r a = 0$ by assumption.

## ψ is an R-module homomorphism:

We can check

$$
\psi ( t r _ { 1 } + r _ { 2 } ) = k ( t r _ { 1 } + r _ { 2 } ) s = t k r _ { 1 } s + k r _ { 2 } s = t \psi ( r _ { 1 } ) + \psi ( r _ { 2 } )
$$

which follows because elements of R commute with those from A under multiplication. ker $\psi = ( s )$

Suppose $x \in$ ker ψ, so kxa = 0. Then kx $\in \mathcal { O } _ { a } = \left( r \right)$ , so $k x = r t _ { 1 }$ . Then

$$
\begin{array} { r l } & { k x a = 0 _ { A } } \\ & { \implies k x = r t _ { 1 } } \\ & { \implies k x = ( s k ) t _ { 1 } } \\ & { \implies k x = k ( s t _ { 1 } ) } \\ & { \implies x = s t _ { 1 } } \end{array}
$$

$$
\in { \mathcal { O } } _ { a }
$$

and so $x \in \left( s \right)$ as desired.

ψ is surjective:

Letting $y \in A [ s ]$ be arbitrary. We have

$$
\begin{array} { r l } & { y \in A [ s ] \implies x = t _ { 1 } a , \quad s x = 0 } \\ & { \implies s ( t _ { 1 } a ) = 0 } \\ & { \implies s t _ { 1 } \in \mathcal { O } _ { a } \implies \exists x \in R \ \geqslant s t _ { 1 } = x r = x ( s k ) } \\ & { \implies s t _ { 1 } = s x k } \\ & { \implies t _ { 1 } = x k \qquad \mathrm { s i n c e } \ R \mathrm { i s ~ a ~ d o m a i n } } \\ & { \implies y = t _ { 1 } a = ( x k ) a = k x a , } \end{array}
$$

so $\psi ( x ) = y$

We can then apply the first isomorphism theorem

$$
R / \ker \psi \cong \operatorname { i m } \psi \implies R / ( s ) \cong A [ s ] .
$$

## 1.7 Problem 7

Lemma: If M is a cyclic module over a PID, then M has exactly 1 invariant factor.

Lemma: Let A be a cyclic module, so $A = R a$ . If the order of A is r, so $\mathcal { O } _ { a } = \left( r \right)$ , then $A \cong R / ( r )$ This means that we can write $A = R / ( a )$ and $B = R / ( b )$ , and $a , b$ are the invariant factors of A, B respectively, and $M : = A \oplus B \cong R / ( a b )$

Since R is a PID, there is unique factorization, so we can write

$$
\begin{array} { c } { { r = \displaystyle \prod _ { i = 1 } ^ { n } p _ { i } ^ { k _ { i } } } } \\ { { \displaystyle } } \\ { { s = \displaystyle \prod _ { i = 1 } ^ { n } p _ { i } ^ { \ell _ { i } } } } \\ { { \displaystyle } } \\ { { \implies r s = \displaystyle \prod _ { i = 1 } ^ { n } p _ { i } ^ { k _ { i } + \ell _ { i } } , } } \end{array}
$$

where we allow some $k _ { i } , \ell _ { i } = 0$ so that we can take the product over the same set of primes.

However, means that the elementary divisors of M are given by the multiset $L : = \left\{ p _ { i } ^ { k _ { i } } \right\} \cup \left\{ p _ { i } ^ { \ell _ { i } } \right\}$

The largest invariant factor $d _ { 1 }$ of M is obtained from the elementary divisors by

a. Forming the multiset L of elementary divisors,

b. Selecting the highest power of each prime occurring, say $s _ { i } : = p _ { i } ^ { \operatorname* { m a x } ( k _ { i } , \ell _ { i } ) }$

c. Removing $s _ { i }$ from $L ,$

d. Then letting $d _ { 1 } = \Pi s _ { i }$

However, this process yields $d _ { 1 } = \operatorname { l c m } ( r , s )$ by construction, since

$$
d _ { 1 } = \prod _ { i = 1 } ^ { n } s _ { i } = \prod _ { i = 1 } ^ { n } p _ { i } ^ { \operatorname* { m a x } ( k _ { i } , \ell _ { i } ) } : = \operatorname { l c m } ( r _ { s } ) .
$$

The next largest invariant factor is obtained by performing the same process on the remaining prime powers in $L .$ However, we can note that after obtaining $d _ { 1 }$ , we have $L = \left\{ p _ { i } ^ { \mathrm { m i n } ( k _ { i } , \ell _ { i } ) } \right\}$ , since there were only two choices for each $p _ { i }$ occurring and we chose the copy with the maximal exponent.

But this means when we perform step (b) to obtain $d _ { 2 }$ , there is now only one choice, and thus each $s _ { i } = p _ { i } ^ { \operatorname* { m i n } ( k _ { i } , \ell _ { i } ) }$ and we have

$$
d _ { 2 } = \prod _ { i = 1 } ^ { n } s _ { i } = \prod _ { i } p _ { i } ^ { \operatorname* { m i n } ( k _ { i } , \ell _ { i } ) } : = \operatorname* { g c d } ( r , s ) .
$$

Note: by construction, $d _ { 2 } \ \backslash \ d _ { 1 }$ , since we are choosing from the same prime powers but with smaller exponents.

Since there were only at most two copies of each prime occurring in $L ,$ where one of them was chosen for $d _ { 1 }$ and the other was chosen for $d _ { 2 }$ , this exhausts all of the elements in $L$ . But this means M has only two invariant divisors,

$$
\begin{array} { l } { d _ { 1 } = \operatorname { l c m } ( r , s ) } \\ { d _ { 2 } = \operatorname* { g c d } ( r , s ) , } \end{array}
$$

which is what we wanted to show.

Note: the indexing convention for di is opposite the usual one here, since we are choosing the largest invariant factor first, and so we have $d _ { n } |  d _ { n - 1 } | \cdot \cdot \cdot |  d _ { 1 } $

## 2 Qual Problems

## 2.1 Problem 8

## 2.1.1 Part 1

The claim is that every element in $M : = R ^ { n } / { \mathrm { i m } }$ A is torsion ⇐⇒ the matrix rank of A is exactly $n \iff$ the Smith normal form of A has exactly n nonzero invariant factors.

To see that this is the case, we can apply the structure theorem for finitely-generated modules over a PID. This gives us

$$
M \cong F \oplus \bigoplus R / ( r _ { i } )
$$

where $F$ is free of finite rank, $R / ( r _ { i } )$ is cyclic torsion, and $r _ { i } \mid r _ { i + 1 } \mid \cdots$ · are the invariant factors of M .

We thus have

$$
M \cong R ^ { n } / { \mathrm { i m ~ } } A \cong F \oplus \bigoplus R / ( r _ { i } ) ,
$$

which will be pure torsion if and only if $F = 0$

But if we compute the smith normal for of A, we obtain

$$
S N F ( A ) = \left[ { \begin{array} { r r r r r } { d _ { 1 } } & { 0 } & { \cdots } & { 0 } & { \cdots } & { 0 } \\ { 0 } & { d _ { 2 } } & { \cdot } & { 0 } & { \cdots } & { 0 } \\ { \vdots } & { \vdots } & { \ddots } & { \vdots } & { \ldots } & { 0 } \\ { 0 } & { 0 } & { \cdots } & { d _ { n } } & { \cdots } & { 0 } \end{array} } \right]
$$

where $d _ { 1 } \left| \begin{array} { l } { d _ { 2 } } \end{array} \right| \cdots \left| \begin{array} { l } { d _ { n } } \end{array} \right.$ , and thus

$$
{ \begin{array} { r l } { \operatorname* { i m } A \cong \operatorname* { i m } S N F ( A ) \cong d _ { 1 } R \oplus d _ { 2 } R \oplus \dotsb \oplus d _ { n } R } \\ { \ } \\ { \implies M = R ^ { n } / { \mathrm { i m } } A \cong { \frac { R ^ { n } } { d _ { 1 } R \oplus d _ { 2 } R \oplus \dotsb \cdot d _ { n } R } } } \\ { \ } \\ & { \cong R / ( d _ { 1 } ) \oplus R / ( d _ { 2 } ) \cdot \cdot \cdot \oplus R / ( d _ { n } ) } \end{array} }
$$

where $R / ( d _ { i } )$ is a cyclic torsion module precisely when $d _ { i } \neq 0$ . If instead some $d _ { i } = 0$ , we then have $R / ( d _ { i } ) \cong R$ , which is a free R-module, yielding non-torsion elements in M .

But det $( A ) = \operatorname* { d e t } ( S N F ( A ) ) = \prod _ { i = 1 } ^ { n } d _ { i }$ , and so if $d _ { i } = 0$ for some i iff det A = 0 iff rank $A < n$

## 2.1.2 Part 2

Identifying

$$
R \times F = F [ x ] \oplus F \cong F [ x ] \oplus { \frac { F [ x ] } { ( f ) } }
$$

where f is any degree 1 polynomial in $F [ x ]$ , by the structure theorem we can pick a matrix $A \in$ $M _ { 2 } ( F [ x ] )$ with invariant factors $d _ { 1 } = 0 , d _ { 2 } = f$ . Then by the same argument given in part 1, we would have

$$
( F [ x ] ) ^ { 2 } / { \mathrm { i m ~ } } A \cong { \frac { F [ x ] } { ( d _ { 1 } ) } } \oplus { \frac { F [ x ] } { ( d _ { 2 } ) } } = F [ x ] \oplus { \frac { F [ x ] } { ( f ) } }
$$

So we can choose $n = 2 .$ , and say $f ( x ) = x + 1$ , and then just pick a matrix that is already in Smith normal form:

$$
A = \left[ { \begin{array} { c c } { x + 1 } & { 0 } \\ { 0 } & { 0 } \end{array} } \right] .
$$

## 2.2 Problem 9

## 2.2.1 Part 1

Let M be a finitely generated module over R a PID.

Then

$$
M \cong F \oplus \bigoplus _ { i = 1 } ^ { n } R / ( d _ { i } )
$$

where $F$ is free of finite rank and $R / ( d _ { i } )$ are cyclic torsion modules (the invariant factors) satisfying $d _ { 1 } \mid d _ { 2 } \mid \ldots \mid d _ { n }$

Equivalently,

$$
M \cong F \oplus \bigoplus _ { i = 1 } ^ { n } R / ( p _ { i } ^ { s _ { i } } )
$$

where $F$ is free of finite rank, $p ^ { i } \in R$ are (not necessarily distinct) prime elements (the elementary divisors), and $s _ { i } \in \mathbb { Z } ^ { \geq 1 }$ .

## 2.2.2 Part 2

Since $\mathbb { Z } ^ { 4 }$ is a finitely generated module over the PID $\mathbb { Z } ,$ the structure theorem applies, and we can write $M \cong \mathbb { Z } ^ { k } \oplus \oplus \mathbb { Z } / ( r _ { i } )$ for some $k \leq 4$ and some collection $r _ { i }$ of invariant factors.

If we write $M \cong \mathbb { Z } ^ { 4 } / N$ where N is the submodule generated by the prescribed relations, then we can construct a homomorphism of Z-modules $L : \mathbb { Z } ^ { 4 } \to N$ which is given by the matrix

$$
A _ { L } = \left( \begin{array} { r r r r } { { 3 } } & { { 1 2 } } & { { 3 } } & { { 6 } } \\ { { 0 } } & { { 6 } } & { { 0 } } & { { 0 } } \\ { { - 3 } } & { { 6 } } & { { - 3 } } & { { 0 } } \\ { { 0 } } & { { 0 } } & { { 0 } } & { { 0 } } \end{array} \right)
$$

Then im $A _ { L } \cong N$ , and we can compute the Smith normal form,

$$
S N F ( A _ { L } ) = { \left( \begin{array} { l l l l } { 3 } & { 0 } & { 0 } & { 0 } \\ { 0 } & { 6 } & { 0 } & { 0 } \\ { 0 } & { 0 } & { 6 } & { 0 } \\ { 0 } & { 0 } & { 0 } & { 0 } \end{array} \right) }
$$

which shows that the invariant factors are 3, 6, 6, 0. We can thus write im $A _ { L } \cong 3 \mathbb { Z } \oplus 6 \mathbb { Z } \oplus 6 \mathbb { Z }$ , and so

$$
M \cong \frac { \mathbb { Z } ^ { 4 } } { 3 \mathbb { Z } \oplus 6 \mathbb { Z } \oplus 6 \mathbb { Z } } \cong \mathbb { Z } \oplus \mathbb { Z } / ( 3 ) \oplus \mathbb { Z } / ( 6 ) \oplus \mathbb { Z } / ( 6 ) .
$$

## 2.3 Problem 10

## 2.3.1 Part 1

An element $x \in M$ is torsion iff there exists some nonzero $r \in R$ such that $r m = 0$ , or equivalently $\mathrm { A n n } ( x ) \neq 0$

## 2.3.2 Part 2

Let $R = \mathbb { C } [ x ]$ $M = \mathbb { C } ^ { 2 }$ , and

$$
A = { \left[ \begin{array} { l l } { 1 } & { 2 } \\ { 2 } & { 1 } \end{array} \right] } \in M _ { 2 } ( \mathbb { C } ) .
$$

Then $\mathbb { C } ^ { 2 }$ is a module over C[x] with action given by

$$
p ( x ) \cap \mathbf { v } : = p ( A ) \mathbf { v }
$$

Then M is cyclic as an R-module and generated by the basis vector $[ 1 , 0 ] ^ { 2 } \in \mathbb { C } ^ { 2 }$ , since

$$
\begin{array} { r l r } & { } & { \left( t A + s \right) \left[ \begin{array} { c } { 1 } \\ { 0 } \end{array} \right] = \left[ \begin{array} { c } { x } \\ { y } \end{array} \right] } \\ & { } & { \Longrightarrow \left[ \begin{array} { c c } { t } & { 2 l } \\ { 2 t } & { t } \end{array} \right] \left[ \begin{array} { c } { 1 } \\ { 0 } \end{array} \right] + \left[ \begin{array} { c } { s } \\ { 0 } \end{array} \right] = \left[ \begin{array} { c } { x } \\ { y } \end{array} \right] } \\ & { } & { \Longrightarrow \left[ \begin{array} { c c } { t + s } \\ { 2 t } \end{array} \right] = \left[ \begin{array} { c } { x } \\ { y } \end{array} \right] } \\ & { } & { \Longrightarrow \left[ \begin{array} { c c } { 1 } & { 1 } \\ { 2 } & { 0 } \end{array} \right] \left[ \begin{array} { c } { t } \\ { s } \end{array} \right] = \left[ \begin{array} { c } { x } \\ { y } \end{array} \right] } \end{array}
$$

which is a linear system of equations represented by an invertible matrix, which always has a solution. So every $\mathbf { v } \in \mathbb { C } ^ { 2 }$ is the image of some polynomial in A.

It is then easy to see that $\mathbb { C } ^ { 2 }$ is torsion as a module over $\mathbb { C } [ x ]$ , since by Cayley-Hamilton we have $\operatorname { A n n } ( A ) = ( { \mathrm { m i n p o l y } } ( A ) ) = ( x ^ { 2 } - 2 x - 3 )$ , and so letting $p ( x ) = x ^ { 2 } - 2 x - 3$ , we find that

$$
\forall \ \mathbf { v } \in \mathbb { C } ^ { 2 } \quad p ( A ) \curvearrowright \sim \mathbf { v } = 0 \curvearrowright = 0 .
$$

## 2.3.3 Part 3

Suppose R is a domain, M an R-module, and let

$$
T ( M ) = \left\{ m \in M \ \geqslant r m = 0 { \mathrm { ~ f o r ~ s o m e ~ } } r \neq 0 \in R \right\} .
$$

Then $T ( R )$ is a submodule iff for all $r \in R$ and all $m , n \in T ( M )$ we have $r m + n \in T ( M )$

So pick annihilators $a _ { m } , a _ { n } \neq 0 \in R$ where $a _ { m } m = 0$ and $a _ { n } n = 0$

Since $a _ { m } \neq 0$ and $a _ { n } \neq 0$ , the product $a _ { m } a _ { n } \neq 0$ because R is a domain.

Since $0 \in T ( M )$ , we can suppose $r m + n \neq 0$ (otherwise this is in $T ( M )$ trivially). Then

$$
\begin{array} { c } { { a _ { m } a _ { n } ( r m + n ) = a _ { m } a _ { n } r m + a _ { m } a _ { n } n } } \\ { { \ } } \\ { { \ } } \\ { { \qquad = r a _ { n } ( a _ { m } m ) + a _ { m } ( a _ { n } n ) } } \\ { { \ } } \\ { { \ } } \\ { { \qquad = r a _ { n } 0 + a _ { m } 0 } } \\ { { \ } } \\ { { \qquad = 0 . } } \end{array}
$$

where the commutativity of $r , a _ { n } , a _ { m }$ follows from the fact that these are all elements of $R ,$ which is a domain, and in particular is commutative.