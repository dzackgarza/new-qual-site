# Real Analysis Preliminary Examination (supplemental) August, 2013

## 1 Part I

1. State Fatou's lemma.

2. State Egoroff's theorem.

## 2 Part 2

Do as many of the following six problems as you can. While fully explained answers are requested, partial answers are also welcome.

1. Suppose that $A \subset \mathbb { R }$ is a Lebesgue measurable set such that for all rational numbers r,

$$
A + \tau = A .
$$

Prove that either m $\langle A \rangle = 0$ or m $( \mathbb { R } \backslash A ) = 0 .$

Here m denotes Lebesgue measure, and $A + \tau$ denotes the translate of A :

$$
A + r = \left\{ a + r : a \in A \right\} .
$$

2. Suppose that $\left( \mathbb { X } , \mathcal { B } , \mu \right)$ be a measure space such that $\mu \left( \bar { X } \right) = 1 ,$ and that $\{ B _ { i } \} _ { i = 1 } ^ { N }$ is a sequence of sets in B such that for each $i , \mu \left( \mathcal { B } _ { i } \right) > \frac { 3 } { 2 }$ . Show that there is a set $C \in { \mathfrak { B } }$ of positive measure such that for all $x \in C$

$$
\frac { 1 } { N } \sum _ { i = 1 } ^ { N } \chi _ { \mathcal { B } _ { i } } \left( x \right) > \frac { 1 } { 2 } .
$$

3. Suppose that $A \subset \mathbb { R }$ is a Lebesgue measurable set, and $f : A  \mathbb { R }$ is a measurable function. Suppose further that for all compact sets ${ \mathcal { K } } \subset A$ we have

$$
\int _ { \bar { \kappa } } \ d z f d m = 0 .
$$

(Here m denotes Lebesgue measure). Does it follow that $f \left( x \right) = 0 a . e . ?$

(a) Give an example of a sequence of functions $\left\{ f _ { n } \right\} _ { n = 1 } ^ { \infty }$ on [0, 1] which are Lebesgue integrable, such that $f _ { n } ( x )  0 a . e .$ , but not in L1.

(b) Give an example of a sequence of functions $\{ f _ { \pi } \} _ { \pi = 1 } ^ { \infty }$ on [0, 1] which are Lebesgue integrable, such that $f _ { n } ( x )   { \mathcal { O } } \mathrm { i n } \stackrel { . . . } { L } ^ { 1 }$ but not a.e. .

5. .

(a) Suppose that $F : [ 0 , 1 ] \longrightarrow \mathbb { R }$ is a strictly increasing function. Does this imply that $\because - 1 = 0 \cdot e \cdot ?$

(b) Suppose that $F : [ 0 , 1 ] \to \mathbb { R }$ is a function such that ${ \mathcal { F } } ^ { \prime } \left( x \right) > 0$ a.e. Does this imply that F is strictly increasing?

6. Suppose that $f : \{ 0 , 1 \} \to \mathbb { R }$ is an absolutely continuous function. Prove, directly from the definitions, that f is of bounded variation.

# Complex Analysis Preliminary Examination

August, 2013

## 1 Theorems and Definitions

In the following questions you are asked to give precise definitions or precise statements of theorems. Explain every symbol you use in the context of your statement or definition.

1. Define the winding number of a path γ about a point $z \in \mathbb { C }$

2. Define the multiplicity of an analytic function $f$ at a point $z _ { 0 }$ in the domain $\odot \hat { \mathbb { I } } \ \hat { f } .$

3. State the Theorem of Casorati-Weierstrass.

4. State Cauchy's Integral Formula

5. State the Open Mapping Theorem.

## 2 Problems

Solve at least four of the following problems. Explain your reasoning and show all work.

1. For each of the following functions, find and classify all isolated singularities. In the case of a pole, give the order of the pole and find the residue:

(a) $f ( z ) = { \frac { \cos z - 1 } { z ^ { 2 } } } .$

(b) $f ( z ) = { \frac { e ^ { ( 1 / z ^ { 2 } ) } } { z - 1 } } .$

(c) $f ( z ) = \frac { \sqrt { { \bar { z } } } } { z - 1 } .$

2. Evaluate the following integrals:

(a) $\int _ { | z | = 1 } { \frac { \operatorname { I } \circ \mathrm { g } ( z + e ) } { z ^ { 2 } } } d z .$

(b) $\int _ { | z | = 4 } \left( { \frac { \sin z } { z } } \right) ^ { 1 0 0 } d ,$ z.

(c) If $\gamma = \beta + \left[ 4 \pi , 0 \right]$ , where $\beta ( t ) = t e ^ { i t }$ for $0 \leq t \leq 4 \pi$ , evaluate the integral $\int _ { \gamma } \frac { z ^ { 2 } + 1 } { ( z + 1 ) ( z + 4 ) } d z$

3. Suppose f and g are both analytic in a domain D whose closure is compact $f$ $g$ in $\mathbb { C } ,$ and suppose both functions extend continuously to the boundary. Show that $| f ( z ) | \div | g ( z ) \rrangle$ takes its maximum on the boundary. Hint: Consider $f ( z ) e ^ { i \alpha } \div g ( z ) e ^ { i \beta }$ for appropriate α and $\beta$

4. Let f be an analytic function in a domain D, and suppose that there exists $f$ $\mathcal { D } _ { : }$ a point $z _ { 0 } \in \mathcal { D }$ such that $| f ( z ) | \geq | f ( z _ { 0 } ) | > 0$ for all $z \in \mathcal { D }$ .Show that $f$ must be constant in $\mathcal { D }$

5. Suppose that $\mathcal { D } _ { \bar { 1 } }$ and $\mathcal { D } _ { 2 }$ are simply connected domains in the complex plane whose intersection is non-empty and connected. Prove that $D _ { 1 } \cap \mathcal { D } _ { 2 }$ and $\mathcal { D } _ { 1 } \cup \mathcal { D } _ { 2 }$ are simply connected.

6. Suppose that $f$ is an even analytic function in a disk $\mathcal { B } ( 0 , \tau )$ , centered at $\ell ,$ and of radius r. Show that the function $g : \mathcal { B } ( 0 , \tau ) \to \mathbb { C }$ ,defined by $g ( z ) = f ( \sqrt { z } )$ is a well-defined analytic function. Express $g ^ { ( n ) } ( 0 )$ (the nth derivative of $\mathcal { G } \ \mathrm { a } _ { \mathrm { t } } ^ { \mathrm { ~ t ~ } } \left( \right)$ in terms of the derivatives of $f$

# Real Analysis Preliminary Examination June, 2013

## 1 Part I

1. State the Lebesgue dominated convergence theorem.

2. State the Radon-Nikodym theorem.

## 2 Part 2

Do as many of the following six problems as you can. Partial answers are welcome.

1. Suppose that $( X , { \mathcal { B } } , \mu )$ is a measure space and $\left\{ A _ { n } \right\} _ { n = 1 } ^ { \infty }$ is a sequence of sets in B such that

$$
\sum _ { n = 1 } ^ { \infty } \mu \left( A _ { n } \right) < \infty .
$$

Prove that

$$
\mu \left( \bigcap _ { n = 1 } ^ { \infty } \bigcup _ { k = n } ^ { \infty } A _ { k } \right) = 0 .
$$

2. (a) Give an example of a compact set $K \subset \{ 0 , 1 \}$ which has positive Lebesgue measure, but which contains no non-empty open interval.

(b) Prove that there exists a set $A \subset \{ 0 , 1 \}$ such that for all intervals $I \subset [ 0 , 1 ]$ of positive measure, we have

$$
0 < m \left( A \cap I \right) < m \left( { I } \right) .
$$

(Here m denotes Lebesgue measure). .

3. Suppose that $\mu$ is a finite Borel measure on R such that for some number $t > 0$ and all Borel sets A, $\mu \left( A + t \right) = \mu \left( A \right)$ . (Here $A + t$ denotes $\{ x + t : x \in A \} )$

Prove that $\mu \left( \mathbb { R } \right) = 0$

4. Suppose that $( X , { \mathcal { B } } , \mu )$ is a measure space and that $f$ is a non-negative integrable function on $X$

(a) Prove that, for each $a > 0 , \mu \left\{ x \in X : f \left( x \right) \geq a \right\} < \infty$

(b) Prove that $\left\{ x \in X : f \left( x \right) > 0 \right\}$ is a countable union of sets of finite measure.

5. Suppose that $( X , { \mathcal { B } } , \mu )$ is a measure space such that $\mu \left( X \right) < \infty .$ and let $\{ f _ { n } \} _ { n = 1 } ^ { \infty }$ be a bounded sequence of measurable functions on X. (That is, for some $M > 0 ,$ and for all $\textstyle n \in \mathbb { N }$ and $x \in X , | f _ { \mathit { n } } \left( x \right) | < M \rangle$ . Suppose that $f _ { n } \to 0$ in measure.

Prove that $\int { \left| { f _ { n } } \right| d \mu \to 0 }$

6. Suppose that $g : [ 0 , 1 ] \to \mathbb { R }$ is an absolutely continuous function. Let $E \subset [ 0 , 1 ]$ be a set of Lebesgue measure zero. Prove that $g \left( { \cal E } \right)$ is also a set of Lebesgue measure zero. (Here $g \left( { \cal E } \right)$ denotes $\{ g \left( x \right) : x \stackrel { \cdot } { \in } \dot { E } \} ,$ +:

# Complex Analysis Preliminary Examination

June, 20013 2013

## 1 Theorems and Definitions

In the following questions you are asked to give precise definitions or precise statements of theorems. Explain every symbol you use in the context of your statement or definition.

1. Define what it means for a function $f : U \to V \subset \mathbb { C }$ to be analytic in the open set $U \subset \mathbb { C }$

2. State the Cauchy Integral Formula for an analytic function $f$ in a disk Δ.

3. State the Residue Theorem.

4. Define what an essential singularity is.

5. Define what it means for a sequence of functions $\{ f _ { n } : U \to \mathbb { C } \}$ to converge locally uniformly to a function $f : U \to \mathbb { C }$

## 2 Problems

Solve at least four of the following problems. Explain your reasoning and show all work.

1. For each of the following functions, classify the singularity and find the residue at the given point:

(a) $f ( z ) = z ^ { 2 } e ^ { - 1 / z ^ { 3 } } , z _ { 0 } = 0 .$

(b) $\begin{array} { r } { f ( z ) = \frac { z ^ { 2 } } { ( z - 1 ) ^ { 2 } } , z _ { 0 } = 1 . } \end{array}$

(c) $\begin{array} { r } { f ( z ) = \frac { \sin ( z ^ { 3 } ) } { ( 1 - \cos ( z ) ) ^ { 3 } } , z _ { 0 } = 0 . } \end{array}$

2. Evaluate the following integrals:

(a) $\textstyle \int _ { \{ | z | = 2 \} } { \frac { e ^ { z } } { ( 1 + z ) ^ { 2 } } } d z$

(b) Let $\gamma ( t ) = 2 \cos ( t ) + i \sin ( t )$ for $0 \leq t \leq 2 \pi$ .Evaluate $\textstyle { \int _ { \gamma } { \frac { 1 } { z ^ { 2 } + 2 i z } } d z }$

(c) Suppose that γ is a piecewise smooth closed path in C. Verify that $n ( \overline { { \gamma } } , \overline { { z } } ) = - n ( \gamma , z )$ , where n denotes the winding number, and - is the complex conjugate, in particular, $\overline { \gamma }$ is parameterized via $\overline { { { \gamma } } } ( t ) =$ $\overline { { \gamma ( t ) } }$

3. Let $f$ be an entire function such that the function $g ( z ) = { \sqrt { f ( z ) } }$ is also an entire function. Prove that $f$ must be constant.

4. Let $\{ f _ { n } \}$ and $\left\{ g _ { n } \right\}$ be sequences of functions that converge uniformly on a set A. Under the assumption that $\{ f _ { n } \}$ and $\left\{ g _ { n } \right\}$ are uniformly bounded $( \mathrm { i . e . }$ there exists a constant M such that $| f _ { n } ( z ) | \leq M$ and $| g _ { n } ( z ) | \leq M$ for all $z \in A$ and all $\boldsymbol { \mathscr { n } } \in \mathbb { N } )$ , prove that the sequence $\{ f _ { n } g _ { n } \}$ converges uniformly on A.

5. Let $\mathcal { D } \subset \mathbb { C }$ be a domain and let $f , g : { \mathcal { D } }  \mathbb { C }$ be analytic. Let $z _ { 0 } \in \mathcal { D }$ and suppose that $f ( z _ { 0 } ) = g ( z _ { 0 } ) = 0$ Suppose furthermore that

$$
\operatorname* { l i m } _ { z \to z _ { 0 } } { \frac { f ^ { \prime } ( z ) } { g ^ { \prime } ( z ) } } \in \mathbb { C }
$$

exists. Show that

$$
\operatorname* { l i m } _ { z \to z _ { 0 } } { \frac { f ( z ) } { g ( z ) } }
$$

exists as well and calculate this limit.

6. Let $f$ be a non-constant entire function. Prove that $f ( \mathbb { C } )$ is dense in $\mathbb { C } .$

7. Let $f$ have a pole of order m at a point $z _ { 0 } ,$ and let g have a pole of order n at the same point. Show that $f + g$ has either a removable singularity at $z _ { 0 }$ or a pole of order not greater than $\mathtt { m a x } \{ m , n \}$ at $z _ { 0 }$

# COMPLEX ANALYSIS PRELIM, 2012

## 1 Basic facts:

Please give brief but informative responses here, not overly long reproductions of a textbook's presentation.

(2 points each)

1. Give three equivalent characterizations of analyticity of a complex-valued function f in a domain ${ \mathcal { D } } \subset \mathbb { C } .$ That is, complete the sentence: "f is analytic in D if and only $\operatorname { i f . . . } "$ in three ways.

2. State a version of a theorem that expresses the $m ^ { t h }$ derivative of an analytic function as an integral.

3. State what is meant by the following terms:

(a) a pole of an analytic function.

(b) the order of a pole of an analytic function

(c) an essential singularity of an analytic function

4. State the Casorati-Weierstrass theorem about the behavior of an analytic function in the vicinity of an essential singularity.

5. State the Residue Theorem.

6. State the Argument Principle.

## 2 Problems:

Please do as many of the following problems as you can. (We recognize that time constraints make it unreasonable to expect you to do all of them!) Partial answers will be awarded partial credit.

(12 points each)

$$
{ 1 . }
$$

(a) Prove that if A and B are the radii of convergence of $\scriptstyle \sum _ { k = 0 } ^ { \infty } a _ { k } z ^ { k }$ and $\scriptstyle \sum _ { k = 0 } ^ { \infty } b _ { k } z ^ { k }$ , respectively, then the radius of convergence of $\scriptstyle \sum _ { k = 0 } ^ { \infty } a _ { k } b _ { k } z ^ { k }$ is at least AB.

(b) Given an example of two series for which the above inequality is strict.

2. .

(a) Find all possible values of the integral

$$
\int _ { \gamma } \frac { 1 } { z ^ { 2 } + 1 } d z
$$

where γ is a closed, smooth curve in $\mathbb { C } \backslash \{ i , - i \}$

(b) Answer the above question under the assumption that γ is a positively oriented circle in $\mathbb { C } \backslash \{ i , - i \}$ , and describe which circles give which of the possible values.

3. Let D be a domain in C with $0 \in D .$ and let $f$ and $g$ be analytic functions in D. Suppose that $f$ and g both have zeros of order 7 at $z = 0$ . Prove that the limits lim $_ { \cdot z \longrightarrow 0 } \frac { f ^ { \prime } ( z ) } { g ^ { \prime } ( z ) }$ and lim $_ { \cdot z \longrightarrow 0 } \frac { f ( z ) } { g ( z ) }$ both exist and are equal.

4. Suppose that $f$ is an entire function and that there is some $M > 0$ and some integer $n ,$ so that for all $z \in \mathbb { C }$

$$
| f \left( z \right) | \leq M \left| z \right| ^ { n }
$$

Prove that $f$ is a polynomial of degree less than or equal to n.

5. Suppose that D is a bounded domain in $C$ and $\left\{ f _ { n } \right\} _ { n = 1 } ^ { \infty }$ is a sequence of analytic functions in ${ \mathcal { D } } ,$ each of which extends to a continuous function on the closure of D. Suppose further that the sequence $\{ f _ { \pi } \}$ converges uniformly on the boundary of D. Prove that the sequence converges uniformly on $\mathcal { D }$

## REAL ANALYSIS PRELIM 2012

1. Let X be a nonempty set, and let A be a collection of subsets of X.

a. Define what it means for A to be a σ-algebra.

b. Using your definition, prove that if A and B are sets belonging to ${ \mathcal { A } } ,$ then the set $A \cap B$ also belongs to ${ \mathcal { A } } .$

c. Give an example of a nonempty set $X ,$ ,a ring $\mathcal { R }$ on $X ,$ and a sequence $A _ { 1 } , A _ { 2 } , \cdots$ of subsets of $X ,$ each belonging to ${ \mathcal { R } } ,$ such that the subset

$$
\bigcap _ { n = 1 } ^ { \infty } A _ { n }
$$

does not belong to ${ \mathcal { R } } .$

d. If X contains exactly four points, determine the possible cardinalities for σ-algebras on $X ,$ and for each cardinality, the number of different σ-algebras having that cardinality.

2. Let $X = \{ 0 , 1 \}$ , let A be the σ-algebra of Borel subsets of X, and denote by λ Lebesgue measure on $( X , A )$

a. Calculate the measure λ of each of the following sets. (You do not need to show that the sets are measurable, but you should give brief reasons for your answers, not simply numbers.)

i) $\textstyle \left\{ x \in X : \sin { \frac { 2 \pi } { x } } \leq 0 \right\}$

ii) $\{ x \in X : x = 0 . x _ { 1 } x _ { 2 } \cdot \cdot \cdot .$ no digit $x _ { i } = 7 \}$

ii $\{ x \in X : x = 0 . x _ { 1 } x _ { 2 } \cdot \cdot \cdot$ , for some $i > 0 , x _ { i } = 5$ and $x _ { i + 1 } = 6 \}$

b. State without proof the dominated convergence theorem for the measure space $( X , A , \lambda )$

c. Define the function f on X as follows: if $x \neq 0 ,$ determine a positive integer $n = n ( x )$ for which the distance $d ( x )$ from x to the point $1 / n$ is minimal, and then set $f ( x )$ equal to $d ( x )$ if n is odd; otherwise set $f ( x )$ equal to zero. Explain why f is integrable (you may use theorems you recall from the course) and calculate

$$
\int f d \lambda .
$$

Please turn the page for problems 3 and 4.

3. Let V be a real vector space.

a. Give the definition of a norm on V.

b. Recall that a norm on V can be used to define a metric on $V ;$ give the corresponding formula for this metric, and prove that it is a metric, using your definition in the previous question.

c. Say what it means for V, under this norm, to be a Banach space.

d. Give an example of a normed vector space which is not a Banach space.

e. State without proof the Uniform Boundedness Theorem, also known as the Banach-Steinhaus theorem, taking care to state the correct hypotheses on the vector spaces and mappings occurring in the theorem.

f. If X and Y are real Banach spaces, and if $T : X  Y$ is a continuous one-to-one linear mapping from X onto Y, prove the the inverse $T ^ { - 1 }$ is a linear mapping. Is in this situation this inverse mapping continuous? Please give a reason for your answer.

4. Which theorem or theorems of this course seem to be the most interesting ones, and why?

GOOD LUCK!

# Complex Analysis Prelim, Summer 2011

1. The real and imaginary parts of the square roots of $2 - 4 i$ can be given by simple algebraic expressions involving only square roots, sums, and differences of real numbers. Give these two expressions for each of the square roots.

2. Represent $\frac { z ^ { 4 } } { z ^ { 3 } - 1 }$ by partial fractions.

3. If $f$ is holomorphic on the open unit disk, then show that g is also, where $g ( z ) : = { \overline { { f ( { \overline { { z } } } ) } } }$

4. Prove that if A and B are the respective radii of convergence of the power series $\sum a _ { n } z ^ { n }$ and $\sum b _ { n } z ^ { n }$ , then the radius of convergence of $\sum a _ { n } b _ { n } z ^ { n }$ is at least AB.

5. Give the definition of $\textstyle \int _ { \gamma } f ( z ) d z$ , if γ is a differentiable curve in the complex plane and $f$ is a continuous function defined on the trace of $\gamma$ .

6. State Cauchy's theorem for triangles.

7. Using the simplest form of Cauchy's estimates, prove that a bounded entire function is constant.

8. How many roots of $z ^ { 4 } - 6 z + 3 = 0$ have absolute values between 1 and 2?

9. A form of the lemma of Schwarz states that if f is holomorphic on the open unit disk, and if $f ( 0 ) = 0$ and $| f ( z ) | \leq 1$ whenever $| z | \leq 1$ , then $| f ( z ) | \leq | z |$ whenever $| z | \leq 1$ . Give a proof using the maximum principle.

10. Evaluate $\textstyle \int _ { 0 } ^ { \infty } { \frac { x ^ { 2 } d x } { x ^ { 4 } + 5 x ^ { 2 } + 6 } }$ using the residue theorem.

Complete as many exercises as possible in the allotted time. Good luck!

## Real Analysis Prelim, Summer, 2011

Solve as many of the following eight problems as you can. Partial answers are welcome. (Note: the rather large number of questions is intended to give you more choices, not to induce despair!)

Throughout this part of the exam, Lebesgue measure is understood whenever the symbol $" m "$ is used.

1. Prove or give a counterexample to the following assertion: If $\left\{ f _ { n } \right\} _ { n = 1 } ^ { \infty }$ is a sequence of continuous functions on [0, 1] with $0 \leq f _ { n } \left( x \right) \leq \bar { 1 }$ for all n and ${ \boldsymbol { \mathfrak { x } } } ,$ and if $\begin{array} { r } { \operatorname* { l i m } _ { n \to \infty } \int _ { [ 0 , 1 ] } f d m = 0 } \end{array}$ ,then $f _ { \pi } ( x )  0$ almost everywhere.

2. Let $f : \mathbb { R } \to \mathbb { R }$ be a Lebesgue measurable function. Does either of the following two conditions on f imply the other?

Condition i : For almost every $x \in \mathbb { R } , f$ is continuous at x.

Condition ii : There is a continuous function $g : \mathbb { R } \to$ R such that $f \left( x \right) =$ g (x) almost everywhere.

Give proofs or counterexamples, as appropriate.

(The term "almost everywhere" refers to Lebesgue measure here.)

3..

a)Suppose $A \subset \mathbb { R } .$ Define the Lebesgue outer measure $m _ { * } \left( A \right)$ of A.

Prove that for all seqeunces $\{ A _ { i } \} _ { i = 1 } ^ { \infty }$ of subsets of R,

$$
m _ { * } \left( \bigcup _ { i = 1 } ^ { \infty } A _ { i } \right) \leq \sum _ { i = 1 } ^ { \infty } m _ { * } \left( A _ { i } \right)
$$

4. Suppose that $\mu$ is a finite Borel measure on $\mathbb { R } ^ { d }$ For each $t ~ > ~ 0$ let $S _ { t } = \left\{ x \in \mathbb { R } ^ { d } : | x | = t \right\}$ . Let $A = \{ t \in \mathbb { R } : \mu ( S _ { t } ) > 0 \}$ . Prove that A is at most) countable.

# Real Analysis section of the Analysis Qualifier Wesleyan University July 14, 2010

## 1 Part I

Give precise statements of the following.

1. The monotone convergence theorem.

2. The Radon-Nikodym theorem

3. The spectral theorem for compact symmetric operators on a Hilbert space.

## 2 Part ⅡI

Do as many of these six problems as you can. Partial answers are welcome.

1. Suppose that $( X , { \mathcal { B } } , \mu )$ is a measure space and that $\left\{ A _ { n } \right\} _ { n = 1 } ^ { \infty }$ is a sequence of measurable sets in $( X , B , \mu )$

(a)Prove that, if $\textstyle \sum _ { n = 1 } ^ { \infty } \mu \left( A _ { n } \right) < \infty$ then the set of x which are elements of infinitely many of the sets $A _ { n }$ has measure zero.

(b) Give an example of a measure space $( X , B , \mu )$ and a sequence of measurable sets $\left\{ A _ { n } \right\} _ { n = 1 } ^ { \infty }$ in $( X , B , \mu )$ such that $\textstyle \sum _ { n = 1 } ^ { \infty } \mu \left( A _ { n } \right) = \infty$ but still the set of x which are elements of infinitely many of the sets $A _ { n }$ has measure zero.

2. Suppose that μ is a Borel measure on R with the properties:

i. For every bounded interval $I , \mu ( I ) < \infty$ and ii. For every rational number r and every bounded interval $I , \mu ( I ) =$ $\mu \left( I + r \right)$ . (The notation $I + r$ means $\{ x + r \mid x \in I \} )$

)Prove that for every $x \in \mathbb { R } , \mu \left( \{ x \} \right) = 0$

(b) Prove that for every bounded interval I and every real number $^ { a , }$ $\mu \left( I \right) = \mu \left( I + a \right)$

3. .

(a) Give an example of a closed subset A of $\mathbb { R } ^ { 2 }$ with positive Lebesgue measure and which contains no non-empty open set.

(b) Suppose that A is a set of the sort described in part ${ \bf a } ,$ and that $f : \mathbb { R } ^ { 2 } \to \mathbb { R }$ is a function such that $f \left( x \right) = \chi _ { A } \left( x \right)$ a.e. Prove that there is a set of positive measure B such that for all $x \in { \mathcal { B } } , f$ is discontinuous at x.

a)Suppose that $f : \mathbb { R } \to \mathbb { R }$ is a Lebesgue integrable function. For each $t \in \mathbb { R }$ let $\pmb { f } _ { t }$ denote the translate of f by t. That is, ${ \dot { f } } _ { t } \left( x \right) = f \left( x - t \right)$ Prove that

$$
\operatorname* { l i m } _ { t  0 } \int _ { \mathbb { R } }  f - f _ { t }  d m = 0
$$

(Here m denotes Lebesgue measure).

(b) Suppose that $A \subset \mathbb { R }$ is a set of finite Lebesgue measure. What does the above result say about the sets $A + t ?$ (As before, $A + t =$ $\{ x + t \mid x \in A \} . )$

5. Let $( X , B , \mu )$ be a measure space.

a)Show that if $\mu \left( X \right) < \infty$ ,then $L ^ { 4 } \left( X , B , \mu \right) \subset L ^ { 3 } \left( X , B , \mu \right)$

(b) Give an example of a measure space $( X , B , \mu )$ and a function  such that $f \in { \cal L } ^ { 4 } \left( X , B , \mu \right)$ but $f \notin L ^ { 3 } \left( X , { \dot { \mathcal { B } } } , \mu \right)$

6. Let ${ \cal H } = { \cal L } ^ { 2 } \left( \left[ 0 , 1 \right] , m \right)$ , where m denotes Lebesgue measure. We know that H is a Hilbert space, with respect to the inner product $\begin{array} { r } { ( f , g ) = \int f \overline { { g } } d m . } \end{array}$

(a) Let E be a Lebesgue measurable subset of [0, 1]. Verify that $H _ { E } =$ $\{ f \in H \mid f = f \chi _ { E } \}$ is a closed subspace of H.

(b) Identify the orthogonal complement of $H _ { E }$ in H.

c) Let $P : H  H$ be the linear map given by $P \left( f \right) = f \chi _ { E }$ Calculate the norm of P.

# Complex Analysis Preliminary Examination

July 14, 2010

## 1 Theorems and Definitions

Answer at least four of the following questions. You are asked to give precise definitions or precise statements of theorems. Explain every symbol you use in the context of your statement or definition.

1. Let $\boldsymbol { U } \subset \mathbb { C }$ be an open set, and let $\{ f _ { n } : U \to \mathbb { C } \}$ be a sequence of functions. Define what it means for the sequence $\{ f _ { n } \}$ to converge locally uniformly in U.

2. Let f be analytic and non-constant in some disk $B ( z _ { 0 } , r ) \subset \mathbb { C }$ Define the multiplicity of f at zo.

3. State the Theorem of Casorati-Weierstrass.

4. Define the winding number of a piecewise smooth closed path γ in C around a point zo.

5. State Morera's Theorem.

6. Let D be a domain in the complex plane with $0 \notin D$ . Define a branch of the logarithm function in D.

## 2 Problems

Solve at least four of the following problems. Explain your reasoning and show all work..

1. Calculate:

$$
\operatorname* { l i m } _ { r \to \infty } \int _ { \gamma _ { r } } \frac { e ^ { i z } } { z ^ { 2 } } d z ,
$$

where for each r the path $\gamma _ { r }$ is defined by $\gamma _ { r } ( t ) = r e ^ { i t } , \ t \in [ 0 , \pi ]$

2. This question has two parts.

(a) Suppose that f has a simple pole at $z = a$ and let g be analytic in an open set containing a. Show that $\operatorname { R e s } ( a , f \cdot g ) = g ( a ) \operatorname { R e s } ( a , f )$

(b) Use part (a) to show that if D is a domain and f is analytic in D except for simple poles at $a _ { 1 } , \ldots , a _ { n } ;$ and if g is analytic in D then

$$
{ \frac { 1 } { 2 \pi i } } \int _ { \gamma } f ( z ) g ( z ) d z = \sum _ { k = 1 } ^ { n } { n ( \gamma , a _ { k } ) g ( a _ { k } ) R e s ( a _ { k } , f ) }
$$

for any piecewise smooth closed path γ not passing through $a , \ldots , a _ { n }$ and such that $\gamma \sim 0$ in D.

3Find all possible values of

$$
\int _ { \gamma } \frac { d z } { 1 + z ^ { 2 } } ,
$$

where γ is any closed piecewise smooth path in C not passing through ±i.

4. Let D be a domain in $\mathbb { C } ,$ and let $f : D  \mathbb { C }$ be analytic and injective. Show that $f ^ { \prime } ( z ) \neq 0$ for all $z \in D$

5. Let $R > 0$ and $D = \{ z : | z | > R \}$ . An analytic function $f : D \to \mathbb { C }$ has a removable singularity, a pole, or an essential singularity at infinity if the function $\begin{array} { r } { g ( w ) = f ( \frac { 1 } { w } ) } \end{array}$ has, respectively, a removable singularity, a pole, or an essential singularity at $w = 0$ . If f has a pole at ∞ then the order of the pole is the order of the pole of g at $w = 0$

(a) Prove that an entire function has a removable singularity at infinity if and only if it is constant.

(b) Prove that an entire function has a pole at infinity of order m if and only if it is a polynomial of degree m.

6. Suppose that a function f is analytic in the unit disk $B ( 0 , 1 )$ , that $f ( 0 ) =$ $f ^ { \prime } ( 0 ) = 0$ ,and that $| f ^ { \prime } ( z ) | \leq 1$ for every $z \in B ( 0 , 1 )$ . Prove that $| f ( z ) | \leq$ $| z | ^ { 2 } / 2$ for every $z \in B ( 0 , 1 ) .$ For which functions f can equality hold in this estimate at some $z \neq 0 ?$

Name:

# COMPLEX ANALYSIS SECTION OF THE ANALYSIS QUALIFIER WESLEYAN UNIVERSITY June 11 2009

## INSTRUCTIONS

1. This exam consists of three sections. Answer all of the questions (short answer/definition) in the first section and the second section. Choose one question to answer from the third section.

2. Clearly indicate the work you wish to be graded. Show all work. Good luck on the exam.

## 1. DEFinITiOn/ SHorT AnSWER

INSTRUCTIONS: Answer all of the following questions.   
Be as complete as possible, and show all relevant work.

(1) Assume $U \subseteq \mathbb { C }$ is open. Define what it means for $f : U \to \mathbb { C }$ to be analytic.

(2) Let U be a set in the complex plane, and let

$$
\alpha _ { i } : [ 0 , 1 ]  U , i = 1 , 2
$$

be a pair of closed paths in U. Define what it means for $\alpha _ { 1 }$ to be freely homotopic to $\alpha _ { 2 }$

(3) State Liouville's Theorem.

(4) True or False: Satisfying the Cauchy-Riemann equations in a domain D is sufficient to observe that a function $f$ is analytic on D.

) Let $U \subseteq \mathbb { C }$ be an open set in the domain of some complex-valued function $f .$ Define what it means for $\boldsymbol { F }$ to be a primitive of $f$

(6) Let U be an open set in $\mathbb { C } ,$ and let $\sigma$ be a cycle in $U$ Define what it means for $\sigma$ to be homologous to zero in $U$(7) Let $D \subset \mathbb { C }$ be the bounded region inside

$$
\{ z \in \mathbb { C } : | R e ( z ) | + | I m ( z ) | = 1 \} ,
$$

where $z = x + i y$

Orient ∂D positively. Calculate

$$
\int _ { \partial D } \frac { \cos z } { z ( z ^ { 2 } + 8 ) } d z .
$$

Show all work.

## 2. SECTION II

INSTRUCTIONS: Answer both of the following questions. Be as complete as possible, e.g. fully quote statements that you are using, and show all relevant work.

(1) Let f be an entire function that satisfies the estimate $| f ( z ) | \leq m e ^ { \alpha x }$ , where m and a are positive constants and $z = x + i y$

Show $f ( z ) = A e ^ { \alpha z }$ for some complex constant A.

(2) Let D be a domain in C. If every function f which is analytic in D possesses a primitive in D, show that D is simply connected. (Recall that a domain is simply connected if every cycle in the domain is homologous to zero.)

## 3. SECTION III

INSTRUCTIONS: Answer one of the following questions. Be as complete as possible, and show all relevant work. Clearly indicate the problem which is to be graded.

(1) Let $f ( z )$ be analytic in the disk $\Delta = \Delta ( z _ { 0 } , r ) ( r > 0 )$ and assume that there exists $\mathrm { ~ a ~ } m > 0$ so that

$$
| f ( z ) - f ( z _ { 0 } ) | \leq m
$$

for all $z \in \Delta$

Prove: The derivative $f ^ { \prime }$ satisfies the inequality

$$
\left| f ^ { \prime } ( z _ { 0 } ) \right| \leq { \frac { m } { r } }
$$

on $\Delta .$

) Let $u : D \to \mathbb { R }$ , where $D$ is a domain. The function $u \in C ^ { 2 } ( D )$ is harmonic if

$$
u _ { x x } + u _ { y y } = 0 .
$$

A function $v : D \to \mathbb { R }$ is a harmonic conjugate to u if the function $f = u + i v$ is analytic in $D$

Prove: If D is simply connected then u possesses a harmonic conjugate. (Hint: consider the function $g = u _ { x } - i u _ { y } . )$

# Real Analysis Prelim, Summer 2009

1.Give a definition of each of the following:

Dynkin system (in Ω)

outer measure (in Ω)

set which is µ\*-measurable $( \mathrm { i } \mathrm { f } \ \mu ^ { * }$ is an outer measure in Ω)

integrable function (on the measure space $( \Omega , A , \mu )$

absolute continuity of $\mu$ with respect to v

normed vector space

2. State the following theorems:

Uniqueness Theorem (concerning premeasures on rings)

Dominated Convergence Theorem (due to H. Lebesgue)

Hahn-Banach Theorem

Uniform Boundedness Theorem (aka Banach-Steinhaus Theorem)

3. Provide solutions to the following:

a. If $( \Omega , A , \mu )$ is a measure space with $\mu ( \Omega ) ~ \dot { < }$ ∞ and if $A _ { 1 } , A _ { 2 } , \cdots \in { \mathcal { A } } ,$ prove that lim sup $\mu ( A _ { n } ) \leq \mu ( \operatorname* { l i m } \operatorname* { s u p } A _ { n } )$

b. It is always true that a content on a ring is subadditive. Assuming this without proof, show that a premeasure is always σ-subadditive.

c. Prove that if $f : X \to Y$ is a bijective (i.e. one-to-one and onto) linear mapping from the vector space $X$ to the vector space $\boldsymbol { Y } ,$ then the inverse $f ^ { - 1 }$ of $f$ is also linear. Using a course theorem, if furthermore the spaces are normed and $f$ is continuous, when can one say that the inverse is continuous? Explain why.

d. In the Banach space of continuous real-valued functions on $[ - 1 , 2 ]$ provided with their uniform norms, determine the uniform closure of the set of functions

$$
\{ f ( x ) = \sum _ { n = 0 } ^ { N } a _ { n } x ^ { 3 n + 1 } : N \geq 0 , a _ { n } \ \mathrm { r e a l } \ \mathrm { f o r } \ 0 \leq n \leq N \} .
$$

# Complex Analysis Preliminary Examination

July 8, 2008

## 1 Theorems and Definitions

In the following questions you are asked to give precise definitions or precise statements of theorems. Explain every symbol you use in the context of your statement.

1. Let $f : U \to \mathbb { C }$ be a function that is analytic at a point $z _ { 0 } \in U$ . Define the multiplicity of f at $z _ { 0 }$

2. Let $\{ f _ { n } \}$ be a sequence of complex-valued functions defined on an open set $\displaystyle U \subset \mathbb { C } ,$ and let $f : U \to \mathbb { C } .$ Define what it means for $\{ f _ { n } \}$ to converge normally (or locally uniformly) to $f$

3. State Liouville's Theorem.

4. State the Cauchy Integral Formula in an open disk.

## 2 Problems

Solve at least four of the following problems. Explain your reasoning and show all work.

1. Find the Laurent series for $f ( z ) = { \frac { 1 } { ( z ^ { 2 } + 1 ) ^ { 2 } } }$ , centered at i. Where does this series converge?

2. Calculate the following integrals:

(a)

$$
\int _ { | z - i | = 1 0 } \left( z + { \frac { 1 } { z } } \right) ^ { 4 } d z
$$

(b)

$$
\int _ { | z | = 4 } \left( { \frac { \sin z } { z } } \right) ^ { 1 0 0 } d z
$$

3. Let $f$ be an analytic function in a domain $D ,$ and suppose that there exists a point $z _ { 0 } \in D$ such that $| f ( z ) | \geq | f ( z _ { 0 } ) | > 0$ for all $z \in D$ . Show that $f$ must be constant in $D$

4. Let $D \subset \mathbb { C }$ be a domain and let $f , g : D \to \mathbb { C }$ be analytic. Let ${ \bf \chi } _ { z _ { 0 } } \in D$ and suppose that $f ( z _ { 0 } ) = g ( z _ { 0 } ) = 0$ Suppose furthermore that

$$
\operatorname* { l i m } _ { z \to z _ { 0 } } { \frac { f ^ { \prime } ( z ) } { g ^ { \prime } ( z ) } } \in \mathbb { C }
$$

exists. Show that

$$
\operatorname* { l i m } _ { z \to z _ { 0 } } { \frac { f ( z ) } { g ( z ) } }
$$

exists as well and calculate this limit.

5. Let $U \subset \mathbb { C }$ be open, and let $f , g : U \to \mathbb { C }$ be analytic except for isolated singularities. Let $z _ { 0 } \in U$

(a) Is the residue of $f + g$ at zo equal to the residue of f plus the residue of $g \smash { ? }$

Is the residue of $f g$ at zo equal to the product of the residues of $f$ and $g \smash { ? }$

# Real Analysis Preliminary Examination July 8, 2008

## 1 Theorems and Definitions

Give precise statements of the following.

1. The definition of the Borel sets in the real line.

2. The definition of Lebesgue outer measure on the real line.

3. The Lebesgue monotone convergence theorem (in an abstract measure space).

4. The Hölder inequality (which concerns functions in certain $L ^ { p }$ spaces).

5. The Radon-Nikodym theorem.

## 2 Problems

1. Let $( X , B , \mu )$ be a measure space and $f : X \to \mathbb { R }$ an integrable function. Prove that $\{ x \in X \mid f \left( x \right) \neq 0 \}$ is a countable union of sets of finite measure.

2. (a) Give an example of a sequence of real valued Lebesgue measurable functions on [0, 1] that converges to 0 almost everywhere (with respect to Lebesgue measure), but does not converge in $L ^ { 1 }$

(b) Give an example of a sequence of real valued Lebesgue integrable functions on [0, 1] that converges to 0 in $L ^ { 1 }$ (with respect to Lebesgue measure), but not.almost everywhere.

3. Suppose that $A \subset \mathbb { R }$ is a Lebesgue measurable set with the property that for every rational number $\boldsymbol { r } ,$

$$
A + r = A
$$

(The notation means: $A + r = \{ a + r \mid a \in A \} )$ . Prove that either $\lambda \left( A \right) =$ 0 or $\lambda \left( \mathbb { R } \backslash A \right) = 0$ , where λ denotes Lebesgue measure.

4. Suppose that $( X , B , \mu )$ is a measure space with $\mu \left( X \right) < \infty$ Show that a sequence $\{ f _ { n } \} _ { n = 1 } ^ { \infty }$ of measurable functions on X converges to O in measure if and only if

$$
\operatorname* { l i m } _ { n \to \infty } \int _ { X } { \frac { | f | } { 1 + | f | } } d \mu = 0 .
$$

5. Suppose that $( V , \parallel )$ is a real normed vector space in which every absolutely convergent series converges. Prove that $( { \vec { V } } , \parallel \parallel )$ is complete with respect to its norm.