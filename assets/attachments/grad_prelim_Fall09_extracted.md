# MATHEMATICS PRELIMINARY EXAM: FALL, 2009

(1) Consider the following statement about real numbers, which we will call S:

“If $x \leq 0$ and $x < y$ , then there is a z such that $x \cdot z > y . ^ { \mathfrak { \prime } }$

(This happens to be false, but for the purposes of this problem, that is irrelevant.)

(a) Using the symbols ∀ (for all), ∃ (there exists), $\lor ( \mathrm { o r } ) , \land ( \mathrm { a n d } ) ,  ( \mathrm { i m p l i e s } ) , \lnot$ (not), write S in symbolic form. Be sure to include implicit quantifiers.

(b) Write the negation of S, in verbal form, without using word “not”.

(c) Write the converse of $S$ , in verbal form.

(2) Work the following Calculus problems.

(a) Compute the derivative of $f ( x ) = \sec ^ { 3 } ( \tan ( x ^ { 2 } ) )$

(b) Find the Taylor expansion for $f ( x ) = \ln ( x )$ about $a = 2$ , and determine its radius of convergence.

(c) Set up (but do not evaluate) the triple integral needed to compute the integral of $f ( x , y , z ) = x \sin ( y + z )$ over the region R in $\mathbb { R } ^ { 3 }$ defined by $x \ge 0 , y + z \ge 2$ and $x ^ { 2 } + y ^ { 2 } + z ^ { 2 } \leq 4 .$

(3) Fix $x \in \mathbb { R }$ . Using mathematical induction, show that if $x \neq 1$ , then for $n = 1 , 2 , 3 , \cdots$ we have

$$
x + 2 x ^ { 2 } + 3 x ^ { 3 } + \cdot \cdot \cdot + n x ^ { n } \ = \ { \frac { n x ^ { n + 2 } - ( n + 1 ) x ^ { n + 1 } + x } { ( x - 1 ) ^ { 2 } } } \ .
$$

(4) Let $f : \mathbb { R } \to \mathbb { R }$ be a function. Give the $\varepsilon { - } \delta$ definition for the existence of the limit of $f ( x )$ as x approaches $x _ { 0 }$ . Then, use the definition to show that $\operatorname* { l i m } _ { x \to 1 / 4 } { \sqrt { x } } = 1 / 2$

(5) (a) Show that for the function $f : \mathbb { R } ^ { 2 } \to \mathbb { R }$ defined by

$$
f ( x , y ) ~ = ~ \left\{ \begin{array} { c l } { { \frac { x y } { x ^ { 2 } + y ^ { 2 } } } } & { { \mathrm { i f ~ } ( x , y ) \neq ( 0 , 0 ) ~ , } } \\ { { 0 } } & { { \mathrm { i f ~ } ( x , y ) = ( 0 , 0 ) ~ , } } \end{array} \right.
$$

we have $\begin{array} { r } { \frac { \partial f } { \partial x } ( 0 , 0 ) = \frac { \partial f } { \partial y } ( 0 , 0 ) = 0 } \end{array}$ , but that f is not even continuous at $( 0 , 0 )$

(b) Give a sufficient condition for a function $g : \mathbb { R } ^ { 2 }  \mathbb { R }$ to be differentiable at a point P , in terms of the partial derivatives of $g .$

(6) Let V be the vector space over R spanned by the functions $f _ { 1 } ( x ) = 1 , f _ { 2 } ( x ) = \sin ^ { 2 } ( x )$ 2 $f _ { 3 } ( x ) = \cos ^ { 2 } ( x ) , f _ { 4 } ( x ) = \sin ( 2 x )$ , and $f _ { 5 } ( x ) = \cos ( 2 x )$ . Find the dimension $\dim _ { \mathbb { R } } ( V )$ and exhibit a basis for V over R.

(7) If A is the $2 \times 2$ complex matrix below, find an invertible complex matrix P for which $P ^ { - 1 } A P$ is diagonal.

$$
A \ = \ \left[ { \begin{array} { c c } { 2 } & { 1 + i } \\ { 1 - i } & { 3 } \end{array} } \right]
$$

(8) Let $f : [ 0 , 1 ] \to \mathbb { R }$ and $f _ { n } : [ 0 , 1 ] \to \mathbb { R } .$ for $n = 1 , 2 , 3 , \cdots$ , be continuous functions. Suppose the sequence $\{ f _ { n } \} _ { n \geq 1 }$ converges uniformly to $f .$ Give the definition of uniform convergence, and using basic properties of integrals and absolute values, show that

$$
\operatorname* { l i m } _ { n \to \infty } \int _ { 0 } ^ { 1 } f _ { n } ( x ) d x \ = \ \int _ { 0 } ^ { 1 } f ( x ) d x \ .
$$