160 points Complex Analysis Qualifying Examination May 20, 2009 JUSTIFY EVERYTHING BY CITATION OR PROOF.

(20) 1. Suppose $f ( z ) = \sum \limits _ { n = 0 } ^ { N } a _ { n } z ^ { n }$ where the $a _ { n } \in \mathbb { C }$ and $\aleph > 0$ , Let M be the maximum of $| f ( z ) |$ on the unit circle about the origin. i) (5) Show that $| a _ { 0 } | \leq M$ ii)(15) Show that $| \mathsf { a } _ { \mathsf { N } } | \leq \mathsf { M }$

(20) 2. Find the number of zeros of $f ( z ) = z ^ { 7 } + 2 z ^ { 3 } + 4$ in the interior of the first quadrant (all z = x + iy with x and y positive).

(20) 3. Suppose that α, B, y, δ are distinct complex numbers. Show that $\frac { \alpha } { ( \alpha - \beta ) ( \alpha - \gamma ) ( \alpha - \delta ) } ~ + ~ \frac { \beta } { ( \beta - \alpha ) ( \beta - \gamma ) ( \beta - \delta ) } ~ + ~ \frac { \beta } { ( \beta - \alpha ) ( \beta - \gamma ) ( \beta - \delta ) } ~ + ~ \frac { \beta } { \gamma } ~ \frac { \delta } { \delta } ~ + ~ \mathcal { O } \left( ( \beta - \gamma ) ( \beta - \delta ) \right)$ $\frac { x } { ( \gamma - \alpha ) ( \gamma - \beta ) ( \gamma - \delta ) } + \frac { \delta } { ( \delta - \alpha ) ( \delta - \beta ) ( \delta - \gamma ) } = 0 .$ (Hint: this is not an algebra qual.)

(20) 4. Suppose f(z) is analytic at z=0. Prove that there is an integer n >0 such that $\left| \textsf { f } ^ { ( \bar { \boldsymbol { \mathsf { n } } } ) } ( \bar { \boldsymbol { \mathsf { n } } } ) \right| \ \leqslant \ \mathsf { n } ^ { \bar { \boldsymbol { \mathsf { n } } } } \cdot \mathsf { n } \downarrow .$

(20) 5. Suppose $\left\{ \mathfrak { f } _ { \mathfrak { n } } ( z ) \right\} _ { \mathfrak { n } \mathfrak { L } 1 }$ is a sequence of analytic functions on a reqion A which converges uniformiy on A to a function f(z). Show that f(z) is analytic on A and that the sequence of derivatives $\left\{ f _ { \mathfrak { n } } ^ { \prime } ( z ) \right\} _ { \mathfrak { n } \mathfrak { z } 1 }$ converges uniformly to $\mathbf { f } ^ { \prime } ( z )$ on compact subsets of A.

(20) 6. State and prove the Weierstrass Product Theorem. You may use any general convergence criteria without proof, but you should state what these criteria are.

(20) 7. Evaluate $\int \limits _ { - \infty } ^ { \infty } ( \frac { \sin ( x ) } { x } ) ^ { 2 } e ^ { i t x }$ dx for all real t20.

(20) 8. Find all entire functions f(z) with the property that for all z, $| \textsf { f } ( z ) | \ s \ \textsf { e } ^ { \texttt { X Y } } ,$ (Here, x and y are the real and imaginary parts of z.)