# APPLIED ALGEBRA QUALIFIER

## PARTI

## REPRESENTATION THEORY

100 points gets vou full credit more would be heavenly

R.1Show that a group of order 121 is Abelian.

(10)

R.2 Show that if f is a nilpotent element of the group algebra of a finite group Γ then $\hat { f } = 0$ Hint:Use the Fourier transform. (20）

R.3 Let the group Γ act on a family $\mathcal { F }$ and let X be the character of the representation resulting from this action.

a)Show that the multiplicity of the trivial in this representation is equal to the number of orbits of $\mathcal { F }$ under the action of I'. (20)

b） Show that the integer $\langle \boldsymbol { \chi } . \boldsymbol { \chi } \rangle$ (oumt.s tle number of orbits in the action of Γ on the family of ordered pairs (20)

$$
\mathcal { F } \times \mathcal { F } = \{ \{ f , g \} : f , g \in \mathcal { F } \}
$$

c） Suppose that $\Gamma$ acts transitively on $\mathcal { F }$ Let $f _ { \varrho }$ be an element of F, H be its stabilizer and let

$$
\Gamma = \frac { } { } F I \tau _ { 1 } H + H \tau _ { 2 } H + \cdot \cdot \cdot + H \tau _ { k } H
$$

be the double coset decomposition of Γ resulting from the equivalence relation

$$
\gamma _ { 1 } \sim _ { H } \gamma _ { 2 } \quad \Longleftrightarrow \quad \gamma _ { 2 } = h ^ { \prime } \gamma _ { 1 } h ^ { \prime \prime } \quad \mathrm { ( f o r ~ s o m e ) } h ^ { \prime } , h ^ { \prime \prime } \in H \mathrm { ) }
$$

Show that in this case $\langle \boldsymbol { \chi } , \boldsymbol { \chi } \rangle _ { \Gamma } = \mathcal { k } .$

(30)

Hint:Use part b)

R.4Give the expansion of the Garnir polynomial ${ \vec { G } } _ { { \mathcal { T } } } \{ x \}$ corresponding to the tableau

$$
T = \{ \begin{array} { c c c c c } & & { 7 } & & \\ & & & { } & \\ & { 1 } & { 1 } & { 5 } \end{array} \}
$$

in terms of the Garnir polynomiais of the standard lableaux of same shape.

(20)

R.5Let P(T) be the Young ideimpoteit corresponding to the row group of the tableau

$$
\begin{array} { c c c c c c c } { \mathit { \check { T } } } & { \mathit { \Psi } } & { \mathit { \Psi } } & { \mathit { \tilde { \Psi } } } & { \mathit { \Psi } } & { \mathit { \Psi } } & { \mathit { \Psi } } \\ { \mathit { \check { T } } } & { \mathit { \Psi } } & { \mathit { \Psi } } & { \mathit { \Psi } } & { \mathit { \Psi } } & { \mathit { \Psi } } & { \mathit { \Psi } } \\ & { \mathit { \Psi } } & { \mathit { \Psi } } & { 1 } & { \mathit { \Psi } } & { \mathit { \Psi } } & { \mathit { \Psi } } \end{array}
$$

Let $\chi$ be the character of the representation resulting from the action of $\mathit { S } _ { 7 }$ on the left ideal $\mathcal { A } \{ S _ { n } \} P ( \mathcal { T } )$ .Give the expansion of Yim terns of tle character basis $\{ \chi ^ { \lambda } \} _ { \lambda \vdash 7 }$

Hint:Use the Frobenius map.

(30)

## PART II

## SYMMETRIC FUNCTION THEORY

## 100 points gets you full credit more would be heavenly

ST.1Use the SF package to compute the two scaiar products

$$
\begin{array} { c l } { { a ) } } & { { \displaystyle \frac { 1 } { 5 ! } \sum _ { \sigma \in S _ { 5 } } \chi ^ { ( 2 \cdot 2 \cdot 1 ) } ( \sigma ) \ \chi ^ { ( 3 . 2 ) } ( \sigma ) \ \chi ^ { ( 3 . 1 . 1 ) } ( \sigma ) \ , \qquad b ) } } & { { \displaystyle \frac { 1 } { 5 ! } \sum _ { \sigma \in S _ { 5 } } \left( \chi ^ { ( 3 . 2 ) } ( \sigma ) \right) ^ { 2 } \chi ^ { ( 3 . 1 , 1 ) } ( \sigma ) } } \end{array}\tag{10}
$$

1）Give a representation theorelical interpretation of the resulting integers.

(15）

2）Could you have predicted the answer lor'b） given the answer for a)?

(20)

## ST.2 Show that

$$
\epsilon ^ { , t \cdots _ { 1 } } \quad \ F _ { 1 } \to \sum _ { \lambda } I ^ { [ \lambda ] } \frac { \cdot \varsigma _ { \lambda } } { \hbar _ { \lambda } }\tag{20}
$$

where $h _ { \lambda }$ denotes the product of t.he houks of 入.

ST.3Give a representation theoretical interpretation of the identity

$$
\begin{array} { r l r } { \mathcal { O } _ { \mu , \nu _ { 1 } } \ : \mathrm { e } ^ { \mu } } & { { } = } & { \hbar 2 \mathcal { C } _ { 1 } ^ { \pi - 1 } } \end{array}\tag{20}
$$

ST.4Compute the the Schur fumction expansion of $s _ { 2 2 } \ \times \ s _ { 2 , 1 }$ by constructing the standard tableaux yielded by the Littlewood Richardson rule. (20)

a) Check your result by means of the SF package.

b)Give a representation theoretical interpretation of the coeffcients in this expansion.(20)

ST.5Let P be the group of rotations of the cube.

1） Compute the Polya enumerator of the aci.ion of F on the edges of the cube.

(20)

2) Use the previous result to compute the number of ways to colour the edges of the cube using seven times RED and five times Yellow. (10)

3） Show that the polynoinial you coinputed in 1） is the Frobenius image of the character of a representation of $S _ { 1 2 }$ (10）

## PART III

## GROBNER BASES METHODS and INVARIANT THEORY

135 points gets you full credit more would be heavenly

GB.1 Use the partial fracion package of Guoce Xin to compute the generating function

$$
F _ { S _ { 2 } } ( \cdot ; x _ { 1 } , \cdot ; x _ { 2 } , \cdot ; x _ { 3 } , \cdot ; x _ { 4 } ) \quad = \quad \sum _ { p \in S _ { 2 } } x _ { 1 } ^ { p _ { 1 } } x _ { 2 } ^ { p _ { 2 } } x _ { 3 } ^ { p _ { 3 } } x _ { 4 } ^ { p _ { 3 } }
$$

where the sum is over the compositions $p = \{ p _ { 1 } , p _ { 2 } , p _ { 3 } , p _ { 4 } \}$ which are solutions of the Diophantine system

$$
\begin{array} { r } { S _ { 2 } = \left. \mathcal { P } _ { 1 } \right. + p _ { 2 } - p _ { 3 } - p _ { 4 } = 0 } \\ { \left. \mathcal { P } _ { 1 } - p _ { 2 } + p _ { 3 } - p _ { 4 } = 0 \right. } \end{array}\tag{20}
$$

GB.2 Use Grobner bases to find all solutions of the system of equations

$$
S _ { : 1 } = \begin{array} { l } { { 1 1 - x ^ { : 2 } \div y + z = 0 } } \\ { { \Big \downarrow } } \\ { { \dots \dots \langle y ^ { 2 } + z = 0 } } \\ { { \Big \downarrow } } \\ { { \Big \downarrow - x ^ { : } \ll y - z ^ { 2 } = 0 } } \end{array}\tag{20}
$$

GB.3 Construct the group G of $3 ~ \times ~ 3 ~$ mat rices by applying lhe Young natural representation indexed by

[2,1,1] to the permutations of $S _ { 4 }$

(10)

(1） Compute the Hilbert series $\widehat { F } _ { \mathbb { R } } c \colon ( q )$ of the ring of G-invariants

(5）

(2）Rewrite it in the form

$$
F _ { { \bf R } } \boldsymbol { \omega } ( q ) = \frac { 1 + q ^ { d } } { ( 1 - q ^ { d _ { 1 } } ) ( 1 - q ^ { d _ { 2 } } ) ( 1 - q ^ { d _ { 3 } } ) }\tag{20}
$$

(3)Calculate the first 10 terms of this series.

(5）

(4） Construct three hoinogeneous G invariants of degrees $d _ { 1 } , d _ { 2 } , d _ { 3 }$

(20）

(5）Compute the Grobner basis of the ideal $\{ \boldsymbol { J } _ { 1 } , \boldsymbol { J } _ { 2 } , \boldsymbol { J } _ { 3 } \}$

(5）

(6）Verify that $I _ { 1 } , I _ { 2 } , I _ { 3 }$ are a systein of parameters by checking that the quotient $\mathsf Q [ x _ { 1 } , x _ { 2 } , x _ { 3 } ] / ( I _ { 1 } , I _ { 2 } , I _ { 3 } )$ has finite dimension. If not go back to step (4). (5）

(7) Construct a G invariant 1 of degree d.

(10)

(9） Verify that it is uol. a polynomial im $l _ { 1 } , I _ { 2 } , I _ { 3 }$ by computing the polynomial Q such that $Q ( \eta , I _ { 1 } , I _ { 2 } , I _ { 3 } ) = 0 .$ (10)

(10) Based on your previous result.s show that step (9） is not needed. (20)

(11）Compute the Jacobian of $I _ { 1 } , I _ { 2 } , I _ { 3 }$ and construct its linear factors.

(5）

(12)Assuming thal there is a refection group $G ^ { \prime }$ that also leaves $I _ { 1 } , I _ { 2 } , I _ { 3 }$ invariant you could have predicted t.he nurmber of these linear factors. Why?. (20）