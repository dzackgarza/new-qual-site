# Preliminary Exam - Spring 1993

Problem 1 Let $\left( a _ { n } \right)$ and $\left( \varepsilon _ { n } \right)$ be sequences of positive numbers. Assume that $\scriptstyle \operatorname* { l i m } _ { n \to \infty } \varepsilon _ { n } = 0$ and that there is a number k in $( 0 , 1 )$ such that $a _ { n + 1 } \leqslant k a _ { n } + \varepsilon _ { n }$ $f o r$ every n. Prove that lim $a _ { n } = 0$

Problem 2 Let $A = \left( a _ { i j } \right)$ be an $n \times n$ matrix such that $\sum _ { j = 1 } ^ { n } \big | a _ { i j } \big | < 1$ for each i. Prove that $I - A$ is invertible.

Problem 3 Evaluate

$$
\int _ { - \infty } ^ { \infty } { \frac { x ^ { 3 } \sin x } { ( 1 + x ^ { 2 } ) ^ { 2 } } } d x .
$$

Problem 4 Suppose that the group G is generated by elements x and y that satisfy $x ^ { 5 } y ^ { 3 } = x ^ { 8 } y ^ { 5 } = 1$ . Does it follow that G is the trivial group?

Problem 5 Let k be a positive integer. For which values of the real number c does the differential equation

$$
\frac { d ^ { 2 } x } { d t ^ { 2 } } - 2 c \frac { d x } { d t } + x = 0
$$

have a solution satisfying $x ( 0 ) = x ( 2 \pi k ) = 0 \ ?$

Problem 6 Find a list of real matrices, as long as possible, such that

• the characteristic polynomial of each matrix is $( x - 1 ) ^ { 5 } ( x + 1 )$

• the minimal polynomial of each matrix is $( x - 1 ) ^ { 2 } ( x + 1 )$

• no two matrices in the list are similar to each other.

Problem 7 Let $f _ { n } : [ 0 , 4 ] \to \mathbb { R } \ ( n = 1 , 2 , . . . )$ be continuous functions that are twice continuously differentiable on (0, 4) and satisfy

$f _ { n } ( 1 ) = f _ { n } ^ { \prime } ( 1 ) = 0$

• $| f _ { n } ^ { \prime } ( x ) | \leqslant C$ for all x in (0, 4), where C is a constant, independent of n.

Prove that the sequence $\left\{ f _ { n } \right\}$ has a uniformly convergent subsequence.

Problem 8 Classify up to isomorphism all groups of order 45.

Problem 9 Let a be a complex number and $\varepsilon \textit { a }$ positive number. Prove that the function $\begin{array} { r } { f ( z ) = \sin z + \frac { 1 } { z - a } } \end{array}$ has infinitely many zeros in the strip $| \mathbb { S } z | < \varepsilon$

Problem 10 Let f be a real valued $C ^ { 1 }$ function on $[ 0 , \infty )$ such that the improper integral $\textstyle \int _ { 1 } ^ { \infty } | f ^ { \prime } ( x ) |$ dx converges. Prove that the infinite series $\textstyle \sum _ { n = 1 } ^ { \infty } f ( n )$ converges if and only if the integral $\int _ { 1 } ^ { \infty } f ( x ) d x$ converges.

Problem 11 Let P be the vector space of polynomials over $\mathbb { R }$ . Let the linear transformation $E : P  P$ be defined by $E f = f + f ^ { \prime }$ , where f 0 is the derivative of f . Prove that E is invertible.

Problem 12 Prove that for any fixed complex number $\zeta ,$

$$
\frac { 1 } { 2 \pi } \int _ { 0 } ^ { 2 \pi } e ^ { 2 \zeta \cos \theta } d \theta = \sum _ { n = 0 } ^ { \infty } \left( \frac { \zeta ^ { n } } { n ! } \right) ^ { 2 } \cdot
$$

Problem 13 Prove that no commutative ring with identity has additive group isomorphic to $\mathbb { Q } / \mathbb { Z }$

Problem 14 Prove that every solution $x ( t ) \ ( t \geqslant 0 )$ of the differential equation

$$
{ \frac { d x } { d t } } = x ^ { 2 } - x ^ { 6 }
$$

with $x ( 0 ) > 0$ satisfies $\operatorname* { l i m } _ { t \to \infty } x ( t ) = 1$

Problem 15 Let Λ be the set of ${ \mathcal { Q } } \times { \mathcal { Q } }$ matrices of the form

$$
\left( \begin{array} { c c } { { a } } & { { - b } } \\ { { b } } & { { a } } \end{array} \right) ,
$$

where a and b are elements of a given field F. Prove that Λ, with the usual matrix operations, is a commutative ring with identity. For which of the following fields F is Λ a field? $\mathbf { F } = \mathbb { Q } , \ \mathbb { C } , \ \mathbb { Z } _ { 5 } , \ \mathbb { Z } _ { 7 }$

Problem 16 Prove that $\frac { x ^ { 2 } + y ^ { 2 } } { 4 } \leqslant e ^ { x + y - 2 } f o r x \geqslant 0 , y \geqslant 0$

Problem 17 Prove that if G is a group containing no subgroup of index 2, then any subgroup of index 3 in G is a normal subgroup.

Problem 18 Let f be an analytic function in the unit disc, $| z | < 1$

1. Prove that there is a sequence $\left( z _ { n } \right)$ in the unit disc with lim $\operatorname { 1 } _ { n \longrightarrow \infty } \left| z _ { n } \right| = 1$ and $\scriptstyle \operatorname* { l i m } _ { n \to \infty } f ( z _ { n } )$ exists (finitely).

2. Assume f nonconstant. Prove that there are two sequences $\left( z _ { n } \right)$ and $( w _ { n } )$ in the disc such that li $\begin{array} { r } { \operatorname* { m } _ { n \to \infty } | z _ { n } | = \operatorname* { l i m } _ { n \to \infty } | w _ { n } | = 1 } \end{array}$ , and such that both limits $\scriptstyle \operatorname* { l i m } _ { n \to \infty } f ( z _ { n } )$ and $\scriptstyle \operatorname* { l i m } _ { n \to \infty } f ( w _ { n } )$ exist (finitely) and are not equal.