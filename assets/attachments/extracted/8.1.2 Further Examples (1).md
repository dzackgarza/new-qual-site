# Conformal Mapping: Further examples

Jingzhi Tie

University of Georgia

jtie@uga.edu

8150 Complex Analysis

March 30 - April 28, 2020

## Overview

<!-- image-->

## Conformal Map

<!-- image-->

2 1. Translation, Dilation and Rotation

<!-- image-->

2. Power functions

<!-- image-->

4 3. Upper half disc to the first quadrant

<!-- image-->

5 4. Logarithm z 7→ log z: H to Strip

<!-- image-->

6 5. Logarithm z 7→ log z

<!-- image-->

7 6. Exponential $z \mapsto e ^ { i z }$

<!-- image-->

7. Half disc to upper half plane

<!-- image-->

8. sin z the half strip to the upper half plane

We gather here several illustrations of conformal mappings. In certain cases we discuss the behavior of the map on the boundary of the relevant domain.

## 1. Translation, Dilation and Rotation

Translations and dilations provide the first simple examples. Indeed, if $h \in \mathbb { C }$ , the translation $z \mapsto z + h$ is a conformal map from C to itself whose inverse is $w \mapsto w - h$ . If h is real, then this translation is also a conformal map from the upper half-plane to itself. For any non-zero complex number $c ,$ the map $f : z \mapsto c z$ is a conformal map from the complex plane to itself, whose inverse is simply $g : w \mapsto c ^ { - 1 } w$ . If c has modulus 1, so that $c = e ^ { i \phi }$ for some real $\phi$ then f is a rotation by $\phi .$ . If $c > 0$ then f corresponds to a dilation. Finally, if $c < 0$ the map f consists of a dilation by $| c |$ followed by a rotation of $\pi .$

## 2. Power functions

$\boldsymbol { n } \in \mathbb { N } , z \mapsto z ^ { \boldsymbol { n } }$ is conformal from the sector

$\begin{array} { r } { S = \{ z \in \mathbb { C } : 0 < \mathsf { a r g } ( z ) < \frac { \pi } { n } \} } \end{array}$ to H with the inverse $w \mapsto w ^ { \frac { 1 } { n } }$ defined in terms of the principal branch of the logarithm.

$0 < \alpha < 2$ , the map $z \mapsto z ^ { \alpha }$ takes H to the sector

$S = \{ w \in \mathbb { C } : 0 < \mathsf { a r g } ( w ) < \alpha \pi \}$ . Indeed, if we choose the branch of the logarithm obtained by deleting the positive real axis and $z = r e ^ { i \theta }$ with $r > 0$ and $0 < \theta < \pi$ , then $f ( z ) = z ^ { \alpha } = r ^ { \alpha } e ^ { i \alpha \theta }$ . Its inverse is given by $w \mapsto w ^ { \frac { 1 } { \alpha } }$ . We need to choose the branch of the logarithm so that $0 < \mathsf { a r g } ( w ) < \alpha \pi$

Boundary behavior of f : If x travels from −∞ to 0 on the real line, then f (x ) travels from $\infty e ^ { i \alpha \pi }$ to 0 on the half-line determined by $\mathfrak { a r g } ( z ) = \alpha \pi$ As x goes from 0 to ∞ on the real line, the image f (x) goes from 0 to ∞ on the real line as well.

By composing the map just discussed with the translations and rotations in the previous example, we may map the upper half-plane H conformally to any (infinite) sector in C.

## 3. Upper half disc to the first quadrant

The map $\begin{array} { r } { f ( z ) = \frac { 1 + z } { 1 - z } } \end{array}$ takes the upper half disc   
$\mathbb { D } ^ { + } = \{ z = x + i \bar { y } : | z | < 1$ and $y > 0 \}$ conformally to the first quadrant $\mathbb { H } ^ { + } = \left\{ w = u + i v : \ u > 0 \right.$ and $v > 0 \}$ . Indeed if $z = x + i y \in \mathbb { D } ^ { + }$ ,i.e., $x ^ { 2 } + y ^ { 2 } < 1$ and $y > 0$ , then we have

$$
f ( z ) = { \frac { 1 + x + i y } { 1 - x - i y } } = { \frac { 1 - { \bigl ( } x ^ { 2 } + y ^ { 2 } { \bigr ) } + 2 i y } { ( 1 - x ) ^ { 2 } + y ^ { 2 } } } \in \mathbb { H } ^ { + } .
$$

The inverse $\textstyle g ( w ) = { \frac { w - 1 } { w + 1 } }$ . is clearly holomorphic in the first quadrant. Moreover, $| w + 1 | > | \dot { w } - 1 |$ for all w in the first quadrant because the distance from w to −1 is greater than the distance from w to 1; thus g maps into the unit disc.

$$
\displaystyle { g ( w ) = \frac { u + i v - 1 } { u + i v + 1 } = \frac { u ^ { 2 } + v ^ { 2 } - 1 + 2 i v } { ( u + 1 ) ^ { 2 } + v ^ { 2 } } \in \mathbb { H } }
$$

if $v > 0$

To examine the action of f on the boundary, note that if $z = e ^ { i \theta }$ $( 0 < \theta < \pi )$ belongs to the upper half-circle, then

$$
t ( e ^ { i \theta } ) = { \frac { 1 + e ^ { i \theta } } { 1 - e ^ { i \theta } } } = { \frac { e ^ { - i \theta / 2 } + e ^ { i \theta / 2 } } { e ^ { - i \theta / 2 } - e ^ { i \theta / 2 } } } = { \frac { 2 \cos { \frac { \theta } { 2 } } } { - 2 i \sin { \frac { \theta } { 2 } } } } = i \cot { \frac { \theta } { 2 } } .
$$

AS θ travels from 0 to $\pi , f ( e ^ { i \theta } )$ travels along the imaginary axis from ∞ to 0. $\textstyle f ( x ) = { \frac { 1 + x } { 1 - x } }$ is a bijection from (−1, 1) to $( 0 , \infty )$

## 4. Logarithm z 7→ log z : H to Strip

The map $z \mapsto$ log z, defined as the branch of the logarithm obtained by deleting the negative imaginary axis, takes the upper half plane H to the strip $\left\{ w = u + i v : \ u \in \mathbb { R } , \ 0 < v < \pi \right\}$ . Let $z = r e ^ { i \theta }$ with $\begin{array} { r } { - \frac { \pi } { 2 } < \theta < \frac { 3 \pi } { 2 } } \end{array}$ then by definition,

$$
\log z = \log r + i \theta .
$$

The inverse map is then $w \mapsto e ^ { w }$ . As x travels from −∞ to 0, the point $f ( x )$ travels from $\infty + i \pi \ { \sf t o } \mathrm { ~ - } \infty + i \pi$ on the line $\{ x + i \pi : \ - \infty < x < \infty \}$ . When x travels from 0 to $\infty$ on the real line, its image f (x) then goes from −∞ to ∞ along the reals.

## 5. Logarithm z 7→ log z : Half disc to the half strip

z 7→ log z also defines a conformal map from the half-disc $\left\{ z = x + i y : \ | z | < 1 , \ y > 0 \right\}$ to the half-strip $\left\{ w = u + i v : \ u < 0 , \ 0 < v < \pi \right\}$ . As x travels from 0 to 1 on the real line, then log x goes from −∞ to 0. When x goes from 1 to −1 on the half-circle in the upper half-plane, then the point log z travels from 0 to πi on the vertical segment of the strip. Finally, as x goes from −1 to 0, the point log x goes from πi to −∞ + πi on the top half-line of the strip.

6. Exponential $z \mapsto e ^ { i z }$ : Half strip to half disc

The map $f ( z ) = e ^ { i z }$ takes the half-strip

$\{ z = x + i y : \ - { \frac { p i } { 2 } } < x < { \frac { \pi } { 2 } } , \ y > 0 \}$ conformally to the half-disc $\{ w = u + i v : ~ | w | < 1 , ~ u > 0 \}$ . If $z = x + i y$ , then

$$
e ^ { i z } = e ^ { - y } e ^ { i x } .
$$

Boundary behavior; If z goes from $\frac { \pi } { 2 } + i \infty$ to $\frac { \pi } { 2 }$ , then $f ( z )$ goes from 0 to $i ,$ and as x goes from $\frac { \pi } { 2 }$ to $- \frac { \pi } { 2 }$ , then $f ( x )$ travels from i to −i on the half-circle. Finally, as z goes from $- \frac { \pi } { 2 }$ to $- \frac { \pi } { 2 } + i \infty$ , we see that $f ( z )$ travels from −i back to 0. The mapping f is closely related to the inverse of the map in Example 5.

## $z \mapsto - { \frac { 1 } { 2 } } { \big ( } z + { \frac { 1 } { z } } { \big ) }$ : Half disc to upper half plane

The function $\begin{array} { r } { f ( z ) = - \frac { 1 } { 2 } ( z + \frac { 1 } { z } ) } \end{array}$ is a conformal map from the half disk $\left\{ z = x + i y : \ | z | < 1 , \ y > 0 \right\}$ to the upper half plane H. Boundary behavior: If x travels from 0 to 1, then f (x) goes from ∞ to 1 on the real axis. If $z = e ^ { i \theta }$ , then $f ( e ^ { i \theta } ) = -$ cos θ and as z travels from −1 to 1 along the unit half circle in the upper half-plane, f (z) goes from 1 to −1 on the real segment. Finally, when x goes from −1 to 0, f(x) goes from 1 to −infty along the real axis.

## 8. sin z : the half strip to the upper half plane

The map $f ( z ) = \sin z$ takes the half-strip   
$\left\{ w = x + i y : - { \textstyle { \frac { \pi } { 2 } } } < x < { \textstyle { \frac { \pi } { 2 } } } , ~ y > 0 \right\}$ conformally onto the upper   
half-plane. If $\zeta = e ^ { i z }$ , then

$$
\sin z = { \frac { e ^ { i z } - e ^ { - i z } } { 2 i } } = { \frac { 1 } { 2 } } \left( - i \zeta + { \frac { 1 } { - i \zeta } } \right) ,
$$

and therefore f is obtained first by applying the map in Example 6, then multiplying by −i (that is, rotating $\mathsf { b y } - \frac { \pi } { 2 } )$ , and finally applying the map in Example 7. Boundary behavior: z travels from $\begin{array} { r } { - \frac { \pi } { 2 } + i \infty \ t \circ - \frac { \pi } { 2 } , \ f ( z ) } \end{array}$ goes from −∞ to −1. When x goes from $- \frac { \pi } { 2 }$ to $\textstyle { \frac { \pi } { 2 } } , f ( x )$ goes from −1 to 1. Finally, if z goes from $\frac { \pi } { 2 }$ to $\frac { \pi } { 2 } + i \infty$ , then $f ( z )$ travels from 1 to ∞ on the real axis.