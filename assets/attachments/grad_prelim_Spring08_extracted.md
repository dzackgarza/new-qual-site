# PrELIMinary ExAm, SprinG 2008

(3 hours, 8 problems counted equally)

1. Suppose $f , g , h : A \longrightarrow A$ are functions. Prove that if  is injective and $f \circ g = f \circ h$ then $g = h$

2. Prove from the $\epsilon { - } \delta$ definition that the function $f : \mathbb { R } \to \mathbb { R }$ defined by $f ( x ) = x ^ { 3 }$ is continuous at 2.

3. State both parts of the Fundamental Theorem of Calculus and outline a proof of one of them.

4. Let  be a linear transformation on a finite-dimensional vector space  . Suppose $x _ { 1 } , x _ { 2 } , \ldots , x _ { n }$ Vis a basis for  with the first  of those vectors being a basis for the x , x , . . . , xkernel of . Prove that $T x _ { k + 1 } , T x _ { k + 2 } , . . . , T x _ { n }$ is a basis for the range of .

5. Let $A : = { \left[ \begin{array} { l l l } { 2 } & { 1 } & { 1 } \\ { 1 } & { 2 } & { 1 } \\ { 1 } & { 1 } & { 2 } \end{array} \right] }$ . Find an orthogonal matrix $Q$ such that ${ \cal Q } ^ { - 1 } A { \cal Q }$ is diagonal.

6. Let $f : \mathbb { R }  \mathbb { R }$ have derivatives of all orders and suppose $f ( x ) = 0$ for each finteger . Prove that for each $n \in \mathbb { N }$ , the $n ^ { \prime } t h$ f xderivative of  has at least one root.

7. Determine, with proof, which if any of the following functions mapping $\mathbb { R } ^ { 2 }$ into R are continuous.

a)

$$
f ( x , y ) = { \left\{ \begin{array} { l l } { { \frac { x y } { x ^ { 2 } + y ^ { 2 } } } , \quad } & { ( x , y ) \neq ( 0 , 0 ) } \\ { 0 , } & { x = y = 0 } \end{array} \right. }
$$

b)

$$
g ( x , y ) = { \left\{ \begin{array} { l l } { { \frac { x ^ { 2 } y } { x ^ { 2 } + y ^ { 2 } } } , } & { \quad ( x , y ) \neq ( 0 , 0 ) } \\ { 0 , } & { \quad x = y = 0 } \end{array} \right. }
$$

8. Give examples of the following. No proofs are required.

a) open sentences $P ( x )$ and $Q ( x )$ such that the statement $\forall x [ P ( x ) \lor Q ( x ) ]$ i s P xtrue, but the statement $[ \forall x { \dot { P } } ( x ) ] \vee [ \forall x Q ( x ) ]$ is false,

b) a (real) power series whose domain of convergence is the half-open interval $[ 0 , 4 )$ .

c) a sequence $\left( f _ { n } \right)$ of real valued functions from R to R which converges pointfwise, but not uniformly.