extends: existence
message: "Avoid '%s' — state the reasoning explicitly."
level: warning
ignorecase: true
tokens:
  - obviously
  - obvious
  - clearly
  - trivially
  - trivial
  - straightforward
  - immediate
  - reader
