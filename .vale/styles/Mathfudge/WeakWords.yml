extends: existence
message: "Avoid '%s' — state the reasoning explicitly."
level: warning
ignorecase: true
tokens:
  - obviously
  - clearly
  - trivially
  - it is trivial
  - this is trivial
  - it is obvious
  - this is obvious
  - straightforward
  - it is immediate
  - this is immediate
