extends: existence
message: "Avoid '%s' — state the reasoning explicitly."
level: warning
ignorecase: true
tokens:
  - obviously
  - clearly
  - trivially
  - trivial
  - straightforward
  - immediate
